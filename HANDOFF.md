# HANDOFF

**这不是文档，是上一个 agent 写给下一个 agent 的信。要短、要具体、只写下次用得上的。**
不写背景（背景在 `CLAUDE.md`），不写论证（论证在 `docs/`）。

**与 `docs/TODO.md` 的分工——两者不重复：**

- `docs/TODO.md` = **任务队列 + 文档地图**。长期、面向「下一步做什么」和「去哪找论证」。
  结论一旦稳定就写进它，它是权威来源。
- `HANDOFF.md` = **会话交接**。短期、面向「上次做到哪、什么半途而废、此刻有什么坑」。
  一条信息一旦稳定到值得进 `docs/TODO.md`，就搬过去，别留在这里。

规矩：

- 新会话结束时加一节，**最新的在最上面**。
- 只保留最近 3 节，更旧的直接删掉（历史在 git 里，不用囤在这）。
- `PENDING` 是下一个 agent 开工的第一件事，必须是可执行的动作，不是「继续优化」。
- 教训写 `MEMORY.md`，不写这里：HANDOFF 会过期，教训不会。

格式：

```markdown
## Session YYYY-MM-DD
- 完成: ...
- PENDING: ...        ← 下次第一件事
- 坑: ...
```

---

## Session 2026-08-03 深夜（R9 → §11 → R10，`/loop` 自主长跑）

- 授权: 用户给总体目标「小型生态群落 + 性状演化」，opus5 执行 / **fable5 审查**，
  最后一句是「我去睡觉了，请你继续往前迭代」。

- **走到哪了（三层，一层比一层深）**:
  1. **R9（96 run）**: 结构化草层造出**空间生态型关联**（原始 r 的 `T1−N1` = −0.2697，
     **12/12**，p=0.00049 地板，效应/噪声 −1.672）——本纲领第一个空间生态型信号。
     但基因**均值**动了（+0.0607，12/12）、**方差没动** ⇒ **一条 cline，不是两个物种**。
     判决 `feasibility.md §10`。
  2. **§11（代数，本 session 最重要的一条）**: `_forage_pref_scale` 是**直线**权衡前沿，
     `W(s)=A·g+B·f` 对 `s` 线性 ⇒ **`d²W/ds² ≡ 0`**。分歧选择**没有一阶项可以来自**，
     与果层份额、与交配结构都无关。**一次解释掉五条归档阴性**（§9.6/§9.10/§9.11/§11/§12）。
     装了 `forage_curvature`（默认 1.0、bit-exact、golden 未动，10 项单测）。
     **出自 `plan-critic` 对一个被否掉方案的审查——被毙的方案带出了比它本身重要得多的东西。**
  3. **R10（96 run）判决**: **不是「弯前沿没用」，是它能注入的曲率只有基线负曲率的一半。**
     可注入 `(1−k)(A+B)` = +0.087（K05），**实测基线 `W''(k=1) = −0.137`**（t=−4.14）。
     完美交付后仍 −0.050，**依然凹**。§14.6 触发 **undecidable**，
     **但补功效没有意义**（预测效应 0.001839 ÷ MDE 0.008787 = **0.209**）。判决 `feasibility.md §12`。

- **正在跑**: `bash explorations/20260803-curvature/probe_saturation.sh`（24 run，探针非结论）
  → `outputs/20260803-curvature/sat/`。问**基线凹度从哪来**，两条互斥的降饱和路径：
  `S1` 降 `eat_rate`（**不加食物**）、`S2` 抬 `regrow_baseline`（加食物）。
  **只有 S1 也让 `W''→0`，供给饱和机制才坐实。**

- **PENDING（下次第一件事）**:
  1. 探针跑完 → 读 `W''`（各臂 `quad_intake` 的原始单位换算，或直接比 `quad_intake`）。
  2. **坐实** ⇒ 在松开饱和的世界里重跑曲率扫描（预注册**必须写「预测效应 ÷ MDE」**）。
     **没坐实** ⇒ 基线凹度另有来源，先找到它，**别重跑曲率扫描**。

- **坑**:
  - **不许降 `k` 再试**（§14.6 禁令）——瓶颈是基线凹度不是 `k`。
  - **不许把 R10 读成「证伪」或「显著反向」**——比值 0.717、7/12、p=0.092。
  - `quad_demog` **已弃用**（中性臂读 +0.0763 而真值必须恰为 0）。
  - **拿小 pilot 定 MDE 会低估**（R10 低估 54%，8 run 的 `σ̂` 自由度只有 ~4）。
  - **这张卡是共用的**，用户白天在上面跑过 18.4 GiB 的任务。

---

## Session 2026-08-03 下午（R9，`/loop` 自主长跑，用户在忙别的任务）

- 授权: 用户开场给的是**总体目标**而非具体任务——「小型生态群落 + 性状演化」，
  「按照这个目标一直往下进行推进」「可以提出并实践一些新奇的 idea」，
  并指定 opus5 执行、**fable5 做审查**。§12.5 等的就是这个签字。

- **已完成并推送**：
  - `2bb48e0` **两个默认关、bit-exact 的地形旋钮**：`grass_shade`（林下遮荫，等量归一化）
    与 `water_sea_dist`（`water_dist = min(d_river, d_sea)`）。11 项单测绿，
    `check.py --full` 42 项绿、golden 未动。
  - `231c016` **§13 预注册**（判据、护栏、失败判据、放弃条件），跑之前提交。
  - `abf8b97` sweep 加显存闸 + 断点续跑；教训进 `MEMORY.md [LEARN:env]`。

- **本轮真正的产出是两条地形级发现，它们在花掉任何 GPU 时间之前就成立**：
  1. **实现与自己声明的设计意图反号**。`config.py` 在 `fruit_max` 上方写「林冠遮蔽草本层
     ⇒ 林下是低放牧底 + 高值例外」，而 `terrain.py` 给满冠层 **1.538×** 的草承载——
     **林冠是全世界草最肥的地方**，而果层又由 `forest²` 门控长在那里。
     默认世界里「果承载前 10% ∩ 草承载 <30% 陆地均值」的格子是 **0 个**；
     `grass_shade=1.3` 是 **173 个（80.5%）**。
  2. **`water_dist` 不是它声称的量**。字段注释写 "distance to nearest river/sea"，
     实现是 `where(is_sea, 0, d_river)`——到海的距离从未被计算。**喝水那半是对的**
     （站上海格就能喝），坏的是**梯度**：海只有岸线处一个阶跃，有效探测距离约 11 世界单位
     对河的 30。27.2% 陆地格水距被高估，中位 43.3；紧贴大海的 376 个陆地格中位水距 **54.7**。
     自 `32c983f` 起未改过，`docs/` 里没有任何「刻意」的记录。
  - **两者合起来**：单独遮荫能分离但把草推离水（重心 49.75→59.27，撞水墙）；单独认海
     不分离但把草拉近水（→37.22）。**一起上（shade=1.3）是分离 0.125 且重心 44.15
     ——低于基线。八个回合里第一次「分离」与「可达」不再对立。**

- **判决已出（96 run，0 错误）**：**分离做到了，物种没做出来。** 全文
  `docs/multispecies_feasibility.md` §10，纲领判决行 §13.7。
  - **正结果**：遮荫世界里选择造出稳健的生态型—生境关联，原始 `r` 的 `T1−N1` = **−0.2697**、
    **12/12 种子**、p=0.00049 触地板、效应/噪声 **−1.672**。**本纲领九个回合里第一个空间
    生态型信号**，遮荫把它放大 **5.29×**。
  - **但不是分化**：基因**均值**动了（+0.0607，12/12），**方差与双峰性没动** ⇒
    **方向性选择 + 空间分选 = 一条 cline**，不是两个物种。
  - **根因一个数**：果层只占陆地能量 **11.15%**，一次解释完三个结果。
  - **`result-analyst` 推翻了我两处**：①「塌缩被地形解除了」站不住（与交互项 p=0.339
    是同一件事，且 CI 含塌缩量）；②预注册 H3 **符号写反**（`forage_pref` 是**草**偏好基因）。
  - **失败判据不触发**：H1/H2 功效仅 0.159/0.216，判 undecidable。
    但这暴露**第二次重犯 §11.5**：护栏容差对着噪声定了，**主判据没算 MDE**。

- **PENDING（下次第一件事）**:
  **要先论证，不要直接开跑。** `feasibility.md §10.10` 排了三条，第 1 条不自动成立：
  1. **换自变量：攻果层能量占比（11.15%），不再攻分离度**（已到顶）。**但「加厚果层」是
     `experiments.md §5` 已关闭的路线**——区别在于那条是**共址**几何下加厚。
     几何已经分开了，加厚的去向不再是同一个碗。**这个区别必须先写成可证伪的论证再开跑。**
  2. **H3 交互补种子**（只需 `s≈14`，补 seed 12–13 共 8 run）。**但这是看到结果后才决定的
     样本量增补**，若做必须单独预注册并同时报 12 与 14 两个 n。
  3. **R9-E 跨地形**，脱离伪重复（H3 目前只对这条河系 + 这条海岸线成立）。

- **坑（本轮新踩的）**:
  - **这张卡是共用的。** 用户的 `bench_train_v21.py --slug v21-throughput-rerun` 占
    18.4 GiB / 24 GiB。我按 6 并发起 sweep，头 11 个里 3 个死于 OOM，**而且在污染对方的
    吞吐量读数**——已停掉全部自己的 run、清空受污染日志、等对方跑完再开。
  - **`water_sea_dist` 翻默认值会翻转 `biology.md:156` 的核心不等号**（全格离水中位
    35.46 → 27.45，跌破感知上限 30），并作废 `fruit_dry_d0/d1` 的标定。逐条清单在 §13.2。
    **本轮默认关，翻不翻由 R9 数据定。**

---

## Session 2026-08-03（用户拍板的两件事，`/loop` 自主执行）

- 授权: 用户拍板执行两件、要求**照着已写好的计划做不要重新设计**、判决派 `result-analyst`、
  撞到要拍板的岔路写进 PENDING 并跳过不要停、做完一件就提交推送。

- **① 统计协议升级 —— 完成并推送**（`2e49b8b` / `33a37d4` / `f46846a`）
  - 协议 **6 配对种子 → 12 种子 × 2 重复**。三条分析口径（格均值 → 效应/噪声比 → 护栏容差
    对着 `√2·σ̂_W` 定）写进 `CLAUDE.md`、`conventions.md §5`、`TODO.md`、`/exp` skill，
    以及**环路上其余六份 agent/skill**（只改 §7.2 那五处落不了地——真正下判决的是
    `result-analyst`，它整节还是旧协议）。
  - **实现收进 `scripts/exp_stats.py`**（三条口径的唯一实现，写新 `analyze.py` 时 import 它）。
    回归验收 `explorations/20260803-partition/verify_exp_stats.py` 用 90 个历史 log 复算 P4，
    与 §9.11 已发表的每个数逐位一致。**改这个库后必须重跑它。**
  - 途中修了自己一个错：功效函数第一版拿「全部种子同向」的概率解样本量，那个概率随 n
    **下降**，于是小效应报出更少种子。已改成配对 Wilcoxon 精确零分布做蒙特卡洛。

- **② 把果层挪出共居带 —— A、B 两阶段完成，C 待跑**
  - **A 判决（§12.3.A'，48 run）：我预注册的猜想被自己的数据推翻。** 默认世界里重叠是
    **纯几何**的（`sel_ratio_water` 1.009，p=0.151，CI 含 0），但在**厚果层世界里是行为性的
    且很强**（**3.712**，12/12，p=0.00049，效应/噪声 **8.73**）。**C/D 的基线因此固定为
    `niche` 臂**——`base` 的「可下降空间 ÷ MDE」只有 0.52，手术做对了也测不出来。
  - **B 落地（§12.3.B）：`fruit_dry_weight` 旋钮已装**，默认 0、bit-exact、golden 未动、
    单测 11 项绿。**装的时候拦下一个会毁掉 C/D 归因的混杂**：朝干旱带 blend 会把果层总承载
    抬 **+91%**（`forest²` 窄带再平方到处都小，`dry` 在四分之一张图上饱和），已加容量归一化
    做成等量再分配。§12.4 风险 3（果层碎裂）**没有发生**（有效格 792→726）。

  - **C 判决：没有可用工作点，按 §12.3.C 收口为负结果，D 不跑。** 决定性的一条不在种群上，
    在地形上：`fertility = grass_base + forest_bonus·forest` 且 `grass_base=0.65`，
    所以 **`fertility ∈ [0.65,1.0]`，草层动态范围结构性封顶 1.538×**，铺满整张地图。
    把果层推到最远，它所在格子的草量仍是陆地均值的 **0.966 倍——没有一处「有果实、没有草」**。
    `w=0.25` 是自带反证：地形间距≈0 而 `herb_water_dist` 照样 +4.93 SE，
    **没有分离照样能把种群赶出去**。护栏只破渴死（+3.98 SE，绝对量 +37.6%），
    `frugivory_frac` 一个剂量都没塌，`carnivore_frac` 重锚后未破（先前读出的「崩了」是
    n=3 参照臂被单个 run 绑架的假象）。**负结果已写进 `experiments.md §5.6`。**

- **PENDING（下次第一件事）= 要用户拍板，不要擅自开跑**:
  **三条资源轴（加厚 / 等量再分配 / 空间分隔）已全部走完并全部关闭。**
  合并结论：**在草层铺满整张地图的前提下，「把两个饭碗在空间上拉开」本身做不到**——
  能挪的只有果层，而另一个饭碗无处不在。
  **唯一还没试过的方向不是果层，是草层**：压低 `grass_base`、抬高 `forest_bonus`，
  让 `fertility` 有真正的动态范围。**但这会改变默认世界的每一个既有结论**
  （`plant_max` 的全部调优记录、golden 全部数字、食草种群的空间分布），
  属于重设世界一级的决定。§12.5 写的「放松水本身」也是同一量级。**两条都交用户定。**

- **坑（都会咬人）**:
  - **sweep 跑的时候绝不能改 `underworld/`**——sweep 是逐批启动进程的，改了后面几批会加载到
    不同的代码，而且不会有任何报错。
  - **`carnivore_frac` 的 2×噪声占基线 71%、`population` 在 niche 世界 `σ_B=0`**——
    **C 的单种子探针只能读「有没有塌到 0」，不能读「有没有变差」**。
  - **主口径 `sel_ratio_water` 的零模型用的是处理后变量**（实测个体自己的水距分布），
    所以它回答不了「种群有没有离开河岸」，那个必须另读 `herb_water_dist`。
  - **48 个 run 全在同一张地图上**，A 的全部结论都是空间性的 → **§12.3 E 的跨地形交叉是
    必须的，不是可选的**。
  - 终端刷屏会把 `ghostty` 刷到 80%+ CPU，进而把 `check.py --full` 从 256s 拖到 10 分钟以上
    （套件是**主机 CPU 编译**瓶颈）。长输出一律重定向到文件。

---

## Session 2026-08-02→03（夜间自主，用户睡前授权：局部多物种生态箱 + 可视化）

- 授权: 用户定盘长期目标「**局部的多物种生态箱演化 + 可视化结果**」，要求**先把工作流设计成
  一个循环**再一直跑下去、别干一半停，并授权分 agent 做测评。也交代了 push 走 `asada-sinon`
  （已设 repo-local 身份 `Asada-Sinon <weibinkong.research@gmail.com>`，与全部历史提交一致）。
- **工作流已定死**: `docs/multispecies_program.md`——纲领 §1 / **七道闸 §2** / 回合台账 §3 /
  backlog §4。下个 session **从 §4 backlog 顶部取题**，不要另起炉灶。
- 完成（生态线，5 个回合全部落档 + push）:
  - **纲领**: 多物种三次失败是同一根因的三个投影——**这个世界只有一个饭碗（河岸的草）**。
    顺序定为「先造生态位，再谈物种」。
  - **R1 判据自身被证伪**（§5）: 现存量量的是「剩多少」不是「供多少」，把生态位低估约一个
    数量级。产出 = `Metrics` 三个流量观测量 `graze_gain`/`fruit_gain`/`frugivory_frac`。
  - **R1b 不通过**（§7）: 份额做到 0.22（6/6、p=0.031）但渴死护栏破 2–3/6。顺带更正自己一句
    过度解读（林冠移动对着零模型读几乎可忽略）。
  - **R1c 不通过且我的诊断被推翻**（§8）: 真因是**承载力**不是水口径——种群超调与渴死增量
    ρ=+0.90，`mean_water` 三臂对基线全无显著变化。
  - **§10 跨回合命题**: **水限世界里加食物只会变成更多身体。** 「加厚食物层造生态位」整条路
    证伪，负结果已进 `docs/experiments.md §5`。
  - **R3 判为「无法判定」**（§11）: 字面上不通过，但**判据容差 ≈ 1 个复现噪声 SD**，通过/不通过
    都没有信息量。**资源轴挂起，不宣布关闭也不宣布成功。**
- 完成（**方法学，本轮真正的头号产出**）: `docs/run_to_run_variance.md`
  - **`--seed` 在 20000 步上控制不住这个世界。** 18 个默认配置 run（3 重复 × 6 种子）方差分解：
    `late_carn`/`death_thirst_frac`/`total_flux` 的**创始者方差分量为 0**，`carnivore_frac`
    ICC 仅 0.130、`population` 0.259。同一个种子能跑出 `carnivore_frac` 0.060–0.2505。
    seed 3 复现（n=6）同量级或更大——**普适，不是某颗种子的特例**。
  - 后果：**配对差噪声 = `√2 × 同种子 SD`**，`carn_frac` 是 0.0625，而 `conventions.md §5` 用的
    跨种子 SD ±0.012 **低估 5.2 倍、样本量低估约 27 倍**。ICC≈0 的指标**配对完全无用**。
    `conventions.md` 已补 §9.1（这是事实更正，不是规则变更）。
  - §6 给每条旧结论标了「效应量/噪声」比值：**红皇后 2.4、armor 1.19 站得住；恐惧场 0.32、
    pred_nocturnal 0.53 降级待重测**。**已发表的 p 值本身没错**（算的是实测配对差）。
- 完成（可视化交付，用户点名要的）: Artifact
  **https://claude.ai/code/artifact/285960f6-abfd-465f-8d82-e50cdc61bf6d**（默认私有）——
  五个回合的判决、基线 vs 再分配的**两张世界对照图**、三张图表（剂量响应 / 方差分解 / 旧结论
  重估）。另外**仪表盘现在能接 `--set`** 了（`UNDERWORLD_SET` 环境变量），
  例：`.venv/bin/python scripts/run_live.py --no-open --set fruit_energy=4.0`。
  - **R2 完成，P2 不成立且失败方式不是预设的那种**（§9.5–§9.9）: 在果层真供 28% 能量的世界里
    把权衡强度开到最大，`forage_pref` 的方差**比中性漂变对照低 38.3%**（6/6、p=0.031、
    效应/噪声 −1.42），两臂直方图都是单峰同峰位。**基因流只能把处理臂压回等于中性、不能压到
    低于中性——所以这是稳定化选择主动收窄，不是基因流被动压平。** 同时证否了
    `feasibility.md §9.5` 的归因（不是「没饭碗」，是「中间型最优」）。
    **[提案，非结论] 为什么中间最优**：果层长在 `forest²` 门控的中海拔近水带，
    正是所有个体本来待的地方，两种资源空间交织 → 「两样都能吃」严格优于专精。
    **教训改写为：资源分割需要的不是「第二个饭碗」，而是「两个饭碗离得够远」。**
  - **P3 剂量项补齐**（§9.10）: 加跑 `forage_tradeoff=0.5`。**单调，无分化窗口**——
    `sd` 0.0951→0.0703→0.0587，`M>P`（歧化方向）的种子数 **0/6**。R2 不是跨过头。
    三条限制一并记了：`M−P` 的 p=0.031 在中位数口径下失守（不稳，但方向 6/6 可信）；
    只测三点、窄窗口未排除；伪重复（果层由 forest 派生）。
  - **P4 窄窗口收口**（§9.11）: 补 0.125 / 0.25 两点凑成**五点曲线、90 run**。
    **没有任何一点的 `sd` 高于中性漂变 → 分化窗口不存在，R2 没跨过头。**
    两处措辞被分析 agent 纠正、已按纠正写入：**不能说「五点单调」**（N−L 抬头 5/6、
    CI 不含 0）；**不能说「N(0.25) 更窄」**（负号来自单个离群 run，剔掉 seed 0 就翻正，
    它是 NULL）。**边界**：只排除了幅度 ≥24–41% 的窗口，更窄的本设计看不见。
- **✅ 用户已拍板（2026-08-03 晨）：执行下面两件，计划已写成可执行的阶段清单。**
- **PENDING（下个 session 第一件事，严格按序）**:
  1. **升级统计协议** —— `docs/run_to_run_variance.md §7`。**这是第 2 件的先决条件。**
     新默认 **12 种子 × 2 重复**（不是我最初说的 6×3——§7.0 有算术：`s` 和 `r` 对精度贡献
     相同，但 **p 下限与伪重复只由 `s` 决定**，`s=6` 时最小双侧 p 永远是 0.031）。
     §7.2 有逐文件落地清单，**头号是 `.claude/skills/exp/SKILL.md` 第 2 步**——
     那份 skill 才是 agent 跑实验时真正照着做的东西。
  2. **把果层挪出共居带** —— `docs/multispecies_program.md §12`。病根已定位到
     `terrain.py` 一行：`forest = elev_band * water_prox`，所以果层的栖息地项本身就是
     「离水近」做出来的，与所有个体的居住带完全重合。§12 有 A–E 五个阶段、
     默认关 bit-exact 的旋钮规格、先写后跑的判据、以及三条预标风险。
     **A 阶段（零成本量重叠度）先做，没有那个基线数字后面无从判定。**
  第 3 条备选（时间轴）暂不做，留在 §4 backlog。
- 状态：八个回合全部走完、全部落档；此刻没有在跑的实验，工作树干净，全部已 push。
- **要用户拍板（都会改默认世界行为或改变后续所有工作的成本）**:
  1. **先定统计协议**（`run_to_run_variance.md §7`）：每格 k=3 重复。代价 run 数 ×3
     （一轮 30 分钟 → 90 分钟）。**这是先决条件**——不定它，后面任何判决继续是抛硬币。
  2. **把两个饭碗在空间上分开**（§9.9 直接推出的唯一活路）：动 `terrain.py:207` 的
     `fruit_capacity = fruit_max × patch × forest² × (1−rock)` 门控，把果层挪到高海拔或远水带。
     代价：改地形派生量 → golden 重 bless；且会与水限约束正面相撞。
  3. **或走时间轴**（同一份资源、不同取用时段，零新增承载力）。`day_length` 已默认开、
     `scripts/probe_diel.py` 可复用。比空间轴便宜，但 `day_night.md` 已判「空间通勤不可达」，
     时间生态位是否可达未知。
- **⚠️ 已划掉、不要再提的三条**（前提都被数据推翻，别让下一个 agent 重捡）:
  - ~~加厚食物层造生态位~~（证伪，`experiments.md §5`）
  - ~~等量再分配~~（被 R2 从上游解决：供给量根本不是瓶颈）
  - ~~生殖隔离 `assortative_mating` on `forage_pref`~~ —— **它对抗基因流，而实测是稳定化选择。
    这条是我在 §9.4 预设的失败子句，已被 §9.7 推翻。**
- 坑:
  - **Bash 工具跑的是 zsh，未加引号的变量不做词分割**。多参数 sweep 一律写成
    `bash <script>.sh` 并用数组传公共臂（`D=(--set a=1 --set b=2)` + `"${D[@]}"`），
    否则整串被塞给 argparse、秒退。已进 `MEMORY.md [LEARN:tooling]`。
  - **确定性 XLA 算子（`--xla_gpu_deterministic_ops=true`）在 20000 步上 >20× 慢**，
    100 分钟跑不完，别再试。「它能否消除散度」**仍未测**，别写成已知。
  - **`fruit_water_frac` 是本轮新增的默认关旋钮**（默认 0.10 = `forage_water_frac`，逐位等价，
    golden 未动）。`fruit_energy` 抬高时果实每卡路里会变干，要用它补。
  - `outputs/20260803-*` 与 `20260802-*` 下有本轮全部产物（gitignored）；分析脚本在
    `explorations/20260802-fruit-flux/`、`20260803-fruit-water/`、`20260803-repartition/`。

## Session 2026-07-25（白天，用户在场：三通路落地 + 6 种子判决）

- 承接夜间 PENDING（用户 #4 多性状 / #5 种间合作），用户拍板**三个方向全做**、分 worktree agent 并行实现。
- 完成（三方向已并入 main 三独立提交、已 push）:
  - **实现**：`cbce9fc` forage_pref（草↔果权衡，trait_dim 7→8）、`1964949` 腐食接 food retina（不动 in_dim）、
    `c25ba60` 共享报警场 alarm（独立场、折进 pred）。三者均默认关（forage 付 golden 重 bless、A/C bit-exact）。
    整合冲突手工解（doc 章节 §7→§8→§9 重排、metrics/tests keep-both）。check.py/contracts/pytest 全绿。
  - **6 种子判决**（36 runs、6 并发、20000 步，各派 result-analyst）——**三者全维持默认关**：
    - **腐食 v2 = 弱阳性**：carn_frac ON>OFF 4/6、p=0.156（未达 6/6 硬标准）；**min 首次抬高 0.098→0.127
      （抗灭绝成立，胜首版 min 反降）**；carrion_total 198≈首版 204（主动觅食**没**加速消费）；pop −80 弱负。
      → 不够默认开。`feasibility.md §7 第二版结果`。
    - **alarm = NULL**：death_predation_frac 4/6、p=0.56、CI 跨 0；机制点火（alarm_total>0）但**信息冗余**
      （§8.4 第 1 类）。`feasibility.md §8.6`。
    - **forage = 证否**：forage_pref_std **OFF>ON**（反向 5/6，方差塌缩 32.6%），种群整体倒向草；**根因果层
      仅草层 0.07%、无生态位可专精**。`feasibility.md §9.5`。dip-test 不必做（必要条件已否）。
- PENDING（下个 session 第一件事）: **方向待用户定盘**。三通路都收口、默认关。候选（研究已备、判决已给出下一步）：
  ①救腐食 v2——补 seed 6–17 到 ≥12 配对把 p 推过线（差值与 SD 同量级，§5 功效算术需 ~20+ 种子），或加消费
  流量计（累计 `scavenge_gain`）诊断"主动觅食是否真生效"；②救 forage——先**加厚果层**（动 `fruit` 生态位）+
  交叉 ridge 地形，否则权衡永远单向塌缩；③救 alarm——加「高 alarm cell 猎物密度」读出 + `los_occlusion` 造
  超视距信息。**不要自己挑一个开跑**，都要 6 种子。
- 坑:
  - **worktree agent 无独立 `.venv`**：用主 checkout 的 `.venv/bin/python`、以 worktree 为 cwd 跑（`sys.path` 的
    `.` 指向 worktree 码），全程带 `XLA_PYTHON_CLIENT_PREALLOCATE=false`。
  - **6 并发 sweep ~27min**（36 run×~4.5min/6 并发）——不是卡住。诊断 CPU 回落坑三连：`nvidia-smi` 看 util/NVML、
    `ps` 看 CPU 时间是否暴涨、log 看 steps/s。本轮 GPU util 100%、驱动 580.173 一致，正常。
  - forage 是**唯一付永久 trait_dim 代价却被证否**的改动，选择留档（回退 8→7 会再 churn golden+作废种群）。
