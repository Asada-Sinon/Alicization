# HANDOFF

**这不是文档，是上一个 agent 写给下一个 agent 的信。要短、要具体、只写下次用得上的。**
不写背景（背景在 `CLAUDE.md`），不写论证（论证在 `docs/`）。

**与 `docs/TODO.md` 的分工——两者不重复：**

- `docs/TODO.md` = **任务队列 + 文档地图**。长期、面向「下一步做什么」和「去哪找论证」。
  结论一旦稳定就写进它，它是权威来源。
- `HANDOFF.md` = **会话交接**。短期、面向「上次做到哪、什么半途而废、此刻有什么坑」。
  一条信息一旦稳定到值得进 `docs/TODO.md`，就搬过去，别留在这里。

规矩：

- 新会话结束时加一节，**最新的在最上面**。
- 只保留最近 3 节，更旧的直接删掉（历史在 git 里，不用囤在这）。
- `PENDING` 是下一个 agent 开工的第一件事，必须是可执行的动作，不是「继续优化」。
- 教训写 `MEMORY.md`，不写这里：HANDOFF 会过期，教训不会。

格式：

```markdown
## Session YYYY-MM-DD
- 完成: ...
- PENDING: ...        ← 下次第一件事
- 坑: ...
```

---

## Session 2026-08-11（R19 判 undecidable：设计上的零点自己涨了；用户拍板换隔离机制）

- **R19 判决已出**（`feasibility.md` **§25**，`3ee4942`）：96 run × 100k 步（约 150 代）、
  崩溃 0。主臂 Δ mean gene **+0.4082**（超过 MDE 0.176）看似阳性，
  **但 C 臂（排序轴=每步重抽的随机数，设计上的零点）涨了 +0.1772**，
  几乎正好等于门槛 ⇒ 三个对比全不显著（A−C 的 CI **含 0**、p=0.129；
  A−B p=0.151；B−C p=0.622）。只有 D（关掉同型交配）干净地不动（−0.026）。
  ⚠️ **这不是阴性**：跑前预告的是「二阶效应弱」，实测问题是**读数被机制污染**。

- **污染随时间累积**：三轮跑前探针在 40 代时 C = −0.007，正式实验 150 代后 +0.177。
  秩变换、方差归一只压住了早期表现。
  **待查猜想**（§25.2）：方差归一了但**分布形状**没有——`(1−w)X+wY` 在 w=0 时均匀、
  w=0.5 时三角，排序时分位不对齐。**若成立，任何「按 w 线性混合两条轴」都不可判。**

- **⚠️ PENDING（下次第一件事）**：**等 fable5 复核回来再动代码**。它的两问会改方向：
  ① A−B = +0.1521 的 **CI 不含 0**（[+0.0092, +0.3074]），我因为它低于 MDE 就没采信
  ——**但 MDE 是对「与中性零假设比」算的，而 A−B 是臂间配对比较，噪声结构不同**，
  这个处理可能错；若 A−B 成立，判决要从 undecidable 改成「有净效应 +0.15」。
  ② C 臂涨的原因若可修，就**不该换机制**。

- **用户 2026-08-11 拍板四件**：①换一种隔离机制（配偶从**空间邻域**里选，
  隔离从「排序规则」变成「空间事实」）；②可视化仍不做；③派复核（已派）；
  ④**全卡自主**——自己定规格、先写判据再跑、不逐次确认。

- **另记**：用户明确说过「别问我了，直接按推荐做」（已进 memory）。
  汇报要用平实的话，不要抛四选一，也不要堆一屏数字。

- **坑**:
  - `golden` 的 smoke **必须先预热一步**（已修）：XLA 首次执行会 autotune，
    `check.py` 每次全新进程 ⇒ 测的永远是那个离群值（`fruit_total` 散布 6.88%，band 才 ±2%）。
  - `git push` 会偶发瞬时失败，**重试即通**，不是 SSH alias 的问题。
  - `test_los_occlusion_default_is_truly_off` 跑 10 轮 0 失败，那次偶发极罕见。

---

## Session 2026-08-06（R18-B 判完：漂移在 gen ~400 停了，但残余 +0.0019/段仍测得到）

- **用户一次性拍板四件**：①下一轮 = R18-B 跑到 ~700 代；②全卡 CONC=8；
  ③派 fable5 复核 R18 判决；④可视化先不做。**四件都已完成。**

- **R18-B 判决已出**（`feasibility.md` **§24**，`b2538c1`）：24 run × 800k 步、
  崩溃 0、15h45m、**实测跨代数 669 代 [594, 751]——不是 700**。
  - **H1 判「停了」**：末段增量 +0.00193、95%CI **[+0.00017, +0.00344]** ⊂ ±δ(0.00549)。
    **⚠️ 但 CI 不含 0**（p=0.05225）——残余漂移仍可测到；且该判定的宽严是
    「|效应|<0.00385 就判停」（CI 半宽仅 0.30·δ），**δ 是自估噪声不是独立等价界**。
    ⇒ **「停了」= 「残余 ≤ +0.0034/段」，不是「零」**——残余 +0.00193 是 24 run
    分辨率（CI 半宽 0.00163）的 **1.18 倍、测得到**，只是小于**单 run** 噪声 δ=0.00549。
    ⚠️ **不许写「小于本实验的分辨率」**（与「CI 不含 0」自相矛盾，复核抓到）。
  - **H3b 少数簇也停了，但停在比峰值低 15.5% 处**（0.4778→0.4037）。
  - **H2 几何外推与实测不矛盾**：同口径预测 **0.5959**，落在 CI 内。
    ⚠️ 初版的「高估 0.38%、落在 CI 之外」**已撤回**——拿终点点值比窗口均值，
    **口径错配（本 session 第二次）**。
  - **H3 双簇形成于头 13 代、到 669 代未并合**；gen 116–117 那次晚期失败按 §23.4b
    判为**暂态**（1 个 run、跨 1.1 代、此后 2871 个检查点全部恢复）。
  - **H4 四个 `Ne` 全部拒绝中性**（24/24 vs 零分布最大 9/5/4/2）。

- **⚠️ 两个下轮设计要用上的观察**：
  - **H4 的零分布随时程变宽**（R18 最大 5/5/5/1 → R18-B 最大 9/5/4/2）——
    **跑得越久纯漂变越容易造出双峰，这条判据会自动变松**。
  - **等价性检验的 δ 若取自估噪声，判定会偏松**；下轮若还用它，
    考虑预注册一个**独立的实际等价界**而不是自估噪声。

- **R18 判决被 fable5 复核改掉四处**（`8edbcb4`），**每处都是变弱的方向**：
  ①「谷全程全空」是假的（窗只覆盖 41.7%）；②`retained` 违反自己写的禁令；
  ③§19 撤回**用错口径**（应为帧比例的 **+0.0015、5/12、p=0.850**）；
  ④末段增量**分析单位违规**（协议口径是 9/12、p=0.00684，不是 21/24）。
  另补漏报的**少数簇缩 13.4%**，并把「H2 独立自由度低于分辨率」改成「p 过线但功效不足」。

- **R19 代码已落地 + 三项跑前分析已做完**（`e201dd5`/`73c11fe`/`2bb16d7`）：
  **MDE=0.176（w 0.044，宽松）**；**选择压在分裂完成后掉 26,000 倍**
  （中间带占比 0.7425→0.00003）⇒ **跑 800k 步是浪费，成败在最初约 63 代**；
  **入侵阈值 `w≈0.2`（gene 0.847）**——`w≤0.10` 选择系数≈0 甚至 −0.003，
  但**初始就有 1.7% 的个体在阈值之上**（669 代 10.1%），
  ⇒ 真问题是「那 1.7–10% 能不能点起正反馈」。
  **判决要加读数 `gene>0.847` 的占比**（`mean gene` 会把正反馈点火平均掉）。
  ⚠️ 实现偏离记在 §22.3b（「双亲均值」循环依赖、不可实现）。

- **PENDING（下次第一件事，要用户拍板）**：**R19 的两个设计决定**——
  ①步数（800k 是浪费，该覆盖分裂形成期）；②要不要增设「初始 gene 在阈值之上」一臂。
  原 PENDING 见下（让
  `mate_forage_weight` **本身可遗传**，回答「隔离能不能自发演化」。
  **已预注册**（`program.md` §22），§22.2 自己写着「这很可能阴性」。
  ⚠️ **会动 `trait_dim`、作废所有已演化种群、golden 必须重 bless**——
  三条都要用户点头，**不要自己开跑**。

- **坑**:
  - **`git push` 会偶发瞬时失败**（GitHub 端关闭连接，报「请确认访问权限」），
    **重试即通**——不是 SSH alias 的问题，别去「修」remote。
  - harness 的后台任务有时限，>30 分钟的计算用 `nohup bash -c '...' &` 脱离。
  - **Monitor 的结束标记要匹配「最后一行」**：我写 `grep -q '分辨率下限'` 等 4 个 `Ne`，
    而每个 `Ne` 的行都含这个词 ⇒ 第一个一打印就误报完成。改用进程退出。

---

## Session 2026-08-06 凌晨（R18-B 已启动；R18 判决补了漏报的比值；fable5 复核在跑）

- **用户一次性拍板四件**：①下一轮 = **R18-B 跑到 ~700 代**；②**全卡 CONC=8**；
  ③**派 fable5 复核 R18 判决**；④**可视化先不做**。

- ⚠️⚠️ **R18-B 正在跑**（`outputs/20260806-gen700/`，**00:32:58 启动**、CONC=8、
  24 run × **800k 步**、预计 **16 小时**，约 16:30 完成）。判据
  `multispecies_program.md` **§23**（`c80c087` 先写后跑）。
  **冻结名单**：`underworld/**`、`trajectory.py`、`split_score.py`、`neutral_null.py`、
  **以及正在执行的 `sweep_r18b.sh` 本身**。
  `analyze_r18.py` 与 `20260805-r18-verdict/*.py` **不在名单里**（没有进程 import 它们）。
  断点续跑已内建；进度 `grep -l '^JSON ' outputs/20260806-gen700/*.log | wc -l`。

- **⚠️ 补了 R18 判决的一个漏报**（`c80c087`）：§22.6 的「末段增量仍显著为正」
  初版只报 21/24 与 p=0.00065，**漏了比值**——补算 σ̂_W=0.00953、配对差噪声 0.00953、
  效应 +0.00999 ⇒ **比值 +1.05，刚过闸**。方向一致性强，但**效应只有噪声量级**。
  ⚠️ **随后复核又抓到那个 21/24 的分析单位也是错的**（逐 run 而非格均值，违反
  `CLAUDE.md` 的协议）——协议口径是 **9/12、p=0.00684**，脚本已修。
  「没停」因此被削弱成「方向上还在涨，涨幅已接近分辨率下限」。§22.7 措辞同步改。
  **这恰恰定义了 R18-B 的价值**：把漂移推到分辨率之下，再用**等价性检验**确认
  它真停了，而不是「没检出」。

- **R18-B 的判据要点**（§23.4，判决时照做）：
  - **H1 是等价性检验**（本项目第一次用）：末段增量的 95%CI ⊂ `[−δ, +δ]` ⇒ **停了**；
    ⊂ `[+δ, ∞)` ⇒ **没停且推翻几何衰减**；否则 undecidable。**δ 用本轮自估**
    （跑前预估 0.0095）。
  - **关键口径：按固定段长（~63 代，与 R18 一致）切，不按固定段数**——
    若同样切 6 段，段长变 ~117 代，噪声与效应都不可比。
  - H2 **已预先声明功效不足**（比值约 1.1），落在区间内只能说「不矛盾」。
  - H3 若**脱离饱和**是本轮最重要的发现，要单独成节 + 报第一次脱离的世代。
  - **不许写成「700 代」**，按实测跨代数写（§21.2）。

- **R18-B 已跑完**：24/24、崩溃 0、00:32:58→16:17:27（**15h45m**）。
  provenance 硬验证过：内核代码在这 15 小时里**一个字节没变**（期间 5 次提交
  全落在 `docs/` 与不被 import 的分析脚本）。
  **实测跨代数 669 [594, 751]——不是 700**（世代钟比 §23.2 的外推还慢）。

- **判决主体已写成草稿**：`scratchpad/r18b_draft.md`（§24，含 §24.0–§24.9），
  **只差 H4 的中性对照**（`nohup` pid 1742703 在跑，约 40 分钟，
  输出 `scratchpad/r18b_verdict.txt`）。H4 一完成就把 §24.7 填上、
  整节写进 `feasibility.md` 并提交。

- **R18-B 的读数（24/24）**：
  - **H1 判「停了」**：末段增量 +0.00193、9/12、p=0.05225、
    95%CI **[+0.00017, +0.00344]** ⊂ ±δ(0.00549)。
    ⚠️ **但 CI 不含 0**——残余漂移仍可测到，只是小于分辨率。
    8/24 与 16/24 时 CI 还含 0，是样本量涨到 12 格才分辨出来的。
    **「停了」≠「测到零」**，且该判定的宽严是「|效应|<0.00385 就判停」（CI 半宽=0.30·δ）。
  - **H3b 少数簇也停了，但已缩 15.5%**（峰值 0.4778 → 末段 0.4037）。
  - **H2 几何外推高估 0.38%**：R18 预测 669 代时 0.5969，实测 0.5947 [0.5927, 0.5967]
    ——**预测落在 CI 之外**，模型方向对量级对但系统性偏高。
  - **H3 全检查点**：266 无双峰（最晚 gen 13）、1207 谷未见底（最晚 gen 117）、
    gap 最小 0.000332。晚期失败按 §23.4b 判为**暂态**
    （1 个 run、4 次、跨 1.1 代、此后 2871 个检查点全部恢复）。
  - **逐段增量从第 7 段（gen ~400）起全部 ≤0.0023** ⇒ 漂移是在 gen 400 前后进噪声的。

- **PENDING（下次第一件事）**：H4 一跑完就合并草稿写进 `feasibility.md` §24 并提交，
  然后更新 `docs/TODO.md` 的队列与文档地图。**之后只剩 R19，要用户拍板**
  （动 `trait_dim`、作废种群、golden 重 bless）。

---
