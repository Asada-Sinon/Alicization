# HANDOFF

**这不是文档，是上一个 agent 写给下一个 agent 的信。要短、要具体、只写下次用得上的。**
不写背景（背景在 `CLAUDE.md`），不写论证（论证在 `docs/`）。

**与 `docs/TODO.md` 的分工——两者不重复：**

- `docs/TODO.md` = **任务队列 + 文档地图**。长期、面向「下一步做什么」和「去哪找论证」。
  结论一旦稳定就写进它，它是权威来源。
- `HANDOFF.md` = **会话交接**。短期、面向「上次做到哪、什么半途而废、此刻有什么坑」。
  一条信息一旦稳定到值得进 `docs/TODO.md`，就搬过去，别留在这里。

规矩：

- 新会话结束时加一节，**最新的在最上面**。
- 只保留最近 3 节，更旧的直接删掉（历史在 git 里，不用囤在这）。
- `PENDING` 是下一个 agent 开工的第一件事，必须是可执行的动作，不是「继续优化」。
- 教训写 `MEMORY.md`，不写这里：HANDOFF 会过期，教训不会。

格式：

```markdown
## Session YYYY-MM-DD
- 完成: ...
- PENDING: ...        ← 下次第一件事
- 坑: ...
```

---

## Session 2026-08-05 凌晨（把分裂画出来，wire v10 + 着色器）

- 授权: `/loop` 自主。opus5 执行、**fable5 审查两次**（动手前审计划、实现后复核）。
  **动手前那次拦下两个 BLOCKER，两个都对。**

- **完成（`docs/forage_visualization.md`，已推送）**：
  - wire **v10**：per-agent record 追加第 10 个 f32 = `forage_pref`。
    **header 不动、任何现有 per-agent 偏移不动**，客户端只改一个 `STRIDE`。
  - 点着色器：食草者颜色沿觅食轴调制，**中性个体保持原色**
    （所以 `check_species_colours` 钉的字面量仍是「没有专精的食草者」的颜色）。
  - `check.py` 新增 **`check_agent_stride`**——per-agent 宽度此前**完全没人管**，
    坏掉的表现是「每个 agent 从第二个字段起全部错位、画面照样出」。做过反向注入验证。
  - `run_live.py --speed`：headless 验证没有手去拖 FLA 滑块。

- **两条被审查拦下的（都对）**：
  - 我原本的截图验证**什么都证不了**：种群起手全在 `pref≈0.5`，默认世界与 R38n 世界
    在 frame 0 长得一样；而服务器 4 步/帧要 6 分钟才演化出东西。
    改成两闸：frame-0 可证伪正确性 + 拉满速度后的现象，判据全写成可复算的数字。
  - **不许在 JS 里重写 `split_score`**（它有「六个臂全读 0.19、毫无分辨力」的前科），
    连带整个直方图面板都推迟——最小充分集是 wire + 着色器。

- **我自己犯并修的一个**：着色斜坡**按值域中点定标**（`|f|/0.5`），结果果专精染上琥珀
  而草专精是一片灰白、「绿」恒读 0.000。真实簇位是 **0.151 / 0.720**，
  上簇离中性只有 **0.22**，在 0.5 的斜坡上只拿到 0.35 权重、压不过紫底的 0.92 蓝。
  改成 `min(|f|/0.22, 1)` 后草侧才出现。**读数要对着被测对象的真实分布定标，
  不能对着值域中点定标**——与 `MEMORY.md` 那三条同型。

- **验收数字**：闸一 变化像素 10320（0.645%）、**UI 面板内变化 0**；
  闸二 默认世界 琥珀0.029/绿0.000/紫0.971 vs R38n 定标后 **0.257/0.060/0.684**。
  另量到**果专精比草专精成团 9.97 倍**（离散指数 88.43 vs 8.87，零分布均 ~1.0）。
  `check.py --full` **47 项全过**，golden 未动。

- **实现后复核（fable5）：无 BLOCKER，三条 SHOULD-FIX 全是真的，都已修并推送**（`fba985b`）：
  ①**验收图是定标前拍的** ⇒ 出厂着色器一度没有闸一证据、闸二在比两个不同着色器。
  全部重拍，数字全变（绿 0.000→0.191，对照组 0.029→0.056），结论方向不变。
  并更正初版「权重上限 0.24」——那是用**被废弃的公式**算的，出厂公式给 0.58。
  ②**`check_agent_stride` 只查首字节，而我把两个标量并成 `vec2` 这一步
  第一次让这个洞可触发**（vec2 从 36 读到 40，`36<40` 静默通过）。改查末字节 + 条数交叉核对，
  两条新断言都做过反向注入验证。
  ③**`--speed` 不同步到 UI**：面板显示「4 步/帧」、「窗口长度」少算 500 倍，
  滑块上限只有 256、碰一下就永久掉下去。**`cc3045a` 提交信息里那句
  「与滑块能发的范围一致」是错的**，更正记在 §4。

- **直方图面板已做完并推送**（`4885a98`，§8）。**验收不是「看起来像双峰」**，
  是「客户端现算的数是否复现已知值」：默认世界读 **6%** vs 理论创始者值 **6.1%**；
  `R38n` 读 **46%** vs §18.4 由完全独立的分析管线量出的 **0.4501**。
  两边实现不共享一行代码。分箱由新增的 `check_ecotype_bins` 钉住（做过反向注入验证）。

- **PENDING（下次第一件事，零 GPU、不需要拍板）**:
  ~~纲领的前提要改写~~ —— **这条已作废，因为它建立在我自己的一个错上**：
  我把 `R38p` 叫成「默认世界」，而它与 `Config()` **差 9 个参数**，
  其中 **`forage_tradeoff` 默认是 0.0**（整个权衡的主开关默认关着，
  关着时 `forage_pref` 完全没有表型效应）、`fruit_regrow_baseline` 是默认的 **250 倍**。
  **纲领的前提没有被证伪**——那九个旋钮正是因为默认世界只有一个饭碗才装的。
  更正已写进 `feasibility.md` §18.2/§18.5 与 `docs/TODO.md`。
  次选（也零成本）：给 wire header append 一个 speed 字段，把 `--speed` 同步到 UI，
  修掉 §4 记的那个 footgun。

- **坑**:
  - **端口用 8137 不用 8000**——用户的 `serve_policy.py` 占着 8000。
  - `--virtual-time-budget` **只加速浏览器不加速服务器**。
  - 属性槽卡在 **8**（WebGL 1.0 下限），再加属性必须先合并。

---

## Session 2026-08-04 深夜（R15-A，**零 GPU**，分裂结构与中性分开）

- 授权: 用户「方案B」那一轮的延续，`/loop` 自主。opus5 执行、**fable5 审查两次，
  第一次拦下一个会让我发假阳率 100% 结论的判据，第二次拦下我给的替代零假设本身**。

- **一件必须先知道的事实**：R13 版 `trajectory.py` 的**轨迹**路径用 `jnp.clip` 分箱（79 行），
  **检查点**路径才用 `np.digitize`（147 行）⇒ **截断 bug 只污染了检查点**。
  逐帧核过 24×373 帧 `sum(hist)==n_herb` 全等。**所以 R13 的轨迹是干净的全员普查。**

- **判决（`feasibility.md` §17/§18，全部已推送）**:
  - **§17.3 最硬的一条**：3 个 run 的检查点表写「簇在 t=62000 消失且再没回来」，
    **同一批个体的全员普查是 0.405 → 0.517、单调在长、`retained` 全程为真**，
    `in_window` 同期掉到 0.487。机制：**簇成功的方式就是往窗外走，所以越成功越看不见。**
  - **§17.9**：「中间被掏空的双峰」在中性漂变+复发突变下**造不出来**——
    主判据 7/24、种子级 3/12，四个 `Ne` 全部拒绝。
    **但敏感性（去掉 3 个丢失捕食者的 run）在最宽松 `N` 上 p=0.060 不过线** ⇒ 当时判 undecidable。
  - **§18 解决了它**：同一判据套到 Stage 2 的 96 run（构造关捕食、修好的 20 箱读数），
    **四个臂全部 p=0.0000，包括有捕食者的基线 `R38p`（10/24）**。
    ⇒ **少数簇不是捕食者缺失的产物**（⚠️ **`R38p` 不是默认世界**，见下条更正）。
  - **§18.3 饱和**：关捕食者 +0.6140、翻倍果供给 +0.6261（都 12/12、p=0.00049），
    但两个一起做 **0/12、−0.05** ⇒ 各自足够、互相饱和。
  - **§18.4 它们做的不是同一件事**：关捕食者 ⇒ **平衡两生态型**（`split_score` **0.4423**）；
    翻倍果供给 ⇒ 整个分布搬到果实侧（`low_mass` 0.61–0.75、`split_score` 只有 0.25–0.30）。
    **只看占空比会把四个臂看成一样的。**

- **判据已收编成常规设施**（`neutral_null.py` 是零假设的唯一实现，
  `trajectory.py` 顶层出 `retained_occ` / `retained_occ_first` / `low_mass_second` /
  `split_score_second`，`tests/test_neutral_null.py` 8 项）。**审查在这一步抓出一个真 BLOCKER**：
  - **`p = (cnt>=T).mean()` 能取到 0，不是有效的蒙特卡洛 p 值。** reps=200 的地板是 0.0050
    ——而 §17.9 印出来的正是 0.0050，**一直贴着地板**。改成 `(1+k)/(reps+1)` 后是 **0.0100**。
    **本项目第二次被 p 地板咬**（第一次是 n=6 符号秩的 0.031）。已加报零分布最大值。
  - 加权口径从 `clip` 换成**保序回归**：理由不是「丢帧」（保序化在 R13 上零权重帧反而升高），
    是 **`clip` 的权重总和是真实跨代数的 2.24 倍**（Σw=1096 vs 跨代 455），
    每次「新生儿潮拉低 + 回升」都被数了两遍。保序口径的比是 1.02。
  - 重跑后**结论全部不变且更强**：R13 三种口径 7/8/9 全部拒绝（零分布最大值都是 7）；
    Stage 2 四臂 × 四个 `Ne` = **16 格全部 p<0.005**，`Ne=2000` 上零分布一次都没到过 1。

- **PENDING（下次第一件事，零 GPU、不需要拍板）**:
  §18.4c 记了一条**没收口的**：「单调化」的实现选择还能再动一个 run
  （PAVA 给 8，`cummax` 与 sklearn 的 `IsotonicRegression` 给 9）。
  R13 的正确说法是「**7–9 / 24，随口径而动，但一律拒绝**」。若要收口就把三种单调化
  都实现并入 `gen_weights`，或者干脆声明「R13 的 `generation` 不是世代钟」而只报区间。

- **要烧卡的那件（要拍板）**：`R38n` 长时程复核——**默认果供给 + 无捕食者跑满 100k 步**，
  12 种子 × r=2，问「50/50 双簇在 200+ 代上稳不稳」。约 48 run。
  **卡上有用户自己的 `serve_policy.py` 占 12.3 GiB，只剩 11.4 GiB，要压并发。**

- **坑**:
  - **过程层面的零假设 ≠ 估计量的零假设**（`MEMORY.md` 新增）。
  - **不许单看占空比**，必须与 `split_score` + `low_mass` 一起读。
  - **§18 不是预注册的**（判据是既有的，应用不是）——它是确认性证据的候选，不是判决。
  - `n` 臂与 `p` 臂**世代钟差 2–3 倍**，任何判据写成「每代」不是「每步」。

---

## Session 2026-08-04 白天→晚（R14 两阶段 + **撤回 R13 判决**）

- 授权: 用户「方案 B」拍板了 Stage 2；opus5 执行、**fable5 审查**（`plan-critic` 定设计、
  `result-analyst` 出判决）。**`result-analyst` 这次推翻了主控刚写下的一句话。**

- **最重要的一件事：R13 的「按现规格关闭」判决已撤回**（`feasibility.md` §16.4）。
  **理由不是结论错了，是证据作废。** 两条支柱各踩一个后来才修好的 bug：
  ①直方图 range 截断（`b06319e`）；②`split_score` 边界补零（`803dc60`，
  **在判决落档 `17ff01b` 之后**才修）。修好后：
  衰减 p 0.0036→**0.063**、「快于中性」p 0.0027→**0.178**、
  「0/15 连续保住」→**4/12**、`Ne·s` 1.22→**0.40（有效中性区）**。
  **撤回后是 undecidable，不是判正。整条路线回到未回答。**
  §14.6 的论证还是**反的**：它点名「丢得最快」的 3 个 run，不截断口径下是
  0.462/0.517/0.510，**全在长**；`wn2_s7_r1` 的 cp4 是 `[51.0, 0×10, 13.5, 35.5]`
  ——**中间 10 个箱一个人都没有的干净双峰**，被旧读数记成 0.0000。
  **与预期相反的收窄也写了**：去掉那 3 个 run 后衰减反而显著（p=0.0192）。

- **R14 判完**：Stage 1 作废（主判据输入被截断）；Stage 2（96 run）
  **H0 成立**（果供给翻倍 +0.3006、12/12）、**H1/H2 undecidable**，
  §17.5 的闸不触发 ⇒ **不能据此关线**。H1 的正面信息：**排除了 >±0.0138 的资源比效应**。

- **本轮撞出的新变量（预注册之外，纲领从没扫过）**：**捕食通道压制果专精簇**。
  `R38n−R38p` = **+0.3519、12/12、p=0.00049、比值 +3.21**，与果供给翻倍同量级甚至更大；
  R13 那 3 个**自发**丢失捕食者的 run 正好是果专精簇长到 0.5 的那 3 个。
  ⚠️ **多变量**（通量/种群/世代钟同时变），**是线索不是结论**。

- **PENDING（下次第一件事）= R15-A，零 GPU 成本，不需要拍板**:
  **已核实的关键事实**：R13 版 `trajectory.py` 的**轨迹**路径用 `jnp.clip` 分箱（79 行），
  **检查点**路径才用 `np.digitize`（147 行）⇒ **截断 bug 没有污染轨迹**。
  逐帧核过 24×373 帧 `sum(hist)==n_herb` **全等**。
  ⇒ 手上有 **373 点、跨 118–613 代的全员普查轨迹**，而原判据只用了 5 个检查点（9.7 代窗）。
  设计（漂移函数 `E[Δx|x]`、留存占空比、捕食者分层）已派 `plan-critic` 审。

- **坑**:
  - **GPU 上有用户自己的 `serve_policy.py`（12.3 GiB）**——烧卡之前先 `nvidia-smi`。
  - **`σ̂_W` 不许跨异质臂池化**（Stage 2 把 t=+10 压成「比值 0.49」）。
  - **不许再扫资源比**；要烧卡就扫捕食通道，且判据必须写成「每代」不是「每步」
    （`n` 臂 28–40 代 vs `p` 臂 91–102 代）。

