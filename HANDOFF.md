# HANDOFF

**这不是文档，是上一个 agent 写给下一个 agent 的信。要短、要具体、只写下次用得上的。**
不写背景（背景在 `CLAUDE.md`），不写论证（论证在 `docs/`）。

**与 `docs/TODO.md` 的分工——两者不重复：**

- `docs/TODO.md` = **任务队列 + 文档地图**。长期、面向「下一步做什么」和「去哪找论证」。
  结论一旦稳定就写进它，它是权威来源。
- `HANDOFF.md` = **会话交接**。短期、面向「上次做到哪、什么半途而废、此刻有什么坑」。
  一条信息一旦稳定到值得进 `docs/TODO.md`，就搬过去，别留在这里。

规矩：

- 新会话结束时加一节，**最新的在最上面**。
- 只保留最近 3 节，更旧的直接删掉（历史在 git 里，不用囤在这）。
- `PENDING` 是下一个 agent 开工的第一件事，必须是可执行的动作，不是「继续优化」。
- 教训写 `MEMORY.md`，不写这里：HANDOFF 会过期，教训不会。

格式：

```markdown
## Session YYYY-MM-DD
- 完成: ...
- PENDING: ...        ← 下次第一件事
- 坑: ...
```

---

## Session 2026-08-17（R21 判完；两个能看的页面都做出来了）

- **R21 完整判决**（`feasibility.md` **§30**）：九档跑完（补上 0.70/0.80/0.90）。
  字面上判据判「有阈值」（跃升在 0.70→0.80、`low_mass` +0.4519、比值 28.81），
  **但 H3 把它的含义整个改掉了**——跃升那一档正是捕食者开始大批灭绝的一档
  （0.70 档失效 1/24，**0.80 档 15/24**，0.90 档 24/24）。
  **决定性的一组数**：只看捕食者幸存的 run，`carn_frac` 在 0.15/0.45/0.70 三档是
  0.1576/0.1539/0.1603——**门槛抬近五倍，捕食者密度纹丝不动**。
  ⇒ **`diet_delta` 是存活闸门不是压力旋钮**，「临界压力在哪」这个世界答不了。
  按存亡拆分合并：捕食者仍在 0.0623 (n=140) vs 已灭绝 0.5087 (n=51)，
  后者精确落在无捕食对照 0.5105 上。**⚠️ 已在 §30.5 写明这是处理后变量条件化
  （collider 风险），只说明共位不证因果。**

- **第二个页面做出来了**：`explorations/20260816-replay/map.html`（4.1 MB 自包含）。
  个体位置画在地形上，两臂并排、共用一根滑杆。**关键读数：偏果的 77–85%
  站在长果子的格上，偏草的只有 3–5%**——第一次把「两个生态型在空间上分开」
  变成可核对的数而不是「你看图」。

- **PENDING（下次第一件事）**：**等 claim-verifier 复核 §30 回来**。
  它在查七件事，最关键的是 §30.4 那张「幸存 run 的 `carn_frac`」表——
  「纹丝不动」这个说法是否真被数据支持（组间差 vs 组内散布），
  那是整节判决的支点，若它站不住，§30.0 的一句话就要改。

- **坑**:
  - **提交消息里的 `%` 会被 `printf` 静默截掉**（连 `Co-Authored-By` 一起丢），
    而提交仍然成功。含 `%` 就走 `git commit -F` 临时文件。已记进 `MEMORY.md`。
  - **量化 ≠ 省体积**：`record_map.py` 把坐标压成 uint16 后又 `.tolist()`
    写成十进制文本，每个体 40 字节而非 6。省体积的是编码（base64），不是量化。
  - **加可视化时口径要跟判决对齐**：地图页初版把食肉者也算进了直方图分母，
    而判决的口径是 `herb = alive & (diet < 0.35)`。**新读数一律回去核对
    `trajectory.py` 用的是什么掩码。**

---

## Session 2026-08-13～16（R20/R21 判完，第一个能看的东西做出来了）

- **R20 判决**（`feasibility.md` **§27**）：144 run，两个硬结论——
  ①**换地图生态型照样在**；②**加捕食者它就被压掉**（果侧簇 ~0.48 → 0.02–0.15，
  三张图 12/12 同向，且在**相同世代数与相同步数**两种对齐下都成立）。
  ⚠️ **复核撤回了 ①的推广**：`base35` 不是第二张新河系，它是 `wn1` 的**环面刚性平移**
  （高程相关 0.9999），**真正的新河流几何只有一张（`wn2`）**
  ⇒ 「仅此一张河系」是**放宽**不是解除。另改五处：主判据的 p 被我低估 30–130 倍、
  漏报 5/72 处理失效、`carn_frac` 是全程帧均（末段 0.096–0.138，是默认强度不是弱捕食）、
  「双峰消失」应为「果侧簇被压掉、两簇未合并（谷深 0.82–1.0）」。

- **R21 判决（部分）**（`feasibility.md` **§29**）：`diet_delta` 0.15/0.25/0.35/0.45/0.60
  五档的 `low_mass` 全在 **0.068–0.100** ⇒ **不是剂量效应，像开关**。
  ⚠️ **但「临界点在哪」答不了，是我的设计缺陷**：五档全落在「捕食仍充分发生」的区间，
  真正的过渡带一个点没采到。脚本按判据算出「有阈值、比值 37」——**那是采样空洞造的假象**。
  根因：把有效范围估成 [0.15,0.6]，而实测 **0.60 档 `carn_frac` 仍有 0.167**。

- **⚠️ 正在跑：R21b**（`outputs/20260816-r21b`）`diet_delta` 0.70/0.80/0.90 × 24 run。
  初步：**0.70 档 `carn_frac` 仍 0.159**，果侧比 0.60 更低 ⇒ 转折多半在 0.80–0.90。
  约凌晨 0:30 跑完。**跑完出剂量曲线的完整判决**（判据 §28.3，
  但 H1 的「相邻跃升」只在档距可比的区间内算——这是 R21 的教训）。

- **第一个能看的东西已经做出来了**：`explorations/20260816-replay/replay.html`
  ——双击即开、数据内嵌、零 GPU。两个世界并排拖进度条：
  左（无捕食）第 717 代果侧 **61.2%**、两簇分开中间空白；右（有捕食）**0.0%**。
  已按 §10 做截图验收。**下一步**：`record_map.py` 已写好并验过字段，
  等卡空录**带个体位置**的数据，把地图加进回放。

- **PENDING（下次第一件事）**：等 72/72 → 出 R21 完整判决 → 派复核 →
  卡空后录地图数据 → 把地图加进 `replay.html`。

- **坑**:
  - **`--set` 一个派生属性会报错**（`ridge_base_y` 是 `ridge_base_frac × world_size`）
    ——好事，不是静默忽略。
  - **zsh 不做词分割**，`$VAR` 传成含空格的整参数；`eval` 的间接数组引用也不可靠。
    **已栽三次**，写验证脚本时直接把参数写全，别绕。
  - **说了要跑就在同一轮启动**——曾经说完下一步却留到下一轮，实验白停了一天。

---

## Session 2026-08-11（R19 判 undecidable：设计上的零点自己涨了；用户拍板换隔离机制）

- **R19 判决已出**（`feasibility.md` **§25**，`3ee4942`）：96 run × 100k 步（约 150 代）、
  崩溃 0。主臂 Δ mean gene **+0.4082**（超过 MDE 0.176）看似阳性，
  **但 C 臂（排序轴=每步重抽的随机数，设计上的零点）涨了 +0.1772**，
  几乎正好等于门槛 ⇒ 三个对比全不显著（A−C 的 CI **含 0**、p=0.129；
  A−B p=0.151；B−C p=0.622）。只有 D（关掉同型交配）干净地不动（−0.026）。
  ⚠️ **这不是阴性**：跑前预告的是「二阶效应弱」，实测问题是**读数被机制污染**。

- **污染随时间累积**：三轮跑前探针在 40 代时 C = −0.007，正式实验 150 代后 +0.177。
  秩变换、方差归一只压住了早期表现。
  **待查猜想**（§25.2）：方差归一了但**分布形状**没有——`(1−w)X+wY` 在 w=0 时均匀、
  w=0.5 时三角，排序时分位不对齐。**若成立，任何「按 w 线性混合两条轴」都不可判。**

- **⚠️ PENDING（下次第一件事）**：**等 fable5 复核回来再动代码**。它的两问会改方向：
  ① A−B = +0.1521 的 **CI 不含 0**（[+0.0092, +0.3074]），我因为它低于 MDE 就没采信
  ——**但 MDE 是对「与中性零假设比」算的，而 A−B 是臂间配对比较，噪声结构不同**，
  这个处理可能错；若 A−B 成立，判决要从 undecidable 改成「有净效应 +0.15」。
  ② C 臂涨的原因若可修，就**不该换机制**。

- **用户 2026-08-11 拍板四件**：①换一种隔离机制（配偶从**空间邻域**里选，
  隔离从「排序规则」变成「空间事实」）；②可视化仍不做；③派复核（已派）；
  ④**全卡自主**——自己定规格、先写判据再跑、不逐次确认。

- **另记**：用户明确说过「别问我了，直接按推荐做」（已进 memory）。
  汇报要用平实的话，不要抛四选一，也不要堆一屏数字。

- **坑**:
  - `golden` 的 smoke **必须先预热一步**（已修）：XLA 首次执行会 autotune，
    `check.py` 每次全新进程 ⇒ 测的永远是那个离群值（`fruit_total` 散布 6.88%，band 才 ±2%）。
  - `git push` 会偶发瞬时失败，**重试即通**，不是 SSH alias 的问题。
  - `test_los_occlusion_default_is_truly_off` 跑 10 轮 0 失败，那次偶发极罕见。
