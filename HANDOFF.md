# HANDOFF

**这不是文档，是上一个 agent 写给下一个 agent 的信。要短、要具体、只写下次用得上的。**
不写背景（背景在 `CLAUDE.md`），不写论证（论证在 `docs/`）。

**与 `docs/TODO.md` 的分工——两者不重复：**

- `docs/TODO.md` = **任务队列 + 文档地图**。长期、面向「下一步做什么」和「去哪找论证」。
  结论一旦稳定就写进它，它是权威来源。
- `HANDOFF.md` = **会话交接**。短期、面向「上次做到哪、什么半途而废、此刻有什么坑」。
  一条信息一旦稳定到值得进 `docs/TODO.md`，就搬过去，别留在这里。

规矩：

- 新会话结束时加一节，**最新的在最上面**。
- 只保留最近 3 节，更旧的直接删掉（历史在 git 里，不用囤在这）。
- `PENDING` 是下一个 agent 开工的第一件事，必须是可执行的动作，不是「继续优化」。
- 教训写 `MEMORY.md`，不写这里：HANDOFF 会过期，教训不会。

格式：

```markdown
## Session YYYY-MM-DD
- 完成: ...
- PENDING: ...        ← 下次第一件事
- 坑: ...
```

---

## Session 2026-08-05 下午/晚（R18 判完：双簇撑住 375 代但不再是 50/50；连带撤回一条已发表数字）

- 授权: `/loop` 自主，全程 opus5 单人执行（本 session 未派 subagent）。

- **R18 判决已出**（`feasibility.md` **§22**，`c1ce8f9`）：24 run × 350k 步、崩溃 0、
  **实测跨代数 375 代 [345, 431]——不是 450**。
  - **双簇撑住了**：两峰在每一个检查点都找得到、谷全程全空、`retained` 24/24，
    H3 四个 `Ne` 全部 p<0.005 拒绝中性。**R16 标题里那个「200+ 代」本轮答了。**
  - **但不再是 50/50**：`low_mass` 0.5070→**0.5820**（Δ+0.0750、12/12、
    p 触地板 0.00049、比值 **+6.63**）。**§21.3 预测的衰减符号被推翻**
    （预测降到 0.339，实测升到 0.582）。
  - **漂移在减速但没停**（非预注册）：增量 +0.1712→+0.0457→+0.0302→+0.0178→**+0.0100**，
    末段是首段的 1/17，但仍 **21/24、p=0.00065** 显著为正。
  - **三条禁令**（§21.4b 写死于只看到 16/24 时）：不许只写「撑住了」、
    不许写「在衰减」、不许写「已达新平衡」。

- **⚠️ 连带撤回了一条已发表数字**（`6625a08`）：`feasibility.md` §19 的
  `split_score` Q4−Q3 从 **+0.0691（比值+3.73）** 重算为
  **+0.0053、9/12、p=0.064（不显著）**。那张表**自己就矛盾**——用它自己的
  Q3 `low_mass`=0.4873 代入恒等式该得 0.4873，而它报 0.4135。
  根因是时间顺序：平台修复 `88b8f44`（09:50）比 R16 判决 `fe54d49`（04:50）**晚 5 小时**。
  **§19 定性结论不变、主论据反而更强**（`low_mass` 重算 +0.0280、12/12、p 触地板）。

- **本轮量出来的恒等式（会反复用到）**：谷见底的干净双峰上
  **`split_score ≡ min(low_mass, 1 − low_mass)`**（32784 个检查点 94.2% 精确）。
  **`min` 在 0.5 有尖点、导数变号** ⇒ 单调上升的 `low_mass` 让 `split_score`
  先升后降，造出「一升一降」的假矛盾。**这两个读数并排时不是两条证据。**
  这个尖点**早在 8 月 4 日 §17 的表里就印出来了**（五格精确吻合），只是没人指出。

- **PENDING（下次第一件事，要用户拍板）**：只剩 **R19**——让
  `mate_forage_weight` **本身可遗传**，回答「隔离能不能自发演化」。
  **已预注册**（`program.md` §22，`15cefd5`），且 §22.2 自己把「这很可能阴性」
  写在最前面。⚠️ **会动 `trait_dim`、作废所有已演化种群、golden 必须重 bless**——
  这三条都要用户点头，**不要自己开跑**。

- **坑**:
  - **harness 的后台任务有时限**：`analyze_r18.py` 的 H3（24 run × 200 reps × 4 个 `Ne`，
    约 40 分钟）被 kill 了两次。**用 `nohup bash -c '...' &` 脱离 harness** 才跑完。
  - **zsh 不做 word splitting**：`for f in $FILES` 拿到的是整个字符串，
    要用数组 `FILES=(...)` + `"${FILES[@]}"`。
  - 判决脚本 `analyze_r18.py` 里的百分比**已改成当场算**，不要再写死常数。

---

## Session 2026-08-05 下午（R18 在跑，判决脚本先修了一个只在崩溃 run 上触发的崩）

- 授权: `/loop` 自主。**本节写的时候 R18 正在跑**，源码树按下面那条规矩冻结。

- ⚠️⚠️ **R18 正在跑**（`outputs/20260805-gen450/`，12:07 启动、CONC=8、24 run ×
  350k 步）。**不许动**：`underworld/**`、`explorations/20260804-readouts/trajectory.py`
  及它 import 的 `split_score.py`、以及**正在执行的 `sweep_r18.sh` 本身**。
  分析脚本 `analyze_r18.py` **不在冻结名单里**——没有任何运行中的进程 import 它。
  断点续跑已内建（`grep -q '^JSON '` 跳过已完成的），中断了直接重跑同一条命令。
  进度看 `grep -l '^JSON ' outputs/20260805-gen450/*.log | wc -l`（目标 24）。
  实测约 **2h20m/批 × 3 批**，即 12:07 起算约 **19:10 全部完成**。

- **⚠️ 本节最重要的一条：撤回了一条已发表结论**（`6625a08`）。
  `feasibility.md` §19 的 R38n 四分位表里，`split_score` 的 Q3 是**平台失效造的假值**，
  `Q4−Q3` 从 **+0.0691（比值+3.73）** 重算为 **+0.0053、9/12、p=0.064（不显著）**。
  **那张表自己就是矛盾的**：用它自己的 Q3 `low_mass`=0.4873 代入恒等式
  `split_score ≡ min(lm, 1−lm)`，该得 0.4873，而它报 0.4135。
  根因是时间顺序——平台修复 `88b8f44`（09:50）比 R16 判决 `fe54d49`（04:50）**晚 5 小时**，
  R17 那次只更正了 Stage 2 的一个值。**§19 的定性结论不变、主论据反而更强**
  （`low_mass` 不经过 `split_score`，重算 +0.0280、12/12、p 触地板）。
  连带补掉三处漏改的 `0.4423`（`6811db2`）——`program.md` 那处写着
  「`split_score` 0.4423、`low_mass` 0.4501」，**同口径下这两个数必须相等**。

- **`split_score` 的恒等式（本轮的核心发现，`f390945`+`542a8d4`+`633f40a`）**：
  谷见底的干净双峰上 **`split_score ≡ min(low_mass, 1 − low_mass)`**，
  16/24 复核实测精确成立 **94.6%**（21856 个检查点）。
  **`min` 在 `low_mass = 0.5` 有尖点、导数在那里变号** ⇒ `split_score` 是
  `low_mass` 的**非单调**函数，一个单调上升的 `low_mass` 会让它先升后降，
  呈现为「一升一降」的假矛盾。这个尖点**早在 8 月 4 日 §17 的表里就印出来了**
  （五格精确吻合，最后一格 `x` 穿 0.5 时 `split_score` 折返到 `1−x`），只是没人指出。
  ⚠️ **16/24 复核里有三个不利趋势照记不误**：`|最大|÷H1效应` 0.233→**0.812**（逼近 1）、
  `均值÷H1效应` 0.047→0.159、`r` 0.992→0.952。**若第三批继续，口径① 会越过 1。**

- **完成（`4c8d2ec`，已推送）**：修掉 `analyze_r18.py` `load()` 里的
  `(bad if collapsed else R).setdefault(0, None)`——**`bad` 是 list，没有 `setdefault`**，
  于是**只要有任何一个 run `collapsed=True`，判决脚本就抛 `AttributeError`**。
  §21.5 恰恰把「崩溃 run 分开报，不池化」写成护栏，**而这条护栏的实现本身是崩的**。
  没被发现是因为那条路一次都没走过：脚本是跑之前写的、提交时手上没有真数据，
  而 R16 同臂实测 0/24 崩溃。R18 是它的 3.5 倍长，触发概率不是零。
  验收 `explorations/20260805-r18-verdict/verify_r18_load.py`：
  **第 0 步先原样复现修复前那一行、确认它确实抛**，再验四条路径。
  `check.py --full` 50 项全过、golden 未动。

- **顺带验过的两件（都通过，不必再验）**：
  - `analyze_r18.py` 在真 RUN_DIR 上空跑（0/24 完成时）**优雅退出**，
    打印「未跑满 24，以下只是管路空跑，不是判决」——`1dfb822` 的承诺成立。
  - H1 的 `early` 窗（gen 110–148，只有 38 代宽）**不会切出空窗**：
    `traj` 是每 ~263 步一个采样点（不是 6 个），R16 的 48 个 run 里
    落进该窗的采样点 **min=12 / 中位 76 / max=130，0 个 run 为空**。
    （空窗的后果本来会很脏：`wmean` 的 `max(w.sum(), 1e-12)` 会**静默返回 0.0
    而不是 NaN**，于是 `early` 格均值变 0、Δ 变成一个巨大的假「上升」。）

- **PENDING（下次第一件事）**: 等 `grep -l '^JSON '` 数到 **24/24**，
  跑 `XLA_PYTHON_CLIENT_PREALLOCATE=false .venv/bin/python
  explorations/20260804-readouts/analyze_r18.py`，按 `multispecies_program.md`
  **§21.4 + 新增的 §21.4b 出判决**写进 `multispecies_feasibility.md`。
  **⚠️ §21.4b 是本轮新写的，判决前必读**（`f390945`）：
  - **H2 不是独立判据**——`split_score` 的谷深因子实测恒为 1.0000（4488/4488），
    晚窗上 `split_score ≡ 1 − low_mass` **精确成立**（残差 0.00000、3501 个检查点）。
    **不许把「H1 上升 + H2 下降」报成两条互相矛盾的发现，那是把一个数报了两遍。**
    （R16 的 H2 是同一个失误的第一次。）
  - **实测是显著为正**（8/24 上 Δ+0.0665、4/4、比值+4.67），而 §21.4 只为
    「≈0 / 显著为负 / 比值<1」写了解读。§21.4b 已把这一格的解读**在看到全部数据
    之前**写死：`low_mass` 上升 = **果专精侧占比上升**，不是衰减；
    **两个轴分开报**——「双簇还在不在」看两峰/谷深/`retained`，
    「对称性漂到哪」才看 `low_mass`。
  - 报**实测跨代数**（16/24 上是 **371 [345, 424]**），**不许写成「450 代」**；
    崩溃 run 分开报；`retained_occ` 已知饱和、**不进主判据**。
  - 再跑一次 `diag_h2_degenerate.py`（24 run 上的数会变，结论未必变）。
    **§21.4b 的 16/24 复核表里写死了「判决时以 24/24 的重跑为准，不许引用本表」。**
  - **§21.4b 的第二个轴现在有读数了**（`73fe8cc`）：「两峰都找到的占比」与
    「谷未见底度」。⚠️ **两条在 16/24 上都完全饱和**（1.0000 / 0.0000、噪声 0），
    所以它们**不做检验、只当监测器**——脱离饱和才是信号。
    饱和的方向本身是本轴的定性答案：**371 代里两峰一个检查点都没消失、谷全程全空**。
    **不许把它写成「经检验双簇稳定」——没有检验，只有饱和。**

- **16/24 的读数（管路空跑，不是判决）**：`low_mass` 晚 0.5791 vs 早 0.5072、
  **Δ +0.0719、8/8 为正、p 触地板 0.00781、比值 +5.92**；
  H3 **16/16** vs 零分布最大 6/4/3/1、四个 `Ne` 全部 p<0.005。

- **坑**: `analyze_r18.py` 里 `np.mean([])` 会在某个 seed 两个 rep 全缺时发
  RuntimeWarning 并返回 nan——**这是保守方向**（nan 会被 `report()` 的
  `np.isfinite` 排除出配对），不要「修」成填 0。

---

## Session 2026-08-05 上午（R16 + R17 判完，纲领走到「隔离的遗传后果」）

- 授权: 用户拍板三件——烧卡 CONC=3、方向=生殖隔离、不做 Artifact。
  opus5 执行、**fable5 审查/判决 4 次**，每次都改变了结论的措辞。

- **R16 判决**（`feasibility.md` §19，`fe54d49`）：**H1 达成**——148 代上双簇
  不是漂变+复发突变造得出来的（24/24 vs 零分布最大 5/4/2/0）。
  **H2 undecidable**（比值分母不存在；那个 12/12 是恒等式 `Δ≡1−首1/4`）。
  **H3 达成** +0.7205。⚠️ **这一轮没回答自己标题的「200+ 代」**：`R38n` 只到 134–165 代，
  而 `R38p` 的陡降恰在 gen 200–452。
  **更正**：不能说「两臂都拒绝中性」——`R38p` 的拒绝由 4 个**处理失效**的 run 扛着，
  存活的 20 个只有 3/20、`Ne=340` 上 p=0.1095 不拒绝。

- **R17 判决**（`feasibility.md` §20，`88b8f44`）：**H1 达成不打折**——
  同型交配把两簇的**全基因组分化**抬高 **+47.8%**（Δ+0.25506、10/12、p=0.00488、
  三口径 1.44/1.96/4.94 全过闸、**超过在看到主臂之前就算死的 MDE 0.1766**）。
  那条「反向」的 `split_score` 是**读数在分裂变尖时失效**（严格不等号 + 平台
  ⇒ 主臂 12/24 按构造返回 0），容忍平台后差异**整体消失**；
  每个独立形状读数都说分裂**变尖了**（簇间距 +0.1024、12/12、比值 +3.67）。
  **隔离不是免费的**：种群代价 **−5.6%**（2/12、p=0.0049、比值 −1.41）。

- **三个修（都跑过各自的回归验收）**：
  - `exp_stats.underpowered` 的**饱和洞**——`abs(nan)<1.0` 是 `False` ⇒
    饱和读数被报成「功效充足」，**连续三轮没被发现，因为它的失败方式是沉默**。
  - `split_score` 的**平台失效**（第四次同型失误）。R11 的 144 run 上变化 ≤+0.0002 ⇒ 向后兼容；
    Stage 2 的已发表值就地更正（`R38n` 0.4423→0.4501），定性结论不变。
  - `_ld_fn` 补回传**分子/分母**，补掉 §20.1 那个 undecidable。
  - `docs/conventions.md` 新增 **§11「饱和的读数不能当判据的分母」**。

- **PENDING（下次第一件事，要用户拍板）**：两条候选，都要烧卡——
  ① **`R38n` 跑到 ~450 代**（约 300k 步、48 run）回答 R16 标题里的「200+ 代」；
  ② 让 `mate_forage_weight` **本身可遗传**（加基因位）回答「隔离能不能自发演化」——
  **那会动 `trait_dim`、作废已演化种群、golden 重 bless**，是更大的改动。

- **坑**:
  - **`merge.conflictstyle=zdiff3` 会让 `git stash pop` 静默丢工作**
    （用 `git show 'stash@{0}:path' > path` 绕开）。**建议用户 unset。**
  - **「跑两次 diff 输出」不能当 bit-exact 验证**（同代码两次 47 个字段不同），
    改用 `jax.make_jaxpr` 指纹，且脚本要 assert 拒绝空输出。
  - sweep/分析在跑时**源码树是冻结的**（它会陆续起新进程 import `underworld`）。
