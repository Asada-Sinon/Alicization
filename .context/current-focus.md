# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前状态（2026-08-03）：**两件都做完了，等用户定盘**

用户拍板的两件事都已完成并推送：

1. **升级统计协议** —— ✅ 完成（12 种子 × 2 重复，实现收进 `scripts/exp_stats.py`）。
2. **把果层挪出共居带** —— ✅ 走完并**判负**（`docs/multispecies_program.md` §12.3.A'/B/C，
   负结果进 `docs/experiments.md §5.6`）。

**此刻没有在跑的实验，没有半途而废的东西。**

## 为什么第 2 件判负（一句话）

**不是没做到，是这个世界做不到。** `fertility = grass_base + forest_bonus·forest` 且
`grass_base=0.65` ⇒ 草层动态范围**结构性封顶 1.538×**、铺满整张地图。把果层推到最远，
它所在格子的草量仍是陆地均值的 **0.966 倍——没有一处「有果实、没有草」**。
能挪的只有果层，而另一个饭碗无处不在。

## 下一件事 = 要用户拍板，不擅自开跑

**三条资源轴（加厚 / 等量再分配 / 空间分隔）已全部走完并全部关闭。** 剩下两个方向，
**都是重设世界一级的决定**：

- **A) 给草层空间结构** —— 压低 `grass_base`、抬高 `forest_bonus`，让 `fertility` 有真正的
  动态范围。这是 §12.3.C 判负直接指出的那条。
- **B) 放松水本身**（§12.5 原本写的那条）。

两者都会**改变默认世界的每一个既有结论**：`plant_max` 的全部调优记录、`golden.json` 全部
数字、食草种群的空间分布、以及所有引用过它们的文档。**未测，也不擅自开跑。**

## 非目标

- **不再动果层的任何参数**（加厚、等量再分配、空间分隔——三条都关闭了）。
- 不动 `in_dim`（作废全部演化脑）。
- 不为让实验「成立」而放宽 `scripts/golden.json` 的 band。
- 不要重捡：加厚食物层、等量再分配、生殖隔离（`assortative_mating` on `forage_pref`）。

## 纪律（下一轮仍然适用）

- **空间分隔类的想法先算地形再跑种群**——地形量不受样本量限制，§12 花了 63 个 run 才判负，
  而最硬的两条证据只要 `terrain.build` 跑一次（`MEMORY.md [LEARN:method]`）。
- **sweep 跑的时候，它读的每一个文件都不许动**（脚本本身、`underworld/`，两种都不报错）。
- 统计一律走 `scripts/exp_stats.py`，改了它必须重跑
  `explorations/20260803-partition/verify_exp_stats.py`。
- **n=3 的参照臂不要用作分母**——C 阶段的「食肉者 −69%」就是被单个 run 绑架的假象。
- 判决派 `result-analyst`，主控不自己读几十份日志。
- 长输出一律重定向到文件（套件是**主机 CPU 编译**瓶颈，终端刷屏会把它拖慢 2.7 倍）。
- 提交身份 `Asada-Sinon <weibinkong.research@gmail.com>`（已设 repo-local）；
  push 用 `GIT_SSH_COMMAND="ssh -o BatchMode=yes" git push`。
