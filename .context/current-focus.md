# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前状态（2026-08-03 深夜）：**R10 判完，正在跑决定下一步的饱和探针**

判决全文 `docs/multispecies_feasibility.md` §12；纲领判决行 `multispecies_program.md` §14.7。

### 这条线走到哪了（三层，一层比一层深）

1. **R9**：结构化草层造出了空间生态型关联（12/12，p 触地板），但基因**均值**动了、**方差**没动
   ⇒ 一条 cline，不是两个物种。
2. **§11（代数）**：`_forage_pref_scale` 是**直线**权衡前沿，`W(s) = A·g + B·f` 对 `s` 线性
   ⇒ `d²W/ds² ≡ 0`。**分歧选择没有一阶项可以来自**——这一条一次解释掉五条归档阴性。
   装了 `forage_curvature`（默认 1.0 bit-exact），`k<1` 让前沿弯向原点。
3. **R10（96 run）判决**：**不是「弯前沿没用」，是它能注入的曲率只有基线负曲率的一半。**
   可注入 `(1−k)(A+B)`：K05 = +0.087；而**实测基线 `W''(k=1) = −0.137`**（t=−4.14）。
   完美交付后仍是 −0.050，**依然凹**。§14.6 触发 undecidable，但**补功效没有意义**。

### 此刻在跑：`explorations/20260803-curvature/probe_saturation.sh`（24 run，探针非结论）

**问的是：基线凹度从哪来。** analyst 的 `[提案]` 是**供给饱和**——
`gain = demand·min(1, plant_cell/demand_cell)`，提高自己偏好层的 demand 买不到成比例的摄入，
而放弃另一层的损失**全额兑现** ⇒ 实现摄入是自身乘子的凹函数，**与 k 无关**。
量级佐证：人均摄入 0.164 而 `eat_rate=1.5`，需求绝大部分未被满足。

**两条互斥的降饱和路径，这是设计要点**：

| 臂 | 手段 | 加食物？ |
| --- | --- | --- |
| `S0` | 基线 | — |
| `S1` | `eat_rate` 1.5 → 0.5（降需求） | **没有** |
| `S2` | `regrow_baseline` 0.010 → 0.060（加供给） | 有 |

**判读**：只有 `S2` 让 `W''→0` ⇒ 那是 `experiments.md §5` 已关闭的「加食物」老故事，不算数；
**`S1` 也让它趋 0，饱和机制才坐实**——而且那条路不加承载力，绕开了那堵墙。

## 下一步（依探针结果二选一）

- **饱和坐实** ⇒ 在松开饱和的世界里**重跑曲率扫描**（那时基线凹度降下来了，
  `(1−k)(A+B)` 才盖得住）。预注册时**必须同时写 MDE 与「预测效应 ÷ MDE」**——
  R10 致命的是后者（0.209），不是前者。
- **没坐实** ⇒ 基线凹度另有来源，先找到它。**那种情况下重跑曲率扫描仍是白跑。**

## 非目标

- **不许降 `k` 再试一轮**（§14.6 禁令）——瓶颈是基线凹度不是 `k`，降 `k` 顶多打平不会翻正。
- 不许把 R10 读成「曲率手术证伪」（比值 0.717 < 1，是 undecidable）**或「显著反向」**（7/12、p=0.092）。
- 不动 `in_dim`；不放宽 golden band。
- `quad_demog` **已弃用**（中性臂读 +0.0763 而真值必须恰为 0，有可测正偏倚）。

## 纪律

- **预注册必须写「预测效应 ÷ MDE」**，不只是 MDE。R10 是这条的来源。
- **拿小 pilot 定 MDE 要用 `σ̂` 的置信上界**（`MEMORY.md [LEARN:stats]`，R10 低估 54%）。
- **开 sweep 前先 `nvidia-smi`**——卡是共用的（`MEMORY.md [LEARN:env]`）。
- **分析脚本必须进版本库**，否则输出的 txt 无法重跑（R10 差点犯）。
- 判决派 `result-analyst`（fable5）；计划派 `plan-critic`（fable5）**在动手之前**——
  本 session 它两次拦下会白跑的设计，一次带出了比方案本身重要得多的 §11。
- 提交身份 `Asada-Sinon <weibinkong.research@gmail.com>`；
  push 用 `GIT_SSH_COMMAND="ssh -o BatchMode=yes" git push`。
