# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前状态（2026-08-03）：**R9 判完，96 run 已落档**。下一件事要重新论证，不要直接开跑

判决全文 `docs/multispecies_feasibility.md` §10；纲领判决行 `multispecies_program.md` §13.7。

**一句话：分离做到了，物种没做出来。**

- **正结果（本纲领九个回合里第一个空间生态型信号）**：遮荫世界里选择造出了稳健的
  生态型—生境关联，原始 `r` 的 `T1−N1` = **−0.2697**，**12/12 种子**，p=0.00049 触地板，
  效应/噪声 **−1.672**。遮荫把它放大了 **5.29×**。
- **但它不是分化**：基因**均值**动了（`herb_forage_pref` +0.0607，12/12，p 地板），
  **方差与双峰性没动**。⇒ **方向性选择 + 空间分选 = 一条 cline，不是分歧选择，不是两个物种。**
- **根因一个数解释完三个结果**：果层只占陆地能量的 **11.15%**
  （草承载 18828.7 vs 果承载×`fruit_energy` 2362.8，实跑配置 `plant_max=2.0`）。

## 下一件事 —— **要先论证，不要直接开跑**

`feasibility.md §10.10` 排了三条，**第 1 条不是自动成立的**：

1. **换自变量：攻果层能量占比（11.15%），不再攻分离度**（分离度已到顶，果富格草量中位
   0.000×）。**但「加厚果层」是 `experiments.md §5` 已关闭的路线**——那条是在**共址**几何下
   加厚（必然只变成更多身体，§10 跨回合命题）。现在几何已经分开了，加厚的去向不再是同一个碗。
   **这个区别必须先写成可证伪的论证再开跑，否则就是重捡已关闭路线。**
2. **H3 交互补种子**：只需 `s≈14`，补 seed 12–13（8 run）。**但这是看到结果后才决定的样本量
   增补（optional stopping）**，若做必须单独预注册、并同时报 12 与 14 两个 n，不许只报后者。
3. **R9-E 跨地形**（`ridge_wavenumber` × `ridge_base_frac`），脱离伪重复——
   H3 是空间性 claim，目前只对「这条河系 + 这条海岸线」成立。

## 悬而未决（要用户拍板）

**`water_sea_dist` 翻不翻默认值。** R9 全部四臂都开着它，数据说它不破任何护栏。
但翻默认值会**翻转 `biology.md:156` 的核心不等号**（全格离水中位 35.46 → 27.45，跌破感知
上限 30）并作废 `fruit_dry_d0/d1` 的标定。逐条代价在 `multispecies_program.md` §13.2。

## 非目标

- 不动 `in_dim`（作废全部演化脑）。
- 不为让实验「成立」而放宽 `scripts/golden.json` 的 band。
- 不再攻分离度（已到顶）。
- **不许把「塌缩被地形解除了」写成结论**——那句和交互项（p=0.339）是同一件事，
  且 `T1−N1` 的 CI 把平世界塌缩量包在里面（§10.5）。

## 纪律（下一轮仍然适用）

- **预注册必须带主判据的 MDE。** R9 只给护栏定了容差，主判据没算功效，
  结果 H1/H2 在功效 0.159/0.216 下判 undecidable——**这是 §11.5 教训的第二次重犯**。
- **空间分隔类的想法先算地形再跑种群**，且**地形脚本必须跑在实验的配置上**
  （R9 的脚本写死 `Config()` 而实验跑 `plant_max=2.0`，比值没错、绝对数错 10%，§13.8）。
- **开 sweep 前先 `nvidia-smi`**——这张卡是共用的（`MEMORY.md [LEARN:env]`）。
- 统计一律走 `scripts/exp_stats.py`；判决派 `result-analyst`，主控不自己读几十份日志。
- 提交身份 `Asada-Sinon <weibinkong.research@gmail.com>`；
  push 用 `GIT_SSH_COMMAND="ssh -o BatchMode=yes" git push`。
