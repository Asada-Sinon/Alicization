# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前状态（2026-08-06 晚）：**R18-B 判完，卡空着，只剩 R19 要用户拍板**

判决全文 `docs/multispecies_feasibility.md` **§24**。判据 `multispecies_program.md`
§23 + §23.4b（跑到 8/24 时补的「暂态 vs 并合」判据，**不改任何阈值**）。

### 这条纲领现在走到哪了

| 问题 | 状态 |
| --- | --- |
| 这个世界坐得下第二个食草生态型吗 | **坐得下**（R9→R14 九项干预造出来的） |
| 那是不是漂变造的假象 | **不是**（R16 148 代、R18 375 代、**R18-B 669 代**，四个 `Ne` 全 p<0.005） |
| 双簇稳不稳 | **形成于头 13 代，到 669 代未并合**（唯一一次晚期扰动跨 1.1 代、随即恢复） |
| 那还是 50/50 吗 | **不是**——多数侧涨到 **0.5963**，**少数侧停在比峰值低 15.5% 处**（0.4778→0.4037） |
| 那个 50/50 是起点吗 | **不是，是穿过的一个点**（第 1 段只有 0.3194） |
| **漂移会停吗** | **停了，在 gen ~400 之后**——但**「停了」= 「小于分辨率」，不是「零」** |
| 加上生殖隔离会怎样 | 两簇的全基因组分化 **+47.8%**（R17，代价 −5.6% 种群） |
| 隔离能不能**自发演化** | **没做**（R19 已预注册，`program.md` §22，**要用户拍板**） |
| 那是两个物种吗 | **不能说**——`diet` 轴上仍是同一个食草者 |

### R18-B 的一句话

**漂移在 gen ~400 之后停下**：末段增量 +0.00193、95%CI [+0.00017, +0.00344]
完全落在 ±δ(0.00549) 内 ⇒ 按 §23.4 的**等价性检验**判「停了」。

⚠️ **两个限定缺一不可，否则会被读成「测到了零」**：
- **CI 不含 0**（p=0.05225）——残余漂移 ≈ +0.0019/段**仍可测到**。
  8/24 与 16/24 时 CI 还含 0，是样本量涨到 12 格才分辨出来的。
- **该判定的宽严**：CI 半宽只有 **0.30·δ**，即 **|效应| < 0.00385 就判停**；
  而 δ 是**用同一批数据自估的噪声**（§23.3 预注册口径），不是独立的实际等价界。

⇒ 准确表述：**残余漂移小于本实验 0.0055 的分辨率界。**

### 本轮另外两个值得记住的数

- **R18 的几何外推被定量检验**：预测 669 代时 0.5969，实测 **0.5947 [0.5927, 0.5967]**
  ⇒ **高估 0.38%，且预测落在 CI 之外**。模型方向对、量级对，但系统性偏高。
- **H4 的零分布随时程变宽**：R18（375 代）最大 5/5/5/1 → R18-B（669 代）最大 **9/5/4/2**。
  **跑得越久，纯漂变越有机会造出一个满足 `retained` 的双峰** ⇒
  **更长的时程本身会让这条判据变松**（实测 24/24 仍远在其外，但下次设计要考虑这点）。

### ⚠️ 上一轮（R18）的判决被 fable5 复核改掉四处

全部已改（`8edbcb4`），**每一处都是变弱的方向**：①「谷全程全空」是假的（那两个窗
只覆盖 41.7% 检查点）；②`retained` 24/24 违反 §21.4b 缺口③ 自己写的禁令；
③§19 那条撤回**用错口径**（应为帧比例的 +0.0015、5/12、p=0.850）；
④末段增量的**分析单位违规**（逐 run 而非格均值）。详见 `feasibility.md` §22 各更正块。

## 下一步：**只剩 R19，要烧卡、要拍板**

让 `mate_forage_weight` **本身可遗传**（加基因位）回答「隔离能不能自发演化」。
**R19 已预注册**（`program.md` §22），且 §22.2 自己把「这很可能阴性」写在最前面
（它是一个**修饰基因座**，而本项目已在同类问题上判过一次负）。

⚠️ **代价**：会动 `trait_dim`、**作废所有已演化种群**、`golden.json` 必须重 bless。
**这三条都要用户点头。**

## 非目标

- **不能说「物种形成涌现了」**，也**不能说「两个物种」**。
- **不许把 669 代写成「700 代」**（§21.2 明令，任何转述层都不许）。
- **不许把「停了」写成「达到平衡」或「测到零」**（CI 不含 0，δ 是自估噪声）。
- **不许写「全程」「每一个检查点」**（双簇形成于头 13 代，不是一开始就在）。
- **不许只报多数侧**而不报少数簇停在低 15.5% 处。
- **不许拿 `retained` 当「双簇撑住」的证据**（它只看多数侧）。
- 不外推到**有捕食者的世界**、**别的地形**（单地形伪重复）、**默认世界**（9 项覆盖）。

## 纪律（每条都有实测代价）

- **等价性检验要自报宽严**：δ 若来自自估噪声，「等价成立」只等于「小于分辨率」。
- **只在窗内验证的读数，不能用「全程」陈述**——先报覆盖率。
- **分析单位是格均值，不是 run**——错了会把 9/12 说成 21/24，而且方向是「更显著」。
- **改一个已发表数字前，先确定它当初用的是哪个口径**（用候选口径去复现它）。
- **两个读数并排当证据前，回到构造看它们是不是彼此的确定性函数**。
- **结束标记要匹配最后一行，不是每行都有的词**——过早报完成会让人拿残缺输出写结论。
- **不写死会过期的常数**：脚本里的百分比当场算。
- 统计**一律走 `scripts/exp_stats.py`**。
