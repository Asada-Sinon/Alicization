# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前状态（2026-08-05 晚）：**R18 判完，卡空着，下一步要用户拍板**

判决全文 `docs/multispecies_feasibility.md` **§22**。判据 `multispecies_program.md`
§21 + §21.4b（跑到 8/24 与 16/24 时写的两次勘误，**一个阈值都没动**）。

### 这条纲领现在走到哪了

| 问题 | 状态 |
| --- | --- |
| 这个世界坐得下第二个食草生态型吗 | **坐得下**（R9→R14 九项干预造出来的） |
| 那是不是漂变造的假象 | **不是**（R16 148 代、**R18 375 代**，四个 `Ne` 全 p<0.005） |
| 它稳不稳 | **双簇撑住了 375 代**——两峰全程在、谷全程空、`retained` 24/24 |
| 那还是 50/50 吗 | **不是**——果专精侧 0.5070→**0.5820**（12/12、p 触地板、比值 +6.63） |
| 那个漂移会停吗 | **在减速但没停**（末段增量是首段的 1/17，仍 21/24、p=0.00065） |
| 加上生殖隔离会怎样 | **两簇的全基因组分化 +47.8%**（R17，代价 −5.6% 种群） |
| 隔离能不能**自发演化** | **没做**（R19 已预注册，`program.md` §22，**要用户拍板**） |
| 那是两个物种吗 | **不能说**——`diet` 轴上仍是同一个食草者 |

### R18 的一句话

**§21.1 的问题有两个答案，必须分开说**：
问「两个簇还在吗」——**在**；问「还是那个 50/50 的平衡吗」——**不是**。

**三条禁令**（§21.4b 写死于只看到 16/24 时）：不许只写「撑住了」（丢掉漂移）、
不许写「在衰减」（说反方向）、不许写「已达新平衡」（末段增量仍显著为正，
渐近 0.5977 是外推）。

### 本轮撤回的一条已发表数字

`feasibility.md` §19 的 `split_score` Q4−Q3 从 **+0.0691（比值+3.73）** 重算为
**+0.0053、9/12、p=0.064（不显著）**——平台失效的假值。
**§19 的定性结论不变、主论据反而更强**（`low_mass` 不经过 `split_score`）。
根因：平台修复 `88b8f44`（09:50）比 R16 判决 `fe54d49`（04:50）**晚 5 小时**，
当时只更正了 Stage 2 的一个值。

### 本轮量出来的那条恒等式（会反复用到）

谷见底的干净双峰上 **`split_score ≡ min(low_mass, 1 − low_mass)`**
（32784 个检查点上 94.2% 精确成立）。**`min` 在 0.5 有尖点、导数在那里变号** ⇒
`split_score` 是 `low_mass` 的非单调函数，一个单调上升的 `low_mass` 会让它
先升后降，呈现为「一升一降」的假矛盾。
⇒ **这两个读数并排时不是两条证据。**

## 下一步：**只剩 R19，要烧卡、要拍板**

让 `mate_forage_weight` **本身可遗传**（加基因位）回答「隔离能不能自发演化」。
**R19 已预注册**（`program.md` §22），且 §22.2 自己把「这很可能阴性」写在最前面
（它是一个**修饰基因座**，而本项目已在同类问题上判过一次负）。

⚠️ **代价**：会动 `trait_dim`、**作废所有已演化种群**、`golden.json` 必须重 bless。
**这三条都要用户点头。**

## 非目标

- **不能说「物种形成涌现了」**，也**不能说「两个物种」**。
- **不许把 375 代写回「450 代」**（§21.2 明令，任何转述层都不许）。
- 第二个轴（双簇还在不在）是**饱和给出的定性答案**，**不是检验**——
  不许写成「经检验双簇稳定」。
- 不外推到**有捕食者的世界**（`diet_delta=1.5` 由构造无捕食）、
  **别的地形**（单地形 `ridge_wavenumber=1`，伪重复）、
  **默认世界**（9 项覆盖，`forage_tradeoff` 默认 0.0）。

## 纪律（每条都有实测代价）

- **两个读数并排当证据之前，回到构造看它们是不是彼此的确定性函数**——
  相关系数检不出来（`r=0.992` 看起来只是高度相关，真实关系是两段折线）。
  这条已经咬了两次，第二次撤回了一条已发表数字。
- **读数失效的修复要回头重算所有引用过它的已发表数字**，不能只改一处。
- **不写死会过期的常数**：脚本里的百分比要当场算（8/24 是 94.4%、16/24 是
  94.6%、24/24 是 94.2%）。
- **饱和的读数不能当判据的分母**（`conventions.md` §11）。
- **顶在地板上的 p 是分辨率耗尽**（n=12 的 0.00049、蒙特卡洛的 `1/(reps+1)`）。
- **只报支持自己结论的那一半数字是禁忌**——16/24 复核里三个不利趋势照记不误。
- 统计**一律走 `scripts/exp_stats.py`**。
