# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前状态（2026-08-03 晨）：一轮自主循环已跑完，**等用户拍板**

用户睡前定盘「局部的多物种生态箱演化 + 可视化结果」，要求做成循环一直跑。
**循环跑完了六个回合，全部落档并推送，可视化已交付。此刻没有在跑的实验。**

工作流在 `docs/multispecies_program.md`（纲领 §1 / 七道闸 §2 / 台账 §3 / backlog §4）。
**下一个 session 的第一件事是把 `HANDOFF.md` 的「要用户拍板」三条拿给用户，不要自己挑一个开跑。**

## 这一轮的两条结论（都写进了 docs）

1. **生态**：加食物造不出生态位（水限世界里只会变成更多身体，`experiments.md §5`）；
   而分化不发生的真原因是**中间型最优**，不是「没有饭碗」也不是基因流
   （§9.6–§9.9）。**资源分割需要的不是「第二个饭碗」，而是「两个饭碗离得够远」。**
2. **方法学（影响面更大）**：`--seed` 在 20000 步上控制不住这个世界，配对差噪声被
   `conventions.md §5` 低估 5.2 倍（`docs/run_to_run_variance.md`）。
   **任何新实验开跑前先定统计协议**（每格 k=3 重复），否则判决是抛硬币。

## 三条已关闭的路（别让下一个 agent 重捡）

- ~~加厚食物层造生态位~~、~~等量再分配~~、~~生殖隔离 `assortative_mating` on `forage_pref`~~
  （最后这条是我自己预设的失败子句，前提已被 R2 推翻——它对抗基因流，而实测是稳定化选择）。

## 纪律

- 并行跑实验**最多 6 个并发**；多参数 sweep **一律写 bash 脚本 + 数组传参**（zsh 不词分割）。
- 空间结论必须交叉地形种子；空间指标先算零模型。
- 跑实验前先 commit；判决派 `result-analyst`，主控不自己读十几份日志。
- 提交身份 `Asada-Sinon <weibinkong.research@gmail.com>`（已设 repo-local）；
  push 用 `GIT_SSH_COMMAND="ssh -o BatchMode=yes" git push`。
