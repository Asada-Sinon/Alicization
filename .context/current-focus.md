# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前目标

**恐惧地景（`eca17e5`）已落地并默认开启，这一轮结束。当前目标是收尾 + 定盘：
把 `docs/TODO.md` 的「队列」一节同步到实际状态，然后由用户在两个候选方向里选一个。**

下一个研究方向**待确认**——不要自己挑一个开跑。

## 为什么做

- `docs/TODO.md` 是每个新会话的权威入口（`CLAUDE.md`「Research lands in `docs/`」那节要求
  新结论都在它里面留指针），但它的「队列」一节已经落后于最近 5 次 commit：仍把已完成的
  「优化/清理审计」写成下一步（`27c01cc`/`33d5baf`/`6f5ec55` 已做完），仍把恐惧地景写成
  未做的「正路」（`eca17e5` 已落地）。文档地图部分是最新的，队列部分不是。入口是错的，
  下一个会话就会从错的地方开工。
- 方向必须用户定盘：两个候选都会动实际世界行为或 golden 带，而 `scale_and_density.md` §5.2
  已明确写「本轮不擅自改世界大小/承载力……应由用户在 A–D 间定盘」。

## 候选方向（供用户选，均未被确认）

1. **D：修比例（压 carn_frac）** —— `scale_and_density.md` §5.3 的净判决。用户原本选的是
   B+D（扩世界 + 修比例），B 已被两轮探针证伪（密度是尺度不变量），**D 是唯一幸存且低风险
   的一条**：把 carn_frac 从当前 ~10–12% 往现实 <1% 压，走 `carnivore_riparian.md` 的
   `carn_cost` / 能量转换路线。注意 `attack_range` 红皇后（`attack_range_redqueen.md`）与恐惧场
   已经各自把 carn_frac 压过一次，起点已经不是当初的 25%。
2. **补「河岸带停留时长 / 到访频率」指标** —— `landscape_of_fear.md` §6「未做/后续」第一条。
   当前只有 `carn_water_dist` 静态均值，分不清「缩短停留」和「完全不去」，所以判不了恐惧场
   究竟改变了哪种行为（§3.2 的第 3 条可证伪预测因此还悬着）。成本低、纯观测、不动世界行为。

## 完成判据

- `docs/TODO.md` 的「队列」一节读起来与 `git log --oneline -10` 一致：上面两处过期已改，
  且明确写出下一个方向是什么。
- 用户在候选 1 / 2 / 其它之间给出选择，本文件被整个重写为那件事。
- `.venv/bin/python scripts/check.py --contracts` 过（纯文档改动，这一条就够）。

## 不做什么

- **不要自己挑一个方向就开跑。** 两条候选都不是既定任务。
- **不要动 `world_size` / `n_max` / 承载力**去「降密度」——`scale_and_density.md` §5.3 已实测
  证伪（密度是强度量，扩世界只是把同样的拥挤度铺到更大的图上并把 agent 顶到 `n_max`）。
- **不要用 `plant_max` 压种群**——同上，几乎不动密度，却在 ≤1.2 时直接饿死捕食者。
- **不要为了让 `carn_water_dist` 更好看去调 `fear_*`**——6 种子已测过四组设置，
  「搬离河岸」缺的是昼夜通勤，不是参数。
- **不要单种子下判断**（`CLAUDE.md` / `conventions.md` §5：6 配对种子起步）。
