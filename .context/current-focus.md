# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前目标（2026-08-02 夜 → 08-03，用户睡前授权自主长跑）

用户定盘：**局部的多物种生态箱演化**，终点要**看得见的可视化结果**，要求做成一个循环
一直跑下去、不许干一半停。工作流定死在 `docs/multispecies_program.md`
（纲领 §1 / 七道闸 §2 / 台账 §3 / backlog §4）。**此刻在跑 R3 等量再分配。**

## 已判（全部已 push）

- **R1**（现存量口径）→ **判据自身被证伪**：现存量量的是「剩多少」不是「供多少」。
  产出 = `Metrics` 三个流量观测量。
- **R1b**（流量口径）→ **不通过**：份额做到 21%（6/6、p=0.031）但渴死护栏破 2–3/6。
  顺带更正自己一条过度解读（林冠移动相对零模型可忽略）。
- **R1c**（果实预成水）→ **不通过，且我的诊断被推翻**：真因是**承载力**不是水口径
  （ρ=+0.90 与种群超调共变，`mean_water` 三臂对基线全无显著变化）。
- **§10 跨回合结论**：**水限世界里加食物只会变成更多身体**。「加厚食物层造生态位」
  整条路证伪，已进 `experiments.md §5`。

## 本夜剩余计划（时间预算，别被科学线挤掉最后一项）

1. **R3 等量再分配**（§11，在跑）：总植物通量守恒 + 份额 ≥20% + 四条护栏 6/6。
   失败则资源轴整条关闭。
2. **R2 分化测试**（§9）：厚果层 × `forage_tradeoff` 0/0.5/1.0 的 2×3 因子。
3. **R4 空间生态位读出**（S3，必须跨地形种子）。
4. **R7 可视化交付** —— **用户点名要的东西，无论前面结论如何都要做完**。
5. `HANDOFF.md` 收尾 + 最后一次状态提交。

## 非目标

- 不动 `in_dim`（作废全部演化脑）。
- 不放宽 `scripts/golden.json` 的 band，也不在看到数据后改判据读法。
- 不擅自把会改默认生态的机制转为默认开——写进 HANDOFF PENDING 交用户。
- 生殖隔离（`assortative_mating` 扩到按 `forage_pref` 排序）是要用户拍板的岔路，不擅自开跑。

## 纪律

- 并行跑实验**最多 6 个并发**；多参数 sweep **一律写 bash 脚本 + 数组传参**（zsh 不词分割）。
- 生态改动 6 配对种子起；空间结论必须交叉地形种子；空间指标先算零模型。
- 跑实验前先 commit；判决派 `result-analyst`，主控不自己读十几份日志。
- 提交身份 `Asada-Sinon <weibinkong.research@gmail.com>`（已设 repo-local）；
  push 用 `GIT_SSH_COMMAND="ssh -o BatchMode=yes" git push`。
