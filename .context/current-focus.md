# 当前焦点

只写此刻在做的那一件事。方向变了就整个重写，不要往下堆。超过一屏说明该拆了。

## 当前状态（2026-08-03）：**R9 在跑** —— 96 个 run，判据已预注册并提交

**做的是什么**：`docs/multispecies_program.md` §13。八个回合动的一直是**果层**，
§12.3.C 判负时自己指出了唯一没走过的方向——**草层**。本轮动它。

**为什么这次不一样**：§12 判负的决定性证据是「没有一处「有果实、没有草」的格子」。
本轮先算地形（`MEMORY.md [LEARN:method]`），在花掉任何 GPU 时间之前就把这个前提做出来了：

| 臂 | 果富十分位处草量÷陆地均草 | 「果前10% ∩ 草<30%均值」格数 | 草重心水距 |
| --- | --- | --- | --- |
| 现状 | **1.307** | **0** | 49.75 |
| shade 单独 | 0.491 | 24 | **59.27** ⚠ 撞水墙 |
| 认海单独 | 1.285 | 0 | 37.22 |
| **两者 (shade=1.3)** | **0.125** | **173 (80.5%)** | **44.15** ← 低于基线 |

**「分离」与「可达」第一次不再对立。** 两个旋钮必须一起上，单独任何一个都不成立。

**已落地**（`2bb48e0` / `231c016`，都已推）：
- `grass_shade` + `water_sea_dist` 两个默认关、bit-exact 的旋钮，11 项单测绿，
  `check.py --full` 42 项绿、golden 未动。
- §13 预注册（判据、护栏容差规则、失败判据、放弃条件）跑之前已提交。

**在跑**：`bash explorations/20260803-shade/sweep_r9.sh` →
`outputs/20260803-shade/r9/`，96 个 log，2026-08-03T14:44 启动，预计 ~1.5 小时。

## 下一件事

1. sweep 跑完（96 个 log 都有 `JSON ` 行）→ **派 `result-analyst` 出判决**，
   照 §13.5 的预注册判据，主口径是**交互项** `d_s = (T1−N1) − (T0−N0)`。
   注意交互项的噪声是 `2·σ̂_W/√r`，即 `pair_noise` 的 `√2` 倍。
2. 判决写进 `docs/multispecies_feasibility.md`（结论归属它，不是纲领文档），
   §13 只留判决行与指针。
3. **`water_sea_dist` 翻不翻默认值是一个独立决定**，由 R9 数据定。翻的代价
   已逐条列在 §13.2 末尾的表里（会翻转 `biology.md:156` 的核心不等号）。

## 非目标

- **sweep 跑完之前不许改 `underworld/`、`measure_ecotype.py`、`sweep_r9.sh`**——
  两种失效机制都不报错。
- 不再动果层的任何参数（加厚、等量再分配、空间分隔——三条都关闭了）。
- 不动 `in_dim`（作废全部演化脑）。
- 不为让实验「成立」而放宽 `scripts/golden.json` 的 band。
- **渴死若再次逐种子破半数，按 §12.5 收口，不降 `grass_shade` 剂量重试**——
  剂量重试是 §12 C 阶段走过且判负的路。

## 纪律（下一轮仍然适用）

- **空间分隔类的想法先算地形再跑种群**（`MEMORY.md [LEARN:method]`）。本轮照做了，
  地形判据在 63 个 run 之前就给出了工作点。
- 统计一律走 `scripts/exp_stats.py`，改了它必须重跑
  `explorations/20260803-partition/verify_exp_stats.py`。
- 判决派 `result-analyst`，主控不自己读几十份日志。
- 长输出一律重定向到文件（套件是**主机 CPU 编译**瓶颈）。
- 提交身份 `Asada-Sinon <weibinkong.research@gmail.com>`（已设 repo-local）；
  push 用 `GIT_SSH_COMMAND="ssh -o BatchMode=yes" git push`。
