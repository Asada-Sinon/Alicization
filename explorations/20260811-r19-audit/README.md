# R19 判决(feasibility §25, commit 3ee4942)的独立复核

- `analyze.py` — 复算四臂 Δ mean gene、三个臂间对比、mf_above、C 臂逐 run 符号、
  C/A 臂按世代对齐的时间曲线(检验「偏差随时间累积」)。读 `outputs/20260807-r19/`。
- `neutral_gen.py` — 按 §22.4b 同规则(WF 重抽 + N(0,0.02) 突变、初始 N(0,0.4))重算
  **实测代数(153 代/40 代)** 的中性零分布分位——判决用的 0.176 是 669 代的门槛。
- `mechanism_sim.py` — 复刻 d3eda56 版 `_assortative_mate` 的排序-配对,量化 C 臂
  (随机轴)下高 w 个体的配对偏置:未中心化归一的均值偏移 vs 判决猜想的「形状不对齐」。

补:`mechanism_burst.py` — 成批繁殖(k=86/43)变体,检验 k 结构敏感性。
全部 stdout 在 `output/*.out`。结论进复核报告(会话内),要点:
「累积」反向(C@gen40=+0.218, 11/12, p=0.0049)、153 代中性 95 分位=0.131 非 0.176、
机制模拟只复现 +0.02(差一个量级)、mf_above 臂间全平(A−C p=0.38)。
