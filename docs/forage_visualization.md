# 让觅食生态型看得见（wire v10 + 点着色器，2026-08-04）

`docs/multispecies_feasibility.md` §17/§18 判决：草↔果觅食生态型的分裂**是真的**
——4 臂 × 4 个 `Ne` = 16 格全部在 p<0.005 上拒绝中性零假设。
**但屏幕上完全看不见它**：颜色只承载 `diet`，一个已经裂成两个食草生态型的世界
和一个没裂的世界长得一模一样。这一节记录把它画出来的改动与验收。

## 1. 改了什么

| 处 | 改动 | 为什么是这个形状 |
| --- | --- | --- |
| `server/protocol.py` | per-agent record 追加第 10 个 f32 = `forage_pref`（v9→v10，36→40 字节） | **header 不动、任何现有 per-agent 偏移不动** ⇒ 客户端只需改一个 `STRIDE` 常量 |
| `server/app.py` | 传 `forage_pref_of(s.genome, cfg)` | 纯读基因的函数，与既有的 `size_of` 同路 |
| `web/main.js` | `STRIDE` 9→10 | 全仓唯一真源；`planes` 偏移与 `agents` 视图都由它导出 |
| `web/render.js` | 新 `a_forage`，食草者颜色沿觅食轴调制 | 见 §2 |
| `scripts/check.py` | 新增 `check_agent_stride`（5 项断言） | 见 §3 |
| `scripts/run_live.py` | 新增 `--speed` | 见 §4 |

## 2. 着色的两个非显然决定

**[对应] 中性个体必须保持原色。** 调制写成
`c = mix(c, spec * b, w)` 且 **`w` 在 `pref = 0.5` 处恰为 0**。
于是 `web/render.js` 里那句 `vec3 herb = vec3(0.62, 0.32, 0.92);`
——`scripts/check.py:check_species_colours` 钉住的那个字面量——
**仍然是「一个没有专精的食草者」的颜色**，而不是变成一个谁都不是的颜色。
（措辞要准：亮度、armor、venom 本来就已经把最终像素挪离 `#9e52eb`，
所以约束是**字面量不变**，不是**像素相等**。）

**[本世界实测] 斜坡的定标点 0.22 是量出来的，不是挑的。**
第一版写 `w = min(|f| / 0.5, 1) * 0.8`，结果**果专精染上了琥珀，草专精是一片说不清的
灰白**。原因在数据里：`R38n` 后半程的真实分布是

```
下簇质心 0.151（质量 0.450）   上簇质心 0.720（质量 0.498）
中间 [0.35, 0.60) 几乎全空：0.00 / 0.00 / 0.01 / 0.21 %
```

上簇离中性只有 **0.22**，在 0.5 的斜坡上只拿到 0.35 的权重，**压不过紫色底里 0.92 的蓝**。
改成 `min(|f| / 0.22, 1) * 0.85` 之后草那一侧才出现。
**教训与 `MEMORY.md` 里那三条同型：读数（这里是调色板）必须对着被测对象的真实分布定标，
不能对着值域的中点定标。**

**[对应] 属性槽维持在 8 个。** 这个 program 原本正好 8 个顶点属性，
而 **8 就是 WebGL 1.0 保证的 `MAX_VERTEX_ATTRIBS` 下限**。加 `a_forage` 会变 9——
实机与 swiftshader 都给 16，会过，但那是在赌规格。
把 wire 里相邻的 `diet` 与 `energy` 并成一个 `vec2 a_de`，腾出的槽给 `a_forage`，
总数仍是 8。

## 3. 补上的契约：per-agent record 的宽度此前完全没人管

`check_wire_protocol` 查的全是 header。**per-agent 那一段一直是裸的。**
它坏掉的表现是 `protocol.encode` 每个 agent 写 K 个 float 而 `main.js` 按 K−1 读，
于是**从第二个 agent 起每一个字段都错位**——画面照样画出来，颜色/大小/位置全是别人的。

`check_agent_stride` 查三处自洽：`protocol.py` 的 `np.empty((n, K))`、
`main.js` 的 `STRIDE = K`、`render.js` 每个 `vertexAttribPointer` 的最大 offset < K*4。
外加一条「`encode()` 必须恰好填满 0..K−1 每一列」——少填一列同样静默
（那一列会是 `np.empty` 的未初始化内存）。

**做过反向验证**：注入 `STRIDE = 10` 而 protocol 仍写 9，检查立刻 FAIL 并报出
「protocol.py writes 9, main.js reads 10」。**一个从不失败的检查器是没用的。**

## 4. `--speed`：headless 验证没有手

`scripts/run_live.py --speed N` 通过环境变量设 `Sim.speed` 的初值，与 `--set` 同路。
装它的理由是 §5 的验收：dashboard 的滑块只能用鼠标拖，而默认 4 步/帧下
`R38n` 需要**几分钟**墙钟时间才演化出可看的东西。
`docs/conventions.md` §10 要的是**用眼睛看**，不是要人有耐心。

## 5. 验收（`docs/conventions.md` §10）

截图用 headless chrome + swiftshader
（`--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader`，1600×1000）。
**截图不进仓库**（`CLAUDE.md`），所以判据写成可复算的数字。
⚠️ 端口用 **8137**，不是默认的 8000——那台机器上 8000 被别的服务占着。

**闸一：正确性（不依赖演化）。** 默认世界，改动前后各一张。

- 变化像素 **10320（0.645%）**，**右侧 UI 面板内变化 0** ⇒ 改动只碰了 agent 着色；
- 变化像素的平均 RGB 从 `(89,50,133)` 到 `(88,58,121)`，
  「绿>红」的比例 **0.000 → 0.011**、「红>蓝」**0.000 → 0.008**。

方向与量级都对：创始者的 `genome_init_scale = 0.4` 让 `pref` 只散布在约 0.35–0.65，
权重上限 0.24，所以**只该有小幅染色**。
**这一闸是可证伪的**：偏移写错时 `v_forage` 会读到 `venom` 或记录外的内存，
颜色会大幅乱跳而不是小幅位移。

**闸二：现象（依赖演化）。** `R38n` 覆盖 + `--speed 2000`，跑 300 秒墙钟后截图。
按主色道给世界画布内足够亮且饱和的像素分类：

| 截图 | 琥珀（果） | 绿（草） | 紫（中性） |
| --- | --- | --- | --- |
| 默认世界（`forage_tradeoff=0`，中性） | 0.029 | **0.000** | 0.971 |
| `R38n`，斜坡定标前 | 0.116 | **0.000** | 0.884 |
| **`R38n`，斜坡定标后** | **0.257** | **0.060** | **0.684** |

（默认世界那 2.9% 的「琥珀」是**肉食者**，不是果专精——肉食红本来就 R>B。）

**[本世界实测] 肉眼看到、而统计此前只能间接测到的一件事**：
**果专精个体在空间上是成团聚集的，草专精是弥散的。** R9（§10）测到过
「结构化草层造出空间生态型关联」（12/12、p 触地板），但那是一个相关系数；
这是第一次能直接看见那个团块。

## 6. 没做的（有意）

- **面板里不画 `split_score`。** `explorations/20260804-readouts/split_score.py` 的
  docstring 记着它第一版**在六个臂上全读 ~0.19、含三个明显单峰的中性臂，毫无分辨力**，
  而 §16.4 记着它在 R13 判决落档时**自己还是坏的**（一个「中间六个箱一个人都没有」的
  教科书双峰被读成 0.0000）。正确版依赖三个非平凡细节。
  **在 JS 里重写一份没有测试的副本，产出的正是看起来合理的错数字。**
  真要加面板，只画直方图 + `low_mass`（一行、无失败模式，而且它才是 §18 判据的分量）。
- **直方图面板本身也没做**：着色一旦沿觅食轴分开，双簇在画面上已经直接可见，
  面板是第二个变更原因，该单独一个 commit。

## 7. 不受影响的

- **不动 `in_dim` / `genome_size` / `trait_dim`** ⇒ **不作废任何已演化种群**；
- **不碰任何生态参数** ⇒ `scripts/golden.json` 十项不动，**不需要 `--bless`**
  （`check.py --full` 42 项已复核）；
- `forage_pref_of` 是纯函数读既有基因，每帧一次 device→host，与 `size_of` 同量级。
