# 任务队列与文档索引

新开一个对话时读这一份就能接上。**这里只放"下一步做什么"和"去哪找论证"，
不承载论证本身**——每条结论都指向它所在的专题文档。

## 文档地图

| 文档 | 内容 | 什么时候读它 |
| --- | --- | --- |
| `docs/conventions.md` | **`CLAUDE.md` 里每条约定背后的论证**：PREALLOCATE 的显存账、commit 拆分的 14 文件教训、记忆不可遗传那次 overclaim 的诚实读法、统计地板的算术、空间零假设、生态调参已被否掉的尝试、golden 带宽的实测定标 | 想改 `CLAUDE.md` 里某条规则、或觉得某条规则没道理时 |
| `docs/biology.md` | 真实世界的生态学与行为学文献基础：水点效应、可食生物量、预成水、空间记忆、Weismann barrier、Type III 存活曲线、亲代照料 | 要论证"自然界如此，所以设计该如此"时 |
| `docs/mortality.md` | 死因分解与竞争性风险删失。**其他所有实验文档的分母** | 提出任何新性状之前，先过这一关 |
| `docs/experiments.md` | 失败实验档案：果实层、被动踩踏、拆反混合装置、踩踏正反馈 | 想到一个"显然该 work"的机制时，先查这里有没有被否过 |
| `docs/trait_evolution.md` | 性状演化与开放式演化：可演化量清单、演化能力的演化、Bedau 天花板、工具使用判决、体型基因证伪 | 讨论"能演化出什么"时 |
| `docs/three_d.md` | 三维化可行性：渲染 / 分层 / 真 3D 三条路线的代价与推荐。§4.4 用三个独立原型实测了"3D 遭遇率稀释"这条推导——**证实**，不是证伪：测出的比值落在推导公式的 0.56–1.47 倍以内。真 3D 唯一"几何账能对上、不需要额外重调"的候选是 `L_z≈16`（配约 1.7–2.0× 密度），代价是长宽比 32:1 的极扁世界；深一点的 `L_z`（如 64）需要生态学重调，不是不可行 | 讨论 3D 时 |
| `docs/carnivore_riparian.md` | 捕食者滞留河道的机制诊断与生态学重调方案 | 讨论空间分布失衡时 |
| `docs/rebalance.md` | **"减慢植物生长换捕食者离水"这条用户假设的实测判决——三目标同时成立的区间不存在，该杠杆只碰资源总量、碰不到离水行为，还会灭捕食者** | 想调植物生长/种群量级/能量损耗之前 |
| `docs/landscape_of_fear.md` | 恐惧地景：让"危险"成为可学习的空间信息，这才是让捕食者离水的正路。头号方案（恐惧场折入 `pred` 通道）零 `in_dim` 改动、不作废种群 | 真要解决捕食者离水时 |
| `docs/trait_roadmap.md` | 性状/形态演化的分阶段落地路线图。**代价货币原则**（代价记在水账还是能账决定 A/B 命运）；`vision_radius` 可遗传是头号目标（既是 A 类又能缓解幼体渴死瓶颈）；`mutation_sigma` 可遗传的逐行实现规格 | 真要动手加可演化性状时 |
| `docs/mutation_sigma_heritable.md` | **`mutation_sigma` 可遗传（"演化能力的演化"）6 种子实测——落"平凡"类负结果。** 静态地形 Ne~2000 下把突变率变基因对选择基本不响应（A p=0.56、B1 无选择设定地板、B2 分裂是基因位搭连非选择、B3 前提被水修复消掉）。修饰基因座机制存在可跑但二阶效应测不出，要看需换尺度/更持久选择压 | 讨论"生物进化的能力"/演化可演化性、或想重试突变率演化时 |
| `docs/vision_radius_heritable.md` | **`vision_radius` 可遗传的完整落地 + 6 种子实测 + 结构性负结果。** 路线图头号 A 类主攻方向被证伪：感知格天花板只允许视野增长 0.33 单位，而水修复把食草—水缺口推到 45.5 单位（population 被赶往内陆），性状没有可用空间。**路线图 §1.4"填 4.7 缺口"与 §7.2"封顶在感知格以下"两条内部矛盾。** 要救活它必须改几何（大 `sense_grid` 格 + 高 `k_neighbors`，或把水通道从邻居块天花板解耦），不是调基因 | 讨论 vision/感知性状、或想动 `sense_grid`/`k_neighbors` 时 |
| `docs/trait_stage0_analysis.md` | **Stage-0 三项零代码分析的结果（新低瓶颈 main 上重测）**：①G 矩阵——三对性状全正相关，成"食肉—大体型—高投资"综合征，**推翻**旧猜测（食肉携带的是更大而非更小体型）；②`invest_frac` 漂移——`mean_invest=0.384` 稳低于中性 0.5，**支持**年龄别死亡率理论，瓶颈 83%→55% 后方向不变、幅度按预期从旧 0.356 回升；③peer 跟随——**证否**：幼体比随机置换**更远**离成体，是隔离不是跟随 | 要给 Stage-1 性状实验打判据底、或问"peer 通道到底在不在工作"时 |
| `docs/attack_range_redqueen.md` | **本项目第一个可测量的红皇后动态：可遗传 `attack_range` vs 对称猎物逃逸，6 配对种子实测。** 双侧成立且干净——逃逸只在功能开启时演化（1.91 vs 单侧中性 0.50，p=0.031）、滞后攻击约 3500 步、把有效攻击距离压回 6.28（≈6.0 起点），捕食者占比减半（13.7% vs 25%，p=0.031）。**但路线图 §7.3"双侧方差更高"被证伪**：两臂攻击距离都收敛到能量税决定的同一均衡（约 8.2）、方差无差异，红皇后是达到协同均衡的瞬态而非失控军备竞赛。代价一律走能量账本（`attack_cost`/`escape_cost`），未碰 `carnivore_riparian.md` 那批捕食参数 | 讨论协同演化/军备竞赛/红皇后、或想动 attack/escape 基因与其代价时 |
| `docs/trait_defense_catalog.md` | **可见形态『防御』性状候选目录（真实文献支撑）。** 8 类候选（护甲/尖刺/诱导型/警戒色+毒/隐蔽/集群稀释/体型/速度）逐条给真实生物学依据（stickleback Eda、Daphnia neckteeth、newt-snake TTX 等，附 DOI）+ 本世界 predation 落地锚点 + A/B/可见性/runaway 判定。头 3 推荐：护甲+尖刺+警戒色。dilution 不推荐（群属性非形态）。**护甲的 A/B 归类与 feasibility 篇有意留张力**（真实代价是生长账=B 类，本世界改能量账=A 类） | 讨论"能加哪些看得见的防御性状"、找防御设计的真实依据时 |
| `docs/trait_defense_landing.md` | **armor（厚皮）+ spike（尖刺）首个落地 + 可视化实录。** trait_dim 5→7、predation 减伤/反伤 + metabolize 能量税、gene=0 中性、四类单元测试全绿、golden 重 bless（1494→1555 等）。wire v8 每 agent 追加 size/armor/spike、shader 画厚描边+放射尖刺+点大小、inspector 加厚皮/尖刺行，headless chromium + 独立 shader 测试双重截图验证。**§6 演化验证判决（6 配对种子）：armor 真演化（herb_armor 0.170 vs 漂变 0.067，6/6，p=0.031，本项目首个演化出来的可见形态防御）；spike 证伪（≈0，收益太弱非成本压 0）；P2 无信号；P3 部分证伪（carn_armor 被基因流抬高）** | 想知道 armor/spike 怎么实现的、可视化怎么接的、演化验证结果、或要接着救 spike/补种子时 |
| `docs/trait_addition_feasibility.md` | **加入防御性状做演化的可行性判决 + 首个落地规格。** 工程（最小钩子集、trait_dim 5→6、golden 必重 bless、可见性管线三处 + 真尖刺几何超出 `gl.POINTS` 的天花板、测试契约）+ 演化（代价货币、防御是成体向 A 类不被瓶颈删失、红皇后 escape 作存在性证明、捕食是活的动态选择压不重蹈通勤证伪、diet 门控防 runaway）。**判决：建议做，首推厚皮/减伤 armor**（escape 结构孪生、个体级信号最干净、避开尖刺几何死胡同）+ 逐文件规格 + 3 条可证伪预测 + 6 种子测试设计 | 真要动手加防御性状、或问"这个能不能做、先做哪个"时 |
| `docs/water_fix_decision.md` | **三候选水修复对比与选型判决（已落地）。** buffer/provisioning/retune 对着用户三条判据选型，只有 retune 全过。**已实现为 main 默认值**（`543483a`）：base/move_water_cost 减半 + carn_cost 0.15。实测 thirst 83%→55%、捕食者离水 11.4→25.3、carn_frac 稳在 22% | 想知道水系统为什么这么调、或要再动水参数时（先读它的"窄工作点"警告） |
| `docs/web_cleanup_audit.md` | **`web/` 前端清理审计（行为/视觉零变化）。** 无死代码；落地两类性能点（`render.js` 缓存 GL location + 采样器只绑一次、`main.js` 缓存每帧 DOM 元素）与两处过期注释（`--plant`、`C` 颜色块）。cross-file 契约全程未碰，`--contracts` 21 项全过，render 改动按 §10 截图验过 | 要动 `web/` 前端、或想知道哪些清理已做过时 |
| `docs/herbivore_overpopulation.md` | **"食草满山遍野"诊断+杠杆盘点+实验。** 判决：是绝对密度问题（66× 现实、水限），非比例问题（比例上捕食者才过多）。用户杠杆多被证伪：扩世界不降密度（强度量）、砍 plant_max 证伪且饿死捕食者。**实验 A（repro_threshold↑）负结果**——不降 pop（水限补偿）、只让种群变老、还压捕食者。**L6 密度制约繁殖**（拥挤格抬繁殖门槛，默认关）见 §6 实验。唯一真降密度的是水经济（高风险）或承认压缩微缩世界 | 讨论食草太多/密度/生育节流/密度制约时 |
| `docs/multispecies_ecology.md` | **多物种/营养级/种间关系的真实生物学依据 + ALife 先例。** 顶级捕食者使系统更脆弱（Estes/Prugh）；稳定 2 物种的第三物种应是**弱杂食者**（McCann-Hastings）而非 apex；ALife 里寄生可涌现（Tierra/Avida/PolyWorld），**互利比寄生更难涌现**；跨种合作用最小互惠脚手架（Quinn 模式）而非硬编码行为。13 篇真实引用附 DOI | 讨论加物种/营养级/种间合作的生物学依据时 |
| `docs/multispecies_feasibility.md` | **多物种工程可行性 + 三条通路 6 种子判决（2026-07-25）。** 头号约束：**diet 轴中段是生态死区**。加物种代价表（trait_dim 便宜/in_dim 作废全脑/新 [n_cells] 场中等）。**三条通路 6 配对种子判完；腐食 v2 扩到 18 配对翻正带代价、另两条维持默认关**：**§7 腐食第二版**（接 food retina 主动觅食、不动 in_dim）=**18 配对翻正**（carn_frac ON>OFF late_carn 16/18 p=0.0034、末态 p=0.0268——6 配对 p=0.156 是功效不足掩盖的真效应；但 **population −5.6% p=0.0237 是坐实的种群代价**，死因 thirst↓/predation↑ 自洽；抗灭绝弱阳 min 0.066→0.080）。**跨地形去伪重复（4 张新图 wn×base，run_id 20260725-carrion-terrain）：正效应方向 4/5 河系一致但单图无一显著、wn2_b0.40（carn 最饱和地形）干净反向、代价也不普适——偏正但未稳健到该默认开，维持默认关、留作可选开关**；**§8 共享报警场 alarm**（用户 #5 Quinn 脚手架）=**NULL**（death_predation_frac 4/6、p=0.56、机制点火但信息冗余）；**§9 资源分割 forage_pref**（trait_dim 7→8、golden 重 bless）=**证否**（方差反向塌缩 32.6%、种群整体倒向草，根因果层仅草层 0.07% 无生态位可专精）。三者均 bit-exact/默认关（forage 例外付了永久 trait_dim 代价、证否但留档）。**§10–§18 是 2026-08-03/04 的 R9–R15 判决链**，读的时候注意 **§14（R13）已撤回**、抬头有撤回notice；**最新结论在 §17（R13 轨迹再分析）与 §18（Stage 2 的 96 run）**：**在 R9→R14 九项干预累积出来的世界里**（`forage_tradeoff=1.0` 等，⚠️ **不是默认世界**——默认那个主开关是 0.0），「中间被掏空的果专精少数簇」在中性漂变+复发突变下造不出来（四臂全 p<0.005，含**有捕食者的基线臂**），关捕食通道给出 `split_score` **0.4501** 的近 50/50 双簇。**§18.5 明令不能说默认世界里有第二个生态位**。**§19 = R16 长时程**（148 代上仍非中性，但**没回答自己标题的「200+ 代」**）；**§20 = R17 生殖隔离**（同型交配把两簇的全基因组分化抬高 **+47.8%**，而那条「反向」的 `split_score` 是**读数在分裂变尖时失效**；**隔离带 −5.6% 的种群代价**；⚠️ 只能读成「若隔离存在则 X」，**不能读成「物种形成涌现了」**）；**§22 = R18 长时程**（24 run × 350k 步）：**双簇形成于头 63 代、此后 312 代未并合**（四个 `Ne` 全 p<0.005 拒绝中性；⚠️ 初版写「全程」是错的，gen 63 前有 275 个检查点无双峰、1248 个谷未见底），**但不再是 50/50**——多数侧 0.5070→**0.5820**（12/12、p 触地板、比值 +6.63）而**少数侧缩了 13.4%**（0.4803→0.4158），且那个 50/50 是系统**穿过**的点不是起点（gen 31 时仅 0.3092）；**漂移在减速但 375 代内没停**（末段增量 9/12、p=0.00684、**比值仅 +1.05**）。**§22 的四处实质错误由 2026-08-06 的 fable5 独立复核抓到并改正，改动全是变弱的方向。**⚠️ **`split_score` 与 `low_mass` 不是两条证据**——干净双峰上 `ss ≡ min(lm,1−lm)`（94.2% 精确），这条恒等式连带撤回了 §19 的 `Q4−Q3=+0.0691`（正确替换值是**帧比例口径**的 +0.0015、5/12、p=0.850，**不是缩水是彻底没了**）；**§24 = R18-B**（24 run × 800k 步、**669 代**）：**漂移在 gen ~400 之后停下**——末段增量 +0.00193、95%CI [+0.00017, +0.00344] ⊂ ±δ(0.00549) ⇒ 等价性检验判「停了」，但 ⚠️ **CI 不含 0，「停了」= 「小于分辨率」而非「零」**；少数簇停在比峰值低 **15.5%** 处；双簇形成于头 13 代、到 669 代未并合；四个 `Ne` 全部拒绝中性；R18 的几何外推**高估 0.38%** | 真要动手加物种/营养级/种间机制时 |
| `docs/forage_visualization.md` | **把已经证实存在的草↔果觅食生态型画出来**（wire v10 + 点着色器 + `check_agent_stride` + `run_live.py --speed`）。两条非显然决定：中性个体必须保持原色（所以 `check_species_colours` 钉的字面量仍是「没有专精的食草者」的颜色）；**着色斜坡的定标点 0.22 是量出来的**——按值域中点定标会让草侧压不过紫底的蓝，实测「绿」恒为 0.000，而 R38n 的真实簇位是 0.151 / 0.720。§10 两闸验收全写成可复算的数字（截图不进仓库）：改动只碰 agent 像素（UI 面板内变化 0），R38n 下 琥珀0.257/绿0.060 vs 默认世界 0.056/0.001；并量到**果专精比草专精成团 9.97 倍** | 要动 wire format / 着色器 / dashboard，或想知道怎么做 headless 截图验收时 |
| `docs/run_to_run_variance.md` | **⚠️ 影响所有已有与未来实验的一把尺子：`--seed` 在 20000 步上控制不住这个世界。** 18 个默认配置 run（3 重复 × 6 种子）方差分解——`late_carn`/`death_thirst_frac`/`total_flux` 的**创始者方差分量为 0**，`carnivore_frac` ICC 仅 0.130、`population` 0.259；同一个种子能跑出 `carnivore_frac` 0.060–0.2505。后果：配对差的噪声是 `√2×同种子SD`（`carn_frac` 0.0625），而 `conventions.md §5` 用的跨种子 SD ±0.012 **低估 5.2 倍、所需样本量低估约 27 倍**；ICC≈0 的指标**配对完全无用**。确定性 XLA 算子能修但慢一个数量级以上（实测 57 分钟未跑完 vs 4.5 分钟），不可用。**已发表 p 值没错**（算的是实测配对差），错的是功效规划与护栏容差。§6 给每条旧结论标了「效应量/噪声」比值（红皇后 2.4、armor 1.19 站得住；恐惧场 0.32 降级待重测）。**§7 新协议 = 12 种子 × 2 重复，已于 2026-08-03 全部落地**（§7.4 有逐文件记录；不是 k=3——§7.0 有算术：`s` 与 `r` 对精度贡献相同，但 p 下限与伪重复只由种子数决定，`s=6` 时最小双侧 p 永远是 0.031）。**实现落在 `scripts/exp_stats.py`（三条分析口径的唯一实现，写新 `analyze.py` 时 import 它，别重推）**，回归验收 `explorations/20260803-partition/verify_exp_stats.py` 用它复算 P4 与 §9.11 已发表数字逐位一致 | 设计任何实验之前；解释「为什么又是 p=0.031」时；重估任何旧结论时 |
| `docs/multispecies_program.md` | **多物种生态箱的研究纲领 + 七道闸自动循环 + 回合台账。** 纲领把多物种三次失败（diet 中段死区 / forage_pref 证否 / 腐食稀薄）归到同一根因：**这个世界只有一个饭碗（河岸的草），只坐得下一个食草生态型**，所以顺序是先造生态位再谈物种。**R1 判据自身被证伪**（现存量量的是「剩多少」不是「供多少」，把生态位低估约一个数量级，产出是 `Metrics` 的三个流量观测量 `graze_gain`/`fruit_gain`/`frugivory_frac`）；**R1b 不通过**（果层份额能做到 20%+、6/6 p=0.031，但**渴死护栏破** 2–3/6，且顺带更正了「种群往林冠移」这句——相对零模型量级可忽略）；**R1c 不通过且诊断被推翻**（真因是承载力不是水口径，ρ=+0.90）；**§10 跨回合命题：水限世界里加食物只会变成更多身体**；**R3 判为无法判定**（判据容差 ≈ 1 个噪声 SD）；**R2 + P3 + P4 分化线收口**——给了 28% 的真饭碗、把权衡强度开到最大，性状方差反而比中性漂变**窄 38.3%**，五点剂量曲线无分化窗口。**真因是中间型最优**：`forest = elev_band * water_prox` 让果层长在所有个体共居的河岸林带。**§12（把果层挪出共居带）已走完并判负**：A ✅（§12.3.A'）、B ✅ 旋钮已落地（§12.3.B）、**C 判「没有可用工作点」，D/E 不跑**。**决定性证据不在种群上，在地形上**：`fertility = grass_base + forest_bonus·forest` 且 `grass_base=0.65` ⇒ 草层动态范围**结构性封顶 1.538×**、铺满整张地图，把果层推到最远它所在格子的草量仍是陆地均值的 0.966 倍——**没有一处「有果实、没有草」**。**三条资源轴全部关闭**，负结果进 `experiments.md §5.6`。**A 的发现推翻了我自己的预注册猜想**：默认世界里果层与种群的重叠是**纯几何**的（`sel_ratio_water` 1.009，扣掉水距后落回零模型），但在判决实际发生的**厚果层世界里是行为性的且很强**（**3.712**，12/12，p=0.00049，效应/噪声 8.73）——把果层变值钱，种群就往上挪 +268%。所以 **C/D 的基线固定为 `niche` 臂**（`base` 的「可下降空间 ÷ MDE」只有 0.52，手术做对了也测不出来）。**B 阶段顺带拦下一个会毁掉归因的混杂**：朝干旱带 blend 会把果层总承载抬 +91%，已加容量归一化做成等量再分配。另有三条方法学教训：频率依赖下 `q≈f`、形状检验绝对 p 值在演化种群上无效、`--seed` 控制不住 20000 步 | 要接着做多物种/第二生态位、或想知道这套自动循环怎么跑的 |
| `docs/water_system.md` | **水系统经济学审计——本轮调研的收敛点。** 用户"水消耗太快"假设的判决（成体不成立、幼体方向对但机制是"出生只给 21% 满箱"）；5 臂消融证明**水是那个总闸门**——调水经济第一次真的把捕食者赶离水（`carn_water_dist` 10.8→34.3）且大降渴死，但每个有效杠杆都让种群涨 27-72%、捕食者占比升到 33-41% | 讨论幼体渴死瓶颈、水参数、或"改一处能不能松动多条线"时 |
| `docs/test_coverage_audit.md` | **测试覆盖审计 + 13 个新单元测试。** 补上六处沉默缺口：红皇后消融开关 no-op 等价性、有效射程=attack−escape 边界、捕食最近猎物+diet_delta 阈值、spatial 邻居索引溢出/死者沉默丢弃、生育 permutation-scatter 能量守恒、记忆分区边界。未发现被测代码 bug | 想加测试、动 `predation`/`reproduce`/`spatial`/消融开关、或问"这条不变量有没有守护"时 |
| `docs/optimization_audit.md` | **JAX kernel 性能审计（行为不变）。** 画像：kernel 是**显存带宽受限**（~597 GB/s，59% 峰值）而非算力受限，`genome`[16384,1383]=90MiB 是搬运大头，热点是 `reproduce`（0.99ms）非 `brain.forward`。落地 1 项：`sensors.sense` 扇区聚合 Python 循环→单次 scatter-max（-30% 组件、逐位一致、scatter-max 幂等无新不确定性）。多数"缓存重复计算"候选查证为 XLA CSE 已免费处理，不改 | 想优化 kernel 性能、或问"某处重复计算要不要手动缓存"时 |
| `docs/cleanup_audit.md` | **AI 冗余代码审计与清理（行为不变）。** 死代码核实（`prey_field` 确已删、无新未用 import）；`graze`/`eat_fruit` 食草锥度去重为 `_herbivory`；修三处过期注释（`memory.py` 那句 overclaim、`sensors.sense` 漏 mem/peer、`spatial` 半宽魔数）；`forage_pool` 更大去重让给性能审计未动。golden held + 全 pytest 过 | 想清理代码、或问"某段重复/死码为什么还在"时 |
| `docs/landscape_of_fear.md` §6 | **恐惧场（设计二）已落地 + 6 种子实测 + 默认开启。** `fear:[n_cells]` 场（仿 `trample`），食肉者散射沉积、折入 `pred` 通道，零 `in_dim` 改动。实测：弱设置（rate0.01）零结果（单种子探针是噪声，再证"绝不单种子调生态"）；**强设置 rate0.05/scale3.0 部分阳性——捕食者游走 carn_speed 1.5→2.4（6/6，p=0.031）、占比 12→10%（5/6）、不恶化渴死（thirst_age +4.9）**，但"离开河道"只弱成立（cWD +1.4）：水是硬约束+捕食者驻水边，恐惧场给不了"搬离河岸"（缺昼夜通勤）。默认开启，`--set fear_rate=0` 消融。golden 已 rebless | 讨论捕食者离水/游走、恐惧场、或想调 `fear_*` 参数时 |
| `docs/day_night.md` | **昼夜系统（恐惧地景的下一步）。** Phase 1 机制已落地、**默认关**（`day_length=0` 编译期 no-op、逐位复现旧基线、无种群作废、golden 不动）：全局标量 `phase` 时钟驱动"暗→只缩 inter-agent 视野（伏击杠杆，不动找水/找食）+ 热→正午耗水↑"的组合节律，**刻意不给大脑显式时钟输入**（让内生时钟自己 entrain）。6 种子实测（相位分箱通勤 + 盯死渴死）待跑；Phase 2 加大脑容量仅在 Phase 1 通勤为 null 时触发 | 讨论昼夜/通勤/`day_length`、或想让捕食者真正搬离河岸时 |
| `docs/scale_and_density.md` | **世界比例尺 + 种群密度现实性校验。** 定标：1 世界单位≈1 m、全图≈0.26 km²（26 ha）、山脊峰≈12 m（爬坡能量学锚定），填上 `carnivore_riparian.md:286` 留的 [推断,未测] 空档。判决：食草密度约现实天花板 66×（区间 29–117）、生物量超 NPP 上限约一个数量级；食肉绝对密度约现实极值 3800×（全图不足一只狮领地的 1/19），= 总密度 66× × 营养级比例 30× 两个独立误差之积。**量纲不自洽**：空间参数说有蹄类、密度说鼠灾。给出 A–D 四条 [提案]（承认压缩/扩世界/压种群/只修比例），未擅改 | 讨论"种群数量对不对"、世界大小、密度、比例尺，或想把"漫山遍野"直觉量化时 |

## 硬约束（每次动手前确认）

- **跑任何 JAX 进程都加 `XLA_PYTHON_CLIENT_PREALLOCATE=false`**。默认预分配 75%
  显存会让第二个进程假 OOM，实测真实峰值只有 918 MiB，加了就能十几个并行。
- **统计协议**：**12 种子 × 每格 2 重复**（每臂 24 run，约 45 分钟/两臂）。
  报告每个种子的数字，不只报均值。配对 Wilcoxon + 效应量 + bootstrap 区间。
  **不做 Bonferroni，报告每一个算出来的 p 值。** 三条分析口径缺一不可：①先取格均值
  再对 12 个格均值做配对检验（`s×r` 个 run 不是独立样本）；②必报「效应量 ÷ √2·σ̂_W/√r」，
  σ̂_W 从本次实验自估，**比值 <1 一律标功效不足，哪怕 p 过线**；③护栏容差对着
  √2·σ̂_W 定。实现走 `scripts/exp_stats.py`，别每轮重推。
  为什么从 6 改成 12：n=6 配对符号秩的最小双侧 p 就是 0.031（顶到地板不是强证据），
  n=12 才降到 0.00049；且 `terrain.build` 不吃 RNG，只有加种子才多一次创始者抽样。
  见 `docs/run_to_run_variance.md` §7。
- **空间性结论需要跨地形种子**（`ridge_wavenumber`/`ridge_amplitude`/
  `ridge_base_y`），因为 `terrain.build` 不吃 RNG，所有 founder 种子共享同一张
  地图。只跨 founder 种子的空间结论只对"这张地图"成立，是伪重复。
- **消融用 `scripts/run_headless.py --set FIELD=VALUE`**（可重复传参），不要改
  `config.py`——那会让两个臂落在两棵不同的工作树上。覆盖项会写进 `--json` 行。
- **调研结论必须落进 `docs/`**，否则视为没做过。见 `CLAUDE.md` 同名一节。

## 队列

### 当前主线（2026-08-02 起，用户定盘）：局部多物种生态箱 —— `docs/multispecies_program.md`

用户给了长期目标「**局部的多物种生态箱演化 + 可视化结果**」并要求做成一个可反复执行的
循环。纲领与七道闸写在 `docs/multispecies_program.md`，**回合台账在它的 §3**，
backlog 在 §4（判决后重排）。下一个回合从 backlog 顶部取题，不要另起炉灶。

**八个回合已全部走完**（§5–§11）。合并结论：加食物、等量再分配、加大选择强度都造不出
第二个生态位——根因是 `terrain.py` 让果层长在所有个体共居的河岸林带，**中间型最优**。
两件已完成：①升级统计协议 `docs/run_to_run_variance.md §7`（实现落在 `scripts/exp_stats.py`）；
②把果层挪出共居带 `§12` —— **判负**，负结果进 `experiments.md §5.6`。

**R9–R14 七个回合已全部走完并判决**（`feasibility.md` §10–§16，纲领 §13–§18）。
**⚠️ R13 的「按现规格关闭」判决已于 2026-08-04 撤回（`feasibility.md` §16.4）——
理由不是结论错了，是证据作废。整条路线的状态是「回到未回答」，不是「判负」。**

- **R9**：结构化草层造出空间生态型关联（12/12、p 触地板）⇒ 但那是一条 **cline**。
- **§11（代数，本线最重要的一条）**：`_forage_pref_scale` 是**直线**权衡前沿 ⇒
  `d²W/ds² ≡ 0`，**分歧选择没有一阶项**。一次解释掉五条归档阴性。
- **R10**：曲率杠杆能注入 +0.087，**实测基线 `W''=−0.137`**，盖不过；补功效没有意义。
- **R11**：`eat_rate` 1.5→0.5 后中间带**可复现地被掏空**（12/12、p 触地板），
  且不是兑换率的产物。**但不等于两个物种。**
- **R13 ⚠️ 已撤回**：原判「少数簇是弱确定性淘汰下的暂态」建在**两个后来才修好的读数
  bug** 上（直方图 range 截断 `b06319e`、`split_score` 边界补零 `803dc60`，后者**在判决
  之后**才修）。修好后：衰减 −0.1168 (p=0.0036) → **−0.0585 (p=0.063)**；
  「快于中性」p=0.0027 → **0.178**；「0/15 连续保住」→ **4/12**；
  `Ne·s` 1.22 → **0.40（落进有效中性区）**。**撤回后是 undecidable，不是判正。**
- **R14 Stage 1**：主判据的输入被截断（`R50` cp1 有 58% 个体在标尺外），本轮作废。
- **R14 Stage 2**（96 run，`diet_delta=1.5` 由构造关掉捕食）：**H0 成立**
  （果供给翻倍抬高果专精簇 +0.3006、12/12）、**H1/H2 undecidable**。
  H1 的正面信息：**排除了「资源比对 Δ 的影响 > ±0.0138」**。

**R21 完整判决（§30，2026-08-17）**：捕食压力的剂量曲线跑完九档。形式上判「有阈值」
（临界点 0.70–0.80），**但那个阈值是捕食者自己的存活阈值，不是食草者对压力的响应阈值**
——把捕食门槛抬近五倍，**没能测出捕食者密度的变化**（六档 0.1539–0.1822，
配对 CI [−0.0239, +0.0275]，只排除了大于约 ±17% 的变化，比值 0.04 ⇒ 功效不足）。
⇒ **`diet_delta` 是存活闸门不是压力旋钮，这个世界答不了「临界压力在哪」**。
干净的做法是外生钳制捕食者密度（`[提案，非结论]`，见 §30.6）。

**⇒ 预注册的两条路都没能关掉这条线，但本轮撞出一个纲领从没扫过的变量：**
**捕食通道压制果专精簇。** `R38n−R38p` = **+0.3519（12/12、p=0.00049、比值 +3.21）**，
与「果供给翻倍」同量级甚至更大；而 R13 那 3 个**自发**丢失捕食者的 run，
正好就是果专精簇长到 0.5 的那 3 个——**两条独立证据同指**。
⚠️ 它是多变量的（通量、种群、世代钟同时变），**现在是线索不是结论**。

**R15-A（2026-08-04 深夜，零 GPU）：分裂结构已与中性分开，路线不是「能不能走」了。**
判决 `feasibility.md` §17（R13 轨迹）与 §18（Stage 2 的 96 run）。

- **§17.3**：R13 的截断 bug **只污染了检查点**（`np.digitize`），**没有污染轨迹**
  （`jnp.clip`，逐帧核过 `sum(hist)==n_herb` 全等）。3 个 run 的检查点表写
  「簇消失且再没回来」，**同一批个体的全员普查是 0.405→0.517、单调在长**。
  机制：**簇成功的方式就是往窗外走，所以越成功越看不见**——
  读数的失效模式与它要测的现象**反相关**。
- **§17.9 + §18**：「中间被掏空的少数簇」**在中性漂变+复发突变下造不出来**。
  Stage 2 的 96 run（构造关捕食、修好的 20 箱读数）上**四个臂全部 p=0.0000**，
  **包括有捕食者的基线臂 `R38p`（10/24）** ⇒ **它不是捕食者缺失的产物**
  （⚠️ **`R38p` 不是默认世界**：它与 `Config()` 差 9 个参数，其中 `forage_tradeoff`
  默认是 **0.0** 即整个权衡的主开关默认关着。本纲领**没有**说默认世界里有第二个生态位）
  （占空比 0.373 vs 其余三臂 0.95–1.00）。
- **§18.3 饱和**：关捕食通道 **+0.6140**、翻倍果供给 **+0.6261**（都 12/12、p=0.00049），
  但两个一起做 **0/12、−0.05** ⇒ **各自足够、互相饱和**。
- **§18.4 它们做的不是同一件事**：关捕食者 ⇒ **平衡的两个生态型**
  （`split_score` **0.4501**，本纲领迄今最平衡）；翻倍果供给 ⇒ **整个分布搬到果实侧**
  （`low_mass` 0.61–0.75，`split_score` 只有 0.25–0.30）。**只看占空比看不出这个区别。**

⚠️ **仍不能说「两个物种」**：生殖隔离没测，`diet` 轴上它们仍是同一个食草者。
⚠️ **§18 不是预注册的**（判据 `retained()` 是既有的，应用不是）。

**R16 / R17 两轮已判完**（`feasibility.md` §19 / §20，2026-08-05）：

- **R16（48 run × 100k 步）**：**H1 达成**——**148 代**上双簇不是漂变+复发突变造得出来的
  （`R38n` 24/24 vs 零分布最大 5/4/2/0，四个 `Ne` 全 p<0.005）。**H2 undecidable**
  （占空比顶格 ⇒ 比值分母不存在；那个 12/12 是恒等式 `Δ≡1−首1/4`）。**H3 达成** +0.7205。
  ⚠️ **它没回答自己标题的「200+ 代」**：`R38n` 只到 134–165 代，
  而 `R38p` 的陡降恰在 gen 200–452。
  ⚠️ **不能说「两臂都拒绝中性」**——`R38p` 的拒绝由 4 个**处理失效**的 run 扛着，
  存活的 20 个只有 3/20、`Ne=340` 上 p=0.1095 不拒绝。
- **R17（48 run × 100k 步，生殖隔离）**：**H1 达成不打折**——同型交配把两簇的
  **全基因组分化抬高 +47.8%**（Δ+0.25506、10/12、p=0.00488、三种噪声口径
  1.44/1.96/4.94 全过闸，**超过在看到主臂之前就算死的 MDE 0.1766**）。
  那条「反向」的 `split_score` 是**读数在分裂变尖时失效**，容忍平台后差异整体消失；
  每个独立形状读数都说分裂**变尖了**。**隔离不是免费的**：种群代价 **−5.6%**。

**⇒ 一句话：若隔离存在，则两簇的全基因组分化高 47.8%。**
⚠️ **不是「物种形成涌现了」**——`mate_forage_weight` 是拧上去的常量，不是演化出来的；
**也不能说「两个物种」**（`diet` 轴上仍是同一个食草者）。

**R18 已判完**（`feasibility.md` **§22**，2026-08-05，24 run × 350k 步，
判据 `program.md` §21 + §21.4b）——**上面那条候选①已完成**：

- **[本世界实测] 双簇形成于头 63 代，此后 312 代未并合**（实测跨代数 375 [345, 431]，
  **不是 450**）：全量 32784 个检查点里 31261 个两峰都找得到且谷精确见底，
  失败的（275 个无双峰、1248 个谷未见底）**全部落在 gen 63 之前**；
  四个 `Ne` 全部 p<0.005 拒绝中性。**R16 标题里那个「200+ 代」本轮答了。**
  ⚠️ **不许写「全程」「每一个检查点」**，也**不许拿 `retained` 24/24 当证据**
  （§21.4b 缺口③ 自己禁的，且它只看多数侧）——两条都是 fable5 复核抓到的。
- **但它不再是 50/50**：果专精侧 0.5070 → **0.5820**（Δ+0.0750、12/12、
  p 触地板 0.00049、比值 **+6.63**）。**§21.3 预测的衰减符号被推翻**
  （预测降到 0.339，实测升到 0.582）。
- **漂移在减速但没停**：逐段增量 +0.1712→+0.0457→+0.0302→+0.0178→**+0.0100**
  （末段是首段的 1/17），而末段仍 **9/12、p=0.00684** 显著为正——
  ⚠️ **但比值只有 +1.05，刚过闸，涨幅已逼近分辨率下限**。
- ⚠️ **少数簇（草专精）在缩**：从峰值 0.4803 跌到 0.4158（**−13.4%**）。
  **只报多数侧上升会让结论听起来比实际乐观。**
  而且那个 50/50 是系统**穿过**的一个点，不是起点（gen 31 时只有 0.3092）。
- ⚠️ **三条禁令**：不许只写「撑住了」（丢掉漂移）、不许写「在衰减」（说反方向）、
  不许写「已达新平衡」（末段增量仍显著为正，那个渐近 0.5977 是外推）。
- ⚠️ **`split_score` 不是 `low_mass` 之外的第二条证据**：干净双峰上
  **`ss ≡ min(lm, 1−lm)`**（32784 个检查点上 94.2% 精确成立），
  Δ 层面独立自由度 ÷ 噪声 = 0.695 < 1。详见 **§21.4b**。
- **连带撤回**：§19 的 `split_score` Q4−Q3 从 +0.0691（+3.73）重算为
  **+0.0015、5/12、p=0.85010**（帧比例口径，即那张表自己的口径）——平台失效的假值，
  **不是缩水，是彻底没了**；§19 定性结论不变（`low_mass` 帧口径重算与已发表值一模一样）。

**R18-B 已判完**（`feasibility.md` **§24**，2026-08-06，24 run × 800k 步、15h45m）
——**R18 留下的那个问题答了**：

- **[本世界实测] 漂移在 gen ~400 之后停下。** 多数侧 `low_mass` 从 0.3194 涨到
  **0.5963**，逐段增量从 +0.1624 衰减到 +0.0019；末段增量 **+0.00193**、
  95%CI **[+0.00017, +0.00344]** 完全落在 ±δ(0.00549) 内 ⇒ 按 §23.4 的**等价性检验**
  判「**停了**」。
- ⚠️ **「停了」的准确含义是「残余 ≤ +0.0034/段」，不是「零」**——**CI 不含 0**，
  残余 +0.00193 是 24 run 分辨率（CI 半宽 0.00163）的 **1.18 倍、测得到**，
  只是小于**单 run** 噪声 δ=0.00549。按它外推走到固定还需 **12,757 代**。
  **不许写「小于本实验的分辨率」**（与「CI 不含 0」自相矛盾，复核抓到）。
- **少数簇同样停了，但停在比峰值低 15.5% 的位置**（0.4778 → 0.4037）。
- **双簇形成于头 13 代，到 669 代未并合**（唯一一次晚期扰动在 gen 116–117、
  跨 1.1 代，按 §23.4b 判为**暂态**——此后 2871 个检查点全部恢复）。
- **H4 四个 `Ne` 全部拒绝中性**（24/24 vs 零分布最大 9/5/4/2）。
  ⚠️ 零分布随时程变宽（R18 是 5/5/5/1）——**更长的时程会让这条判据变松**。
- **R18 的几何外推与实测不矛盾**：同口径预测 **0.5959**，落在实测 CI 内。
  ⚠️ 初版的「高估 0.38%、落在 CI 之外」**已撤回**——拿模型终点点值比窗口加权均值
  （重心 gen 587 而非 669），**口径错配**。
- ⚠️ **实测跨代数 669 [594, 751]，不是 700**——世代钟比 §23.2 的外推还慢。

**R19 已判完**（`feasibility.md` **§25**，2026-08-11，96 run × 100k 步 ≈ 150 代）
——**判 undecidable，不是阴性也不是阳性**：

- 主臂 Δ`mean gene` **+0.4082**，看似阳性；**但 C 臂（排序轴 = 每步重抽的随机数、
  设计上的零点）也涨了 +0.1772** ⇒ A−C 的 CI **含 0**（p=0.129），三个对比全不显著。
- **预注册读数 `mf_above` 臂间精确为零**（A−C p=0.380、A−B p=1.000）
  ⇒ 多涨的部分**没有过阈形态**，加强 undecidable。
- **污染在头 40–60 代就全部到位**（gen40 时 +0.216、11/12、p=0.0049），此后回落
  ——判决初版写「随时间累积」**已撤回，方向说反了**。
- ⚠️ **跑前那个「三轮修复已把偏差修掉」的验收是 4-run 噪声上的过度解读**
  （SD 0.373，对 +0.22 无功效）——**假的安全感让正式实验白跑了 8.5 小时**。
- **换机制的理由**：剩余偏置在最忠实的中性简化里无法复现（模拟给 +0.02~0.04，
  实测 +0.177）⇒ 不是键的几何问题，**继续修键不会收敛**。
  ⚠️ 但空间配对**同样自带**「改变亲缘结构 ⇒ 与选择面交互」的二阶通路，
  **C 类对照臂必须保留**。

**下一步：换一种隔离机制（用户 2026-08-11 拍板），要烧卡**：
让 **`mate_forage_weight` 本身可遗传**（加基因位）回答「隔离能不能自发演化」
（**R19 已预注册**，`program.md` §22，且 §22.2 自己写着「这很可能阴性」）——
⚠️ 会动 `trait_dim`、**作废所有已演化种群**、golden 必须重 bless。

（下面这段是 R9 开跑时的记录，保留作历史。）

**R9（2026-08-03，`§13`）：这是 §12 判负时自己指出的唯一没走过的方向——动草层，不是果层。**
两个默认关、bit-exact 的地形旋钮已落地（`2bb48e0`），判据跑之前已预注册（`231c016`）：

- **`grass_shade`** —— 林下遮荫 + 等量归一化。装它的理由是**实现与自己声明的设计意图反号**：
  `config.py` 在 `fruit_max` 上方写「林冠遮蔽草本层 ⇒ 林下是低放牧底 + 高值例外」，
  而 `terrain.py` 给满冠层 **1.538×** 的草承载。默认世界里「果承载前 10% ∩ 草承载 <30%
  陆地均值」的格子是 **0 个**；`grass_shade=1.3` 是 **173 个（80.5%）**。
- **`water_sea_dist`** —— `water_dist = min(d_river, d_sea)`。字段注释写「到最近的河**或海**」，
  实现只算到河。**喝水那半是对的**，坏的是梯度：海只有岸线处一个阶跃，有效探测距离约 11
  世界单位对河的 30。紧贴大海的 376 个陆地格中位水距 **54.7**。
  **翻默认值会翻转 `biology.md:156` 的核心不等号**，逐条代价在 §13.2，本轮默认关。
- **两者必须一起上**：单独遮荫分离但撞水墙（草重心 49.75→59.27），单独认海不分离；
  一起是分离 0.125 且草重心 **44.15 < 基线 49.75**。八个回合里第一次「分离」与「可达」不对立。

**R9 判决已出（96 run、0 错误，全文 `docs/multispecies_feasibility.md` §10）：分离做到了，
物种没做出来。** 遮荫世界里选择造出**稳健的生态型—生境关联**（原始 `r` 的 `T1-N1` =
**-0.2697**、**12/12 种子**、p=0.00049 触地板、效应/噪声 **-1.672**，遮荫放大 **5.29x**）——
这是本纲领九个回合里**第一个空间生态型信号**。但基因**均值**动了而**方差与双峰性没动**：
**方向性选择 + 空间分选 = 一条 cline，不是分歧选择，不是两个物种。**
根因一个数解释完三个结果：**果层只占陆地能量的 11.15%**（⚠️ 那是 `niche` 厚果层臂的数，`fruit_energy=4.0`；**默认世界是 0.37%**，差 30 倍）。
H1/H2 判 **undecidable**（功效仅 0.159/0.216），失败判据不触发。
`result-analyst` 推翻主控两处：①「塌缩被地形解除了」站不住；②预注册 H3 **符号写反**
（`forage_pref` 是**草**偏好基因）。

**下一步 —— 要先论证，不要直接开跑**（`feasibility.md §10.10`）：
①**换自变量攻果层能量占比**（不再攻分离度，已到顶）——但**必须先论证它与
`experiments.md §5` 已关闭的「加厚果层」的区别**（那条是**共址**几何下加厚）；
②H3 交互补 seed 12-13（`s≈14` 即达功效）——**但属 optional stopping，须单独预注册**；
③R9-E 跨地形，脱离伪重复。

（下面这条是本主线之前的记录，保留作历史。）

### 上一条主线：可见形态防御性状——armor✅、spike 重设计后进攻侧✅/防御侧✗

**尖刺重设计（进攻/防御双用+venom，`04d18e7`；可视化中毒染绿 `a9978ab`）+ 6 种子验证
（run_id 20260724-spike-redesign）判决**：**进攻侧盘活**——carn_spike ON 0.202 vs OFF 0.052，
6/6，p=0.031，相对旧设计 10.6×（食肉尖刺穿透 armor 被清晰选择）；**防御侧仍未活**——herb_spike
ON 0.023 < OFF 0.044，2/6，p=0.44，贴旧漂变；根因是 venom 毒发时猎物已被咬死、收益回退亲缘。
护栏健康（carn_frac 0.189 不灭绝）。救防御侧需让 venom 在攻击瞬间给猎物逃脱概率（见
`docs/trait_defense_landing.md` §8）。**下一个方向待用户定盘。**
（下面这行是重设计前的旧记录，保留作历史：armor 演化✅、旧 spike 证伪。）

水系统 ✅、性状进化两波 ✅、优化/清理审计 ✅、恐惧地景 ✅、昼夜系统 ✅ 均已落地。
**防御性状 armor+spike 机制+可视化已落地**（`docs/trait_defense_landing.md`）。**6 配对种子演化
验证（run_id 20260724-defence-evo）判决**：**armor 真的演化出来**——herb_armor ON 0.170 vs 中性
漂变 0.067，6/6 同向、配对 Wilcoxon p=0.031（地板），是本项目第一个演化出来的可见形态防御；
**spike 证伪**——herb_spike ON≈OFF≈0.027、3/6、p=1.0，且是「反伤收益太弱」而非「成本压 0」
（同税下 armor 能演化）。P2 无信号（功效不足或无密度效应），P3 部分证伪（carn_armor 被基因流
抬高、非中性）。后续候选：救活 spike（上调 spike_reflect）/ 补种子脱离功效不足 / 查 carn_armor
基因流。**下一个方向待用户定盘。**

**可见形态防御性状（`docs/trait_defense_catalog.md` + `docs/trait_addition_feasibility.md`，
2026-07-24，本轮只出报告）**：用户要「看得见的性状演化」（尖刺/厚皮）。调研判决——防御是
现有 docs 没碰的新维度、作用在成体捕食维度（天然 A 类、不被幼体渴死删失）、复用红皇后 escape
模板、红皇后成功是存在性证明。**首推首个落地性状=厚皮/减伤 armor**（`feasibility.md` §C 有
逐文件规格 + 3 条可证伪预测 + 6 种子测试设计）。**下一步可执行动作**：按 `feasibility.md` §C.2
落地 armor（`trait_dim` 5→6、predation 减伤 + metabolize 能量税、golden 重 bless、可选上屏），
跑 6 种子验 P1-P3——**等用户点头再动手**（会作废种群 + 动 golden 带）。留意 §B.2 那条 A/B 张力：
代价务必记 metabolize 能量账，记水账/生长账会滑回 B 类重演体型基因方向反转。

**昼夜系统（`docs/day_night.md`，2026-07-24）**：恐惧场缺的"昼夜通勤"这条线收口。五杠杆探索
判决 **"捕食者搬离河岸"结构不可达**（水太硬把所有个体钉在河边）；`pred_nocturnal`（夜间捕食者
射程↑）是唯一干净阳性，**已默认开启**（`day_length=400`+`pred_night_amp=1.0`，`8096bda`）：
捕食风险昼夜错峰（hunt_success 夜−昼 +0.245，6/6）、渴死 −13pp，代价 carn_frac +3.3pp。
**Phase 2（加大脑演化出空间通勤）证伪**（§6.2，静态地形二阶演化测不出）。

**下一步不是既定任务——方向待用户定盘**（会动世界行为或 golden 带，不要自己挑一个就开跑）。
**注意基线已迁移**：昼夜默认开，今后实验默认带昼夜捕食，`carn_frac` 基线已被抬 +3.3pp；要昼夜前
基线做对照一律 `--set day_length=0`。候选：

1. **D：修比例（压 `carn_frac`）** —— `scale_and_density.md` §5.3 的净判决。B（扩世界降密度）
   已被两轮探针证伪（密度是尺度不变量），D 是唯一幸存且低风险的一条：把 `carn_frac` 从
   当前 ~10–12% 往现实 <1% 压，走 `carnivore_riparian.md` 的 `carn_cost` / 能量转换路线。
2. **补「河岸带停留时长 / 到访频率」指标** —— `landscape_of_fear.md` §6 后续第一条。当前只有
   `carn_water_dist` 静态均值，分不清「缩短停留」和「完全不去」。成本低、纯观测、不动世界行为。
3. **腐食通路第二版 6 种子验证** —— carrion 已折进 food retina 让食肉者**主动找尸体**（`sensors.sense`、
   diet 加权、**零 `in_dim` 代价、默认关 bit-exact、golden 不动**）。设计与判据已写进
   `multispecies_feasibility.md` §7"第二版：接 retina 主动觅食"（run_id `20260725-carrion-v2`），
   **待跑 6 配对种子 ON vs OFF**（`--set carrion_enabled=True`）。首版 4/6 弱阳性，本版期望 6/6 且抬 min。

### 已实现、已验证、结论已归档

| # | 事项 | 结论 | 详见 |
| --- | --- | --- | --- |
| 11 | **恐惧地景（设计二）** | **✅ 已落地并默认开启（`eca17e5`）**：`fear:[n_cells]` 场折入 `pred` 通道，零 `in_dim` 改动、不作废种群。6 种子实测捕食者从坐等转游走（`carn_speed` 1.5→2.4，6/6，p=0.031）、占比 12→10%、不恶化渴死；但"搬离河岸"只弱成立（`carn_water_dist` +1.4）——缺昼夜通勤。`--set fear_rate=0` 消融 | `landscape_of_fear.md` §6 |
| 8 | **可遗传 `attack_range` + 对称 escape（红皇后）** | **✅ 已落地（性状进化首个赢家）**：本项目第一个可测量的协同演化——逃逸只在功能开启时演化（1.91 vs 0.50，p=0.031）、滞后攻击约 3500 步、把有效攻击距离压回 6.28≈起点，捕食者占比减半（13.7% vs 25%）。到 ~15k 步收敛成协同均衡（非失控）。trait_dim 3→5，种群作废一次 | `attack_range_redqueen.md` |
| 9 | `vision_radius` / 更远水感知（两次实测） | **❌ 整条路线划掉**：先证伪 as-specified（感知格天花板），再解耦几何把天花板拆除后**仍无适应度价值**——水感知距离根本不是渴死约束。mortality §1.4 该路线关闭 | `vision_radius_heritable.md`、`vision_geometry_unlock.md` |
| 10 | `mutation_sigma` 可遗传（演化能力的演化） | **❌ 落"平凡"类**：Ne~2000 静态地形下对选择基本不响应（A/B1/B2/B3 全测不出）。只留文档弃代码 | `mutation_sigma_heritable.md` |
| 1 | 同类可见性（`peer` 通道） | 微弱正信号：`mean_age` +29 步（CI 排除零）。**跟随的直接证据现已测出且为反向——幼体比随机置换更远离成体，是隔离不是跟随；`mean_age` 收益机制非空间跟随** | `trait_evolution.md`、`trait_stage0_analysis.md` §3 |
| 2 | 拆反混合装置（四个开关） | 实验够不到问题：单峰 founder 六种子全灭绝。需先做生态学重调 | `experiments.md` 第 3 节 |
| 3 | 生态位构建 Stage 0（两版） | 负反馈版和正反馈版**都没形成路径**。符号不是主要障碍，缺的可能是"家"（固定端点） | `experiments.md` 第 2、4 节 |
| 4 | 体型基因 | **预测被反向证伪**：预测 [1.1,1.4]，实测 0.725 | `trait_evolution.md` |
| 5 | 果实层 | 全图供给只有植物层的 0.35%，对空间行为无效果。三条出路未决 | `experiments.md` 第 1 节 |
| 6 | 地形视线遮挡（`los_occlusion_enabled`） | 已实现、默认关闭、45 测试全过。**实测步进成本 +8%（s=2）到 +15%（s=4），是旧"<5%"估算的 2-3 倍** | `three_d.md` §5.1 |
| 7 | 3D 渲染器（路线 A） | **已合并，截图验证通过**：真实立体起伏+自遮挡、轨道相机、点击选中对位。不动 wire、不作废种群 | `three_d.md` §2.1 |

### 未做——按最新调研更新（水系统是收敛点）

**四份调研（`rebalance` / `landscape_of_fear` / `trait_roadmap` / `water_system`）在同一个点
会合：幼体渴死瓶颈（81.9% 死亡）是共同的总闸门。而 `water_system.md` 的消融证明：
调水经济是第一个能真正松动这个闸门的杠杆——`carn_water_dist` 10.8→34.3（捕食者
终于离水）、渴死大降。但有一个必须正视的代价：每个有效水杠杆都让种群涨 27-72%、
捕食者占比从 23% 升到 33-41%，加重 `carnivore_riparian.md` 已指出的"捕食者偏多一个
数量级"问题。所以水系统不能naive地拉，要挑"精确打击幼体、不连带推高捕食者占比"
的那个旋钮。**

**主线 0 —— 水系统 ✅ 已完成并落地（`543483a`，已 push）**
- 三候选全部 6 种子实测、判决见 `docs/water_fix_decision.md`。选中 retune（耗水减半
  + carn_cost 0.15）作为 main 默认值。实测：thirst 83%→55%、捕食者离水 11.4→25.3、
  carn_frac 稳在 22%（无团灭）。代价：种群 +79%（正交轴，见 rebalance.md）。
- 两个精确打击 flag（`water_deficit_buffer`、`water_lactation_floor_frac`）已合并、
  默认关闭，作为可选消融保留。buffer 挂在 carn_frac 顶到 41%；provisioning 碰不到
  离水诉求（反而更贴水）。
- **副作用记录**：捕食者离水这条（原主线 A）已被水修复顺带松动（`carn_water_dist`
  11.4→25.3），恐惧地景不再是唯一路径——但离水后捕食者占比仍偏高，`carnivore_riparian.md`
  的占比问题未解。降种群仍未解且被水修复加重。
- **对下游的关键影响**：幼体渴死瓶颈已从 83% 降到 55%，`trait_roadmap.md` 的"B 类
  性状必须先解瓶颈"前提**部分解除**——B 类候选（体型基因重做、maturity_age 门）现在
  统计功效应该显著改善，可以进 Stage 2 分支 A。

下面两条主线仍然成立，且 `water_system.md`/水修复落地后的关系：**形态演化会松动
（正交互补，且瓶颈已缓解，B 类解锁），捕食者离水已被水修复部分解决，降种群方向相反
被加重。**

**主线 A —— 捕食者离水**（用户反复提的问题一）✅ **恐惧地景已落地并默认开启**
- **不要走"减慢植物生长"这条路**——`rebalance.md` 三种子实测判决：它只碰资源总量、
  碰不到离水行为，温和减速让捕食者反而更贴水，激进减速直接灭绝捕食者。三目标同时
  成立的区间**不存在**。
- **✅ 恐惧地景（设计二）已合并（`eca17e5`）并默认开启**（下表 #11、`landscape_of_fear.md`
  §6）：`fear:[n_cells]` 场折入现有 `pred` 通道，**零 `in_dim` 改动、不作废种群**，复用踩踏
  场的散射累加原语。6 种子实测（rate0.05/scale3.0）：捕食者从坐等转向游走（`carn_speed`
  1.5→2.4，6/6，p=0.031）、占比 12→10%（5/6）、不恶化幼体渴死（`thirst_age` +4.9，预标风险
  未兑现）。**但"搬离河岸"只弱成立**（`carn_water_dist` +1.4，噪声大）——水是硬约束+捕食者
  驻水边，恐惧场给不了通勤，要真正离水缺的是昼夜通勤而非再调 `fear_*`。消融 `--set fear_rate=0`。
- **✅ 昼夜系统已整条落地（`docs/day_night.md`，2026-07-24）**：五杠杆探索判决"捕食者搬离河岸"
  结构不可达（水太硬）；`pred_nocturnal`（夜间射程↑）唯一干净阳性、**已默认开启**（`8096bda`：
  `day_length=400`+`pred_night_amp=1.0`，热/暗/觅食默认关）——hunt_success 夜−昼 +0.245（6/6）、
  渴死 −13pp（缓解头号瓶颈）、代价 carn_frac +3.3pp。Phase 2（加大脑演化空间通勤）证伪。
  `--set day_length=0` 逐位回退昼夜前基线。**恐惧场那条"缺昼夜通勤"的注脚就此收口：昼夜给的是
  时间维错峰，不是空间迁移——离水在本世界结构不可达。**
- **水修复也顺带松动了离水**（`carn_water_dist` 11.4→25.3，主线 0），故离水现已有两条独立
  证据但都不彻底——捕食者占比仍偏高，`carnivore_riparian.md` 的占比问题未解（见候选方向 D）。

**主线 B —— 性状/形态演化**（用户反复提的问题二）✅ **两波已落地**
- **✅ 赢家：可遗传 `attack_range` + 对称 escape（红皇后军备竞赛）已合并进 main**
  （trait_dim 3→5，种群作废一次）。这是用户"看得见性状在演化"诉求第一次被真正满足——
  捕食者攻击距离与猎物逃逸互相追着往上演化、相位滞后 ~3500 步、有效距离被压回起点。
  意外好处：捕食者占比减半（13.7% vs 25%），顺带缓解捕食者偏多。详见 `attack_range_redqueen.md`。
- **❌ `vision_radius` 可遗传：已实测、已证伪，只留文档不留代码。**
  `docs/vision_radius_heritable.md`：感知格天花板只给 0.33 单位增长空间，水修复把
  缺口推到 45.5 单位，性状近中性、不缓解渴死、无副作用。**下一步不是调这个基因，而是
  先改几何**（大 `sense_grid` 格 + 高 `k_neighbors`，或把水通道解耦出邻居块）——这是
  一块独立的、更大的前置工作。在几何改之前，vision_radius 不进任何 trait_dim 打包。
- **`mutation_sigma` 可遗传：已实测、落"平凡"类负结果，只留文档不留代码。**
  `docs/mutation_sigma_heritable.md`：Ne~2000、20000 步、静态地形下，把突变率变基因
  **对选择基本不响应**——A（reduction 方向）p=0.56 测不出；B1 地板停在 ~0.048 但与冻结
  对照无法区分、无"选择设定的地板"；B2 食肉/食草分裂在**无功能的冻结臂里反而更大**
  （是 sigma-diet 基因位搭连，非对突变率的选择，差中差 p=0.84）；B3 不成立，且水修复
  已把建群瓶颈削弱到 B3 前提不存在。要看二阶效应需换尺度或更持久的选择压，不是"再跑
  一次"。代码同样不合入 main（null 机制不值一次种群作废）。
- **代价货币原则**（`trait_roadmap.md` 的核心洞见）：新性状代价记在**水账**会栽进
  幼体渴死陷阱（体型基因就是），记在**能账**才安全。任何新性状先过这一关。
- B 类性状（体型重设计、幼体期门控）**必须等瓶颈解除**，现在别碰。

**打包提示**：主线 A 头号方案不改 `in_dim`；主线 B 的 `vision_radius`/`mutation_sigma`
只动 `trait_dim`（便宜）。若要做会改 `in_dim` 的（危险记忆槽 A2、果实层删除、踩踏
"家"槽），应打包进同一次种群作废摊薄代价。

**零散**（与两条主线正交，随时可做）：
- **补上 peer 通道缺的那一环**：幼体到最近成体的平均距离 vs 随机置换基线。成本很低
  （run 结束对活体状态算一次），是"活得久一点"和"学会了跟随"之间唯一缺的证据。
- **踩踏全地形版**：`trample_path_gain` 接到 `move_cost`，排除"只在林冠下生效"。
- **果实层三选一**（放大/删除/承认装饰）。删除改 `in_dim`，打包做。

## 不做的事

- **不要许诺"完全开放式演化"（Bedau Class 3）**——没有任何已发表系统在静态环境
  下达到过。现实天花板是 Class 2。
- **不要做字面意义的工具使用**（捡起/使用物体）——需要先塞入一个物体类别，直接
  违反"不显式指定人类属性"的约束。
- **不要把 ANNECS 当作本项目的指标**——它专属"环境生成"路线（POET 系），本项目
  地形静态，不适用。
- **不要重新加记忆遗传。** Weismann barrier + 消融 + Rogers' paradox 三条独立
  理由同向。
- **不要重新论证果实层的"logistic 再生陷阱"**——根因是量级不是陷阱。
- **不要再调踩踏参数**——两个符号都测过了。
- **在做生态学重调之前不要再拆反混合装置**——单峰 founder 会直接饿死，测不到
  任何东西。
- **不要用"减慢植物生长/降资源总量"来赶捕食者离水**——三种子实测证伪（`rebalance.md`）：
  只碰资源总量、碰不到离水行为，还会灭捕食者。离水是资源的**空间结构**问题，不是
  总量问题。
- **不要单独下调 `carn_cost` 来"让捕食者活得起"**——安全但会加重"捕食者占比已经
  偏高一个数量级"的问题（`carnivore_riparian.md`），不是免费旋钮。
