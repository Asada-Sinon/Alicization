# 多物种生态箱：研究纲领与自动循环

**这份文档是「怎么干」，不是「干出了什么」。** 每一回合的**结论**写进它所属的专题文档
（`multispecies_feasibility.md` / `herbivore_overpopulation.md` / …），这里只保留：
纲领（为什么是这条路）、循环的七道闸、以及**回合台账**（每回合一行：假设 / 判决 / 指针）。

建立于 2026-08-02，用户睡前授权自主长跑，目标是「局部的多物种生态箱演化 + 可视化结果」。

---

## 1. 纲领：本世界为什么现在只有一个物种可活

把已归档的判决串起来，多物种失败不是三次独立失败，而是**同一个根因的三个投影**：

| 已判决 | 表面结论 | 投影回的根因 |
| --- | --- | --- |
| `feasibility.md §1` diet 轴中段死区（六种子全灭绝） | 第三营养级进不去 | 营养级轴上只有两个可活点 |
| `feasibility.md §9.5` forage_pref **证否**（方差反向塌缩 32.6%） | 资源分割不发生 | ~~果层只有草层的 0.07%，没有第二个饭碗可专精~~ **← 这条归因已被 §9.8 证否**：给了 28% 的真饭碗 + 最大选择强度，塌缩反而更深（−38.3%）。真因是**中间型最优**，不是没有饭碗 |
| `feasibility.md §7` 腐食 v2 18 配对翻正但带 −5.6% 种群代价、跨地形只 4/5 | 腐食通路太稀薄 | 尸体流量相对草层同样是零头 |
| `water_system.md` / `day_night.md` 「捕食者搬离河岸」结构不可达 | 空间分化不发生 | **水把所有个体钉在河岸，只有一种栖息地** |

**[本世界实测] 一句话根因：这个世界只有一个饭碗（河岸的草），所以只能坐得下一个食草生态型。**
金标 `plant_total=10790` vs `fruit_total=40.3`（`scripts/golden.json`）——果层是草层的 **0.37%**，
20000 步长跑下更低（§9.5 测得 0.07%）。在这种供给比下，任何「专精果实」的基因型都必然饿死，
方差塌缩是**正确**的演化响应，不是机制没点火。

**所以纲领是：先造出第二个生态位，再谈第二个物种。** 顺序不能反——先加基因、再指望生态位
自己出现，已经被 forage_pref 那次证否了（付了永久 `trait_dim` 7→8 的代价）。

### 1.1 三条候选轴（按「造饭碗」的便宜程度排序）

1. **资源轴——加厚果层。** 纯 `--set`（`fruit_max` / `fruit_regrow_rate` / `fruit_patch_threshold`），
   零代码、零种群作废；forage_pref 基因**已在 main**，代价已付。**这是第一回合。**
2. **空间轴——让第二个栖息地能活人。** 水是硬约束（`water_system.md`），但果层若够厚，
   林冠区（中海拔近水）本身就是一个和河岸草原不同的斑块。空间分化是「**局部**多物种」这个
   诉求的字面含义，需要新的读出指标（生态型 × 空间的联合分布），不是新机制。
3. **营养级轴——腐食 / 顶级捕食者。** 已知稀薄；腐食留作已翻正但带代价的价值决策，
   顶级捕食者只作快速证伪。**排在资源轴与空间轴之后**，因为它们不造饭碗，只分饭碗。

### 1.2 什么算「成功」（终局判据，先写后跑）

用户要的是「局部的多物种生态箱演化」+ 看得见的可视化。拆成四条可证伪的：

- **S1 生态位存在**：第二资源层的现存量 ≥ 草层的 10%（当前 0.37%），且不靠杀死草层达成。
- **S2 分化发生**：`forage_pref_std` 处理臂 > 对照臂，≥5/6 同向，配对 Wilcoxon p≤0.05；
  且基因分布的**形状检验**拒绝单峰（真·双峰，不只是方差变大）。
  **[修订 2026-08-02，跑之前改，理由在此]** 原文写的是 Hartigan dip test。本环境无
  `diptest` 包，而手工移植 AS 217 的 GCM/LCM 交替拟合是典型的「错了不报错、只返回一个
  看起来合理的数」——正是本项目反复警告的那类失效。改用**1-vs-2 高斯成分的参数自助
  似然比检验（BLRT，McLachlan 1987）**：EM 拟合、LR 统计量、自助零分布三部分都能对着
  已知真值的合成数据验，`tests/test_kernel.py::test_shape_test_separates_unimodal_from_bimodal`
  就在验。它对「性状替代=两个群」这个**具体**备择假设也比通用单峰性检验更有功效
  （dip 检验的是对任意单峰性的偏离）。实现在 `scripts/probe_trait_dist.py`。
  **[再修订，仍在跑之前] 形状检验的绝对 p 值在演化种群上无效，判据改为配对臂间比较。**
  实测（2000 步、seed 0、herb 携带者）：`forage_tradeoff=0.5` 给 LR=212.8 / p=0.0200（地板），
  而**中性对照 `forage_tradeoff=0.0`——基因被编译期断开、什么都做不了——同样给 p=0.0200 地板**。
  根因：演化种群不是随机样本而是**谱系**，亲缘共享基因值使分布天然呈家系团块，
  有效样本量是独立世系数而非个体数，自助零分布（按 iid 重采样）因此窄得离谱。
  故 S2 的形状部分改判为：**`blrt_lr_per_n` 处理臂 > 对照臂，6 配对种子 Wilcoxon**。
  除以 n 是因为 `2(ll2−ll1)` 随样本量线性增长而两臂携带者数不同，直接比 LR 等于在比种群大小。
- **S3 局部性成立**：两个生态型在空间上分开——生态型 × 栖息地（林冠/河岸）的联合分布
  显著偏离独立零模型（这条**必须跨地形种子**，否则是伪重复，见 `conventions.md` §6）。
- **S4 不塌方**：护栏三条同时守住——`carnivore_frac` ≥ 0.05（捕食者不灭绝）、
  `population` 相对基线在 ±30% 内、`death_thirst` 占比不恶化。

S1→S2→S3 是**串联**的：S1 不成立就不必跑 S2（这正是 §9.5 那次浪费掉的 36 个 run 的教训）。

---

## 2. 循环工作流（七道闸）

**一回合 = 一个可证伪假设走完七道闸。** 每回合结束在台账（§3）追加一行。
闸门是顺序的，前一道不绿不许进下一道；某道闸黄了就**降级本回合的 claim**，不是放宽闸门。

### G0 选题
从 §4 backlog **顶部**取一条。取出来必须当场能写成「假设 + 成功判据数字 + 失败判据」，
写不出具体数字说明还没想清楚——那就把它拆开，先跑一个能写出数字的探针回合。
按 `/exp` 第 1 步的字段，在目标 `docs/<topic>.md` **先建一节**（此时实验还没跑）。

### G1 净树
`git status --short` 干净、`check.py` 绿。不干净先提交——脏树上跑出的结果 provenance
对不上任何 commit（`MEMORY.md` 的 `[LEARN:tooling]` 就是这条）。

### G2 最小改动
**能用 `--set` 做到就绝不写代码。** 必须写代码时：默认关、旧路径 bit-exact、补单元测试、
`check.py --full` 全绿、提交（中文 message，说清为什么改）。
动 `in_dim` 要单独确认（作废全部演化脑）；动默认生态参数要预告 golden 重 bless。

### G3 跑
6 配对种子 × 2 臂 × 20000 步，**并发上限 6**（12 会 GPU OOM），`--json`，
产物落 `outputs/<run_id>/`，先写 `provenance.txt`（git hash + resolved config + seed 列表 + 完整命令）。
后台跑，别傻等；12 个 run 约 10–15 分钟，36 个约 27 分钟。
**空间性结论额外交叉地形因子**（`ridge_wavenumber` / `ridge_base_y`），否则是伪重复。

### G4 判决
**派 `result-analyst` 读 `outputs/`，主控不自己读日志**（十几个 run 的日志进主 context 就清不掉，
而且 fresh eyes 不知道我希望看到什么）。要求：逐种子数字 + 每个数字的出处文件 +
配对 Wilcoxon/Mann-Whitney + 效应量 + bootstrap CI + 判据逐条「达成/未达成」。
**n=6 配对的最小双侧 p 是 0.031——那是地板，不是强证据。**

### G5 落档
结果写回 G0 建的那一节，**四标签逐条标注**（`[现实]`/`[本世界实测]`/`[对应]`/`[提案，非结论]`）。
负结果照样完整写——它们是唯一能阻止同一个想法被重试的东西。
`docs/TODO.md` 加指针；§3 台账加一行；§4 backlog 按判决重排（判决要能改变下一回合选题，
否则这个回合白跑）。提交 + push。

### G6 可视化闸
**每 3 回合，或任何机制转为默认开时，必过一次。** 本项目有过教训：着色器把植物场画成
一块饱和色板，只靠读代码审了很久没发现（`CLAUDE.md`「用眼睛验证 GPU 与画布输出」）。
过法：起 `run_live.py`、headless chromium 截图（`--use-gl=angle --use-angle=swiftshader
--enable-unsafe-swiftshader`）、**人眼看**。终局交付物是「生态箱面板」（§1.2 四条判据各一张图）。

### 停机与降级规则
- backlog 空了 → 不停机，去 §4 的「补给区」按纲领生成新候选。
- 撞到需要用户拍板的岔路（会作废种群 / 会改默认生态 / 花费超过一晚）→ **不擅自选一条**，
  写进 `HANDOFF.md` 的 PENDING，然后**跳过它继续下一条 backlog**，不要停在那里等。
- 某回合判决为负 → 这是正常产出，不是失败。落档、更新 backlog、进下一回合。

---

## 3. 回合台账

| # | 回合 | 假设 | 判决 | 落档指针 |
| --- | --- | --- | --- | --- |
| R1 | 果层供给扫描（现存量口径） | 存在一组 `fruit_*` 参数使果层**现存量** ≥ 草层 10% | **判据自身被证伪**：现存量量的是「剩多少」不是「供多少」，把生态位低估约一个数量级。产出是补上流量观测量 | §5 |
| R1b | 果层供给扫描（流量口径） | 存在参数点使 `frugivory_frac ≥ 0.20` 且护栏不破 | **不通过**：主判据达成（两臂 6/6 同向 p=0.031）但**渴死护栏破**（B 2/6、C 3/6）。附带更正一条自己的过度解读（林冠移动相对零模型可忽略） | §6 / §7 |
| R1c | 果实的预成水 | 渴死破裂是水口径问题（果实每卡路里的水只有草的 1/`fruit_energy`），修正后护栏可守 | **不通过，且假设本身被推翻**：三剂量渴死只守住 0/6、3/6、4/6；真因是**承载力**——种群比值与渴死增量 ρ=+0.90，而 `mean_water` 三臂对基线全无显著变化。加进去的水被多出来的身体吃掉了 | §8 |
| — | **跨回合合并结论** | — | **水限世界里加食物只会变成更多身体**；「加厚食物层造生态位」整条路证伪，已进 `experiments.md §5` | §10 |
| R3 | 等量再分配 | **同样多**的食物分成两层供应能造出生态位（总植物通量守恒是硬约束） | **无法判定**：字面上两臂都不通过（C2 2/6、C5 4/6 与 2/6），但**判据容差 ≈ 1 个复现噪声 SD**，通过/不通过都没有信息量。资源轴**挂起**不关闭。供给端确实做成了（份额 +0.27，6/6，效应是噪声的 500 倍） | §11 |
| **R4** | **同种子重跑方差**（方法学，插队） | 20000 步下重跑同一种子的散度是否大到让「6 配对种子」协议失效，以及确定性 XLA 算子能否修 | **坐实**：`--seed` 只解释 0–26% 的方差，`carn_frac` 同一种子跑出 0.060–0.2505；配对差噪声被 `conventions.md §5` 低估 5.2 倍。确定性算子 >20× 慢、不可用 | `docs/run_to_run_variance.md` |
| R2 | 厚果层 + 最大选择强度下的资源分割 | 给了真饭碗（果供能 28%）后 `forage_pref` 会分化 | **P2 不成立，且失败方式不是预设的那种**：`sd` 比中性漂变**低 38.3%**（6/6，p=0.031）——**是稳定化选择主动收窄，不是基因流被动压平**。同时证否了 `feasibility.md §9.5` 的归因（不是「没饭碗」，是「中间型最优」） | §9.5–§9.9 |
| P3 | 权衡强度剂量项（补 0.5 档） | 稳定化挤压随强度单调？还是弱权衡歧化、R2 跨过了头？ | 三点上挤压单调、`M>P` 0/6。**但只测三点，窄窗口未排除** → 触发 P4 | §9.10 |
| P4 | 窄窗口收口（补 0.125 / 0.25 两点，共五点 90 run） | 0 与 0.5 之间有没有「弱权衡歧化」的窗口 | **没有高于漂变的窗口**（`L>P` 0/6，`N>P` 整体打平）。**但曲线不单调**（N−L 5/6、CI 不含 0），且 N(0.25) 是 NULL 不是更窄。**边界：只排除了幅度 ≥24–41% 的窗口** | §9.11 |

---

## 4. Backlog（顶部优先，判决后重排）

**[2026-08-03 全部回合走完后重排]**

**已关闭，不要再试**：
- ~~加厚食物层造生态位~~ —— 证伪（§5–§10，负结果进 `experiments.md §5`）。
- ~~等量再分配~~ —— 判为无法判定后，**已被 R2 从上游解决**：饭碗有没有做厚不是瓶颈，
  中间型最优才是。**再调果层供给量没有意义。**
- ~~生殖隔离（`assortative_mating` 扩到按 `forage_pref` 排序）~~ —— **前提被 §9.7 推翻**：
  它对抗的是基因流，而实测是稳定化选择。**不要把它当下一步交给用户。**
- ~~可视化交付~~ —— 已完成（Artifact，见 `HANDOFF.md`）。

**新的顶部（都出自 §9.9，且都会改默认世界行为 → 要用户拍板，不擅自开跑）**：

1. **把两个饭碗在空间上分开。** §9.9 的诊断：果实长在森林里，而森林就是所有个体本来
   待的中海拔近水带，两种资源**交织**，于是「两样都能吃」严格优于专精。要让专精划算，
   得让个体大部分时间只遇得到一种资源。最小改动是动 `terrain.py:207` 的
   `fruit_capacity = fruit_max × patch × forest² × (1−rock)` 门控——例如把果层挪到
   **高海拔**或**远水**带。**代价：改地形派生量 → golden 重 bless；且会与水限约束正面相撞。**
2. **时间轴分割**（同一份资源、不同取用时段，零新增承载力）。`day_length` 已默认开、
   `scripts/probe_diel.py` 的相位分箱可直接复用。比空间轴便宜，但 `day_night.md` 已判
   「空间通勤结构不可达」，时间生态位是否可达未知。
3. ~~补剂量点排除窄分化窗口~~ —— **已做完**（§9.10 + §9.11，五点 90 run，无窗口）。
   **不需要拍板的 backlog 至此清空。** 若将来仍想追，最省的一步是把 N(0.25) 与 L(0.125)
   的种子从 6 扩到 12 配对（N−L 现为 5/6、p=0.0625，正好卡在地板上方一档），
   而**不是**再加剂量点——`blrt_lr_per_n` 的 MDE 已达 +104%，指标分辨率不够。
4. **顶级捕食者快速证伪**（`feasibility.md §2 c2`，预期 NULL，只为拿实测）。

**先决条件**：以上任何一条开跑之前，先定统计协议（`run_to_run_variance.md §7`，
每格 k=3 重复），否则判决继续是抛硬币。

**补给区**（backlog 空时从这里生成，都出自已归档判决的「下一步」）：
- 救 alarm：加「高 alarm cell 猎物密度」读出 + `los_occlusion` 造超视距信息（`§8.6` 的下一步）。
- 果层增厚后重跑 diet 三峰可行性（中段死区是否被第二饭碗松动）。
- 生态型 × 昼夜相位的时间生态位分割（`day_night.md` 已有相位分箱探针 `scripts/probe_diel.py`）。

---

## 5. R1 果层供给扫描（2026-08-02，run_id: 20260802-fruit-supply）

- **假设**: 存在一组 `fruit_*` 参数，使果层现存量达到草层的 ≥10%（当前 0.37%），
  同时不破坏 §1.2 S4 三条护栏。
- **成功判据**: 至少一个参数点在 6 种子上满足 —— `fruit_total / plant_total ≥ 0.10`（中位数）
  **且** `carnivore_frac ≥ 0.05` **且** `population` 落在基线的 [0.7, 1.3] 倍内
  **且** `death_thirst` 占比不高于基线 +5pp。
- **失败判据**: 所有参数点要么达不到 10%，要么达到了但至少破一条护栏 —— 则「加厚果层」
  这条路被证伪，S 链在入口断掉，backlog 跳到空间轴（R3 改为在薄果层下做）。
- **对照臂**: 默认配置（`fruit_max=4.0`、`fruit_regrow_rate=0.008`、`fruit_patch_threshold=0.55`）。
- **种子**: 0–5（founder）。本回合是**供给量**结论，不是空间结论，暂不交叉地形；
  R3 的空间结论会补地形因子。
- **git hash**: `227cca4`（dirty: false），见 `outputs/20260802-fruit-supply/probe/provenance.txt`

### R1 定标探针结果（单种子，**非结论**）

6 臂 × seed 0 × 20000 步，出处 `outputs/20260802-fruit-supply/probe/*.log` 的 `JSON` 行：

| 臂 | pop | plant_total | fruit_total | fruit/plant | carn% | thirst% |
| --- | --- | --- | --- | --- | --- | --- |
| p0 默认 | 1905 | 4915 | 3 | **0.06%** | 8.9 | 52.6 |
| p1 `fruit_regrow_rate=0.03` | 1941 | 4547 | 4 | 0.10% | 6.5 | 50.7 |
| p2 `=0.06`（等于草） | 1744 | 6184 | 28 | 0.46% | 19.6 | 47.8 |
| p3 `=0.06` + `fruit_max=8` | 1719 | 6599 | 36 | 0.54% | 17.9 | 42.1 |
| p4 `=0.12` + `fruit_max=8` | 1699 | 6960 | 182 | **2.61%** | 20.7 | 42.2 |
| p5 p3 + `fruit_patch_threshold=0.35` | 1840 | 5460 | 30 | 0.54% | 15.4 | 51.5 |

**[本世界实测] 成功判据「现存量比 ≥10%」未达成**，最激进臂（再生率 15×、容量 2×）只到 2.61%。

### 判据本身写错了观测量（本回合的真正产出）

**[本世界实测]** p4 的果层容量是 `8.0 × 20% × 16384 ≈ 26000`，现存只有 182，即容量的 **0.7%**——
果子一长出来就被吃光。而 `ecology.regrow` 是
`growth = rate × field × (1 − field/cap)` **加** `baseline = baseline_rate × cap/ref_max`：
**现存量被消费钳在 0 附近时 logistic 项跟着归零，真正在供货的只剩 baseline 那一项。**

**[对应]** 按 `underworld/ecology.py:42-49` 逐项估算每步供给流量：
果层 `0.001 × 约 3277 个果格 ≈ 3.3 单位/步`；草层 `baseline 16384 × 0.015 × 约 0.65 ≈ 160`
加 `logistic 0.06 × 4915 × 0.79 ≈ 233`，合计约 393 单位/步。**果层真实供给约是草层的 0.8%，
而现存量比说的是 0.06%——现存量把生态位大小低估了约一个数量级。**

**结论 [本世界实测]：`fruit_total / plant_total` 量的是「剩多少」，不是「供多少」。**
一个一长出来就被吃光的层，现存量恒等于 ~0，与它是否在养活种群无关。**S1 判据挂在了错的
观测量上**——这不是判据太严，是判据测错了东西。按纲领的规矩明写，不把靶子挪走了事。

**[对应] 修法**：`Metrics` 追加三个**流量**观测量（`graze_gain` / `fruit_gain` /
`frugivory_frac`，`underworld/metrics.py`，`step.py:222` 把 stage-4 的两个 gain 传进去）。
纯观测、世界行为逐位不变、`check.py --full` 42 项全过且 golden 守在 1591 未动。
两个单元测试守住「两路流量划分总量」与「`fruit_max=0` 时 0/0 不出 NaN」。

**S1 据此改写为 R1b（见 §6），先写后跑，不复用本节的判据。**

---

## 6. R1b 果层供给扫描（流量口径）（2026-08-02，run_id: 20260802-fruit-flux）

**S1 改写**：生态位大小以**流量**度量——`frugivory_frac` = 果实供能 /（草+果实供能）。

- **假设**: 存在一组 `fruit_*` 参数使 `frugivory_frac ≥ 0.20`，同时不破坏 §1.2 S4 三条护栏。
- **为什么门槛定在 0.20**: 资源分割要能被选择，次级资源得撑得起一个可观的亚种群。
  `forage_tradeoff=0.5` 下专精者在本层拿 1.5×、在另一层只剩 0.5×，所以果层至少要供到
  两成，专精果实才可能不亏本。0.8%（§5 估算）下专精必饿死——这正是 §9.5 方差反向塌缩的原因。
  **0.10–0.20 记为「边际」，<0.10 记为「仍然没有第二个饭碗」。**
- **成功判据**: 至少一个参数点在 6 配对种子上中位 `frugivory_frac ≥ 0.20`
  **且** `carnivore_frac ≥ 0.05` **且** `population` 落在同种子基线的 [0.7, 1.3] 倍内
  **且** `death_thirst_frac` 不高于基线 +5pp。
- **失败判据**: 所有点要么达不到 0.20，要么达到了但破护栏——则「加厚果层」被证伪，
  S 链在入口断掉，backlog 跳到空间轴。
- **主旋钮**: `fruit_regrow_baseline`（默认 0.001）。§5 已证这是果层被吃空时**唯一还在供货**
  的那一项；`fruit_regrow_rate` 只在现存量非零时起作用，对被吃空的层近乎无效。
- **替代 vs 增补**: 单纯加果层是往世界里**增加**总生产力，会把已经 66× 现实的食草密度
  推得更高（`scale_and_density.md`）。所以同时探一条**替代臂**（抬果层 baseline 的同时
  压草层 `regrow_baseline`），目标是**换饭碗而不是多加饭**。
- **种子**: 定标探针 seed 0；正式判决 0–5 配对。
- **git hash**: 写本节判据时 HEAD 是 `f72babf`；**正式判决的 18 个 run 实际跑在 `365be70`**
  （以 `outputs/20260802-fruit-flux/verdict/provenance.txt` 为准；两次中间提交只动了
  `scripts/probe_trait_dist.py`，不进 kernel，不构成跨 commit 污染）。

### R1b 剂量探针一：生物量路线（单种子，**非结论**）

6 臂 × seed 0 × 20000 步，出处 `outputs/20260802-fruit-flux/probe/*.log`：

| 臂 | grassFlux | fruitFlux | **frugivory%** | pop | minpop | carn% | thirst% | starv% |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| q0 默认 | 245.5 | 0.3 | **0.12** | 1950 | 1346 | 7.8 | 51.7 | 19.5 |
| q1 frb=0.005 | 253.3 | 1.4 | 0.55 | 1965 | 1317 | 9.1 | 50.5 | 13.8 |
| q2 frb=0.015 | 251.3 | 4.8 | 1.87 | 1915 | 1446 | 12.0 | 44.4 | 19.5 |
| q3 frb=0.04 | 240.6 | 11.8 | 4.67 | 1838 | 1255 | 11.2 | 50.5 | 11.9 |
| q4 frb=0.10 | 238.5 | 21.4 | 8.22 | 1795 | 1149 | 18.4 | 43.0 | 13.1 |
| q5 frb=0.10 + grb=0.008 | 171.1 | 20.6 | **10.74** | 1626 | 1401 | 9.5 | 52.5 | 21.4 |

**[本世界实测] 流量口径下的基线是 `frugivory_frac = 0.12%`**——比 §5 按 `ecology.regrow`
公式估的 0.8% 还低一个量级（果格并非都坐在满容量上，`baseline_rate × cap/ref_max` 里的
`cap/ref_max` 实际远小于 1）。§5 的估算方向对、量级偏乐观，此处以实测为准。

**[本世界实测] 生物量路线单调但饱和**：`fruit_regrow_baseline` 从 0.001 抬到 0.10（100×）
只把份额推到 8.22%，且 0.04→0.10 剂量 2.5× 而流量只 1.8×——果层供给已有一部分吃不掉
（现存量从 2 涨到 260）。纯生物量路线要够 0.20 需要极端剂量。

**[本世界实测] 替代臂效率更高**：q5 同时把草层 baseline 压到 0.008，份额升到 10.74%
（分母降了），且 `min_pop` 反而从 1149 抬到 1401（种群底更稳），`population` 1626 仍在
S4 窗口 [1365, 2535] 内。代价是饿死占比 19.5%→21.4%、捕食者占比回落到 9.5%。

### R1b 剂量探针二：加入能量密度杠杆（单种子，**非结论**）

**[对应]** `dynamics.eat_fruit:170` 是 `gain = taken × fruit_energy × eat_efficiency`，而
`frugivory_frac` 算的是**能量**份额，取水走的是 `taken`（生物量）× `forage_water_frac`。
所以 `fruit_energy` 能不加生物量、也不碰水经济地抬能量份额。
**[现实]** 这正是真实果实的生态角色——相对叶片/草的纤维素，果实是浓缩糖分的高能量密度资源，
这是果食性动物存在的物质基础（需补文献，见 backlog）。

按 `k·F/(G + k·F)` 外推，`frb=0.10` + `fruit_energy=6.0` 应落在 21% 附近。
6 臂 × seed 0 × 20000 步，出处 `outputs/20260802-fruit-flux/probe2/*.log`
（frb=`fruit_regrow_baseline` 默认 0.001；fe=`fruit_energy` 默认 2.0；grb=`regrow_baseline` 默认 0.015）：

| 臂 | grassFlux | fruitFlux | **frugivory%** | pop | minpop | carn% | thirst% | starv% | forest% |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| r0 frb.10 fe4 | 226.3 | 28.4 | 11.16 | 1888 | 1443 | 16.5 | 48.8 | 12.3 | 20.6 |
| r1 frb.10 fe6 | 241.5 | 62.0 | **20.42** | 2065 | 1512 | 14.8 | 51.2 | 13.6 | 24.8 |
| r2 frb.25 fe2 | 228.0 | 37.7 | 14.17 | 2024 | 1548 | 12.9 | 55.7 | 11.3 | 22.9 |
| r3 frb.10 fe6 grb.010 | 208.2 | 59.5 | 22.22 | 1886 | 1544 | 9.2 | **59.8** | 17.5 | 26.0 |
| r4 frb.25 fe4 | 232.3 | 64.5 | **21.74** | 2145 | 1512 | 13.6 | 53.9 | 13.6 | 26.7 |
| r5 frb.25 fe4 grb.010 | 191.4 | 101.4 | **34.62** | 1947 | 1682 | 9.3 | 56.8 | 15.1 | 27.4 |

**[本世界实测] 20% 门槛可达**（r1 / r4 护栏干净；r3 破渴死 +8.1pp；r5 到 34.6% 但渴死 +5.1pp 压线）。

**[本世界实测] 压草层一律抬渴死**，机制清楚：`forage_water_frac=0.10`，**吃草本身在供水**
（`dynamics.graze` 返回 `taken × forage_water_frac`），砍草供给等于砍掉水补贴。
这是真实耦合而非噪声，也说明「替代」路线不是免费的。

**[本世界实测] `forest_frac` 随果层增厚单调上升**（21.7% → 26.7~27.4%）：种群确实往林冠移。
这是 S3 空间信号的第一个正向迹象，但单种子、且尚无零模型，**不构成结论**。

### 两条必须记下来的诚实说明

**(1) [对应] `frb=0.25` 与设计意图冲突。** 相对容量的自发再生速率：草层
`0.015/2.2 = 0.68%/步`，果层 `0.25/4.0 = 6.25%/步`——**果子比草恢复快约 9 倍**，而
`config.py:541` 写明「fruit's slowness is exactly what makes a remembered patch worth
something」。把果层做成快速再生资源，专精它就不再是一种不同的生活方式，只是换个地方吃。
r1（frb=0.10 → 2.5%/步，仍比草快 3.7×）程度轻些但同向。**这是一笔要认的账，不是可以
略过的细节。**

**(2) [对应] 面积路线有硬天花板，到不了 20%。** `terrain.py:207` 是
`fruit_capacity = fruit_max × patch × forest² × (1-rock)`——果层被**森林平方**门控。
于是 (i) 抬 `fruit_max` 对基线通量**完全无效**（通量 `= baseline_rate × Σ(cap/fruit_max)`，
`fruit_max` 约掉了），这正是探针一里 p3/p4 抬容量没反应的原因；(ii) 把
`fruit_patch_threshold` 放到底，`Σ patch·forest²` 最多涨约 10×，即通量约 3 单位/步、
份额约 1.2%。**空间独特性是 `forest²` 给的，不是斑块阈值给的**——所以放宽阈值不会把果实
漏进草原，但也救不了量级。

### 我自己预注册里的算术错了（记下来，但不用它改本轮判据）

§6 立门槛时我论证「`forage_tradeoff=0.5` 下专精者在本层拿 1.5×、另一层只剩 0.5×，
所以果层至少要供到两成，专精果实才不亏本」。**这个论证是错的**：资源分割是频率依赖的。
设果层占植物总供给 `f`、专精果实者占种群 `q`，两类专精者的人均获得在平衡时相等：
`1.5f/q = 1.5(1−f)/(1−q)`，解得 **`q = f`**。

**也就是说任何 `f` 都能撑起一个比例为 `f` 的亚种群，专精者永远不亏本。** 真正卡住分化的
不是 `f` 有多大，而是**选择梯度能否压过基因流**——那取决于 `forage_tradeoff` 的**强度**
（以及有无生殖隔离），不取决于果层份额。`§9.5` 那次证否的根因诊断（「果层只有 0.07%、
无生态位可专精」）**在这个意义上也是不完整的**：0.07% 的问题不是「专精不划算」，而是
比例为 0.0007 的亚种群会被漂变和基因流直接抹平。

**但这不构成事后改判据的理由。** 判据按原文跑完、按原文判（见 §7），算术更正只用来指导
R2：R2 必须把 `forage_tradeoff` **强度**当作与果层份额同等重要的因子，而不是只开 0.5 一档。

---

## 7. R1b 判决：**不通过**——主判据达成，但渴死护栏破

18 个 run（3 臂 × 6 配对种子 × 20000 步），产物 `outputs/20260802-fruit-flux/verdict/`，
分析脚本 `explorations/20260802-fruit-flux/analyze.py`（数字全部由它产生，无口算）。
**运行 commit 是 `365be70`**（provenance 为准；§6 里记的 `f72babf` 是写判据时的 HEAD，已更正）。

臂：`A_base` 默认；`B_frb10_fe6`（frb=0.10, fe=6.0）；`C_frb25_fe4`（frb=0.25, fe=4.0）。

### 7.1 裁决：护栏按逐种子读

判据原文对聚合口径有歧义（「中位」明确修饰 `frugivory_frac`，护栏部分没写）。
**裁为逐种子**，依据是判据自己的措辞：「`population` 落在**同种子基线**的 [0.7,1.3] 倍内」
是明确的逐种子语言，`death_thirst_frac` 的「基线」在配对设计下继承同一口径。
**不采「护栏取中位」那个读法——它能让两个臂都通过，而正因为它能通过才更不该在看到结果
之后去选它。**

### 7.2 判据逐条

| 判据 | B_frb10_fe6 | C_frb25_fe4 |
| --- | --- | --- |
| ① 中位 `frugivory_frac` ≥ 0.20 | **达成** 0.2154（逐种子 4/6） | **达成** 0.2191（逐种子 4/6） |
| ② `carnivore_frac` ≥ 0.05 | **达成** 6/6，中位 0.1414 | **达成** 6/6，中位 0.1512 |
| ③ `population` ∈ 同种子基线 [0.7,1.3]× | **未达成** 5/6（s2 为 1.492） | **达成** 6/6（最大 1.240） |
| ④ `death_thirst_frac` ≤ 基线 +5pp | **未达成** 4/6（s3 +7.57、s4 +6.92） | **未达成** 3/6（s1 +7.90、s3 +7.63、s4 +9.98） |
| 四条同时满足的种子数 | 1/6（仅 s0） | 2/6（s0、s5） |

**[本世界实测] 判决：B 与 C 均不通过。** 按 §6 的失败判据原文——「达到了但破护栏」——
**「加厚果层」这条路在当前实现下被证伪**。

### 7.3 配对 Wilcoxon（双侧，n=6，未做 Bonferroni，全部报出）

| 指标 | B−A 差 [bootstrap CI] | p | C−A 差 [CI] | p |
| --- | --- | --- | --- | --- |
| frugivory_frac | +0.1955 [+0.169,+0.220] | **0.031**(地板) | +0.2276 [+0.203,+0.255] | **0.031**(地板) |
| forest_frac | +0.0310 [+0.021,+0.039] | **0.031**(地板) | +0.0321 [+0.018,+0.047] | **0.031**(地板) |
| population | +203.8 [+54,+430] | 0.0625 | +210.5 [+55,+348] | 0.0938 |
| carnivore_frac | +0.0135 [−0.062,+0.075] | 0.4375 | +0.0235 [−0.023,+0.074] | 0.8438 |
| death_thirst_frac | +0.0087 [−0.039,+0.053] | 0.8438 | +0.0252 [−0.030,+0.073] | 0.5625 |

C vs B：frugivory +0.032 (p=0.0625)、渴死 +0.017 (p=0.219)，其余三项 p=1.0。
**只有 `frugivory_frac` 与 `forest_frac` 的臂间差值超出臂内 SD**，其余全部落在噪声里。
`carnivore_frac` 的臂内 CV 高达 0.29–0.48——正是 `CLAUDE.md` 警告的近阈值噪声。

### 7.4 我在 §6 写的「种群往林冠移」被零模型大幅削弱

`explorations/20260802-fruit-flux/forest_null.py` 从 `terrain.build(Config())` 算出四个零模型：
全格均匀 0.1405、可居住格均匀 0.1786、**按草层承载力加权 0.2190**、按果层承载力加权 0.6123。

**[本世界实测]** 基线 `forest_frac = 0.2073` 实际**低于**按草层加权的零模型 0.2190；
处理臂 0.2384 / 0.2394 只是**刚刚越过**它。而若种群真按果实分布走，该值应趋向 0.6123。
**结论：臂间移动是真的（6/6 同向、p=0.031 地板），但相对零模型的绝对量级几乎可以忽略。**
§6 那句「种群确实往林冠移」**是过度解读，此处更正**——这正是 `conventions.md` §7
「空间指标先算零假设」存在的理由，我在 §6 违反了它。
（并且这仍是伪重复：6 个种子共享同一张地图，结论只对这一条河系成立。）

### 7.5 渴死破在哪：代码层面已定位，且方向与真实生态相反

**[对应]** `dynamics.graze:143` 返回水 `= gain × forage_water_frac`，而草的
`gain = biomass × eat_efficiency`（无能量系数，`eat_efficiency=1.0`）→ **草每卡路里给 0.10 水**。
`dynamics.eat_fruit:174` 返回水 `= taken × forage_water_frac`，用的是**生物量**，
而 `gain = taken × fruit_energy × eat_efficiency` → **果实每卡路里只给 `0.10 / fruit_energy` 水**。

于是 `fruit_energy=4.0` 时果实每卡路里的水只有草的 **1/4**，`fe=6.0` 时只有 1/6。
把两成热量挪到果实，就等于砍掉近两成的觅食水补贴——**渴死护栏破在这里，不是玄学**。
死因分解自洽：两臂饿死都降（−0.020/−0.018），压力转移到渴死。

**[现实] 而这与真实生态相反。** 果实含水量典型 80–90%，高于草料的 60–80%；
`graze` 自己的注释就引了「Real herbivores draw much of their intake preformed」，
`biology.md` 有「预成水」一节。**当前实现让果实比草更干，是一处与文献相反的建模选择**
（草的水挂能量、果实的水挂生物量，两处口径本就不一致）。

**→ 这直接生成 R1c（§8）：给果实它应有的预成水，再看渴死护栏是否可修。**
若可修，则 R1b 的失败是口径问题而非果层路线的本质代价；若不可修，果层路线证伪成立。

---

## 8. R1c 果实的预成水（2026-08-03，run_id: 20260803-fruit-water）

- **假设**: R1b 的渴死护栏破裂是**水口径问题**而非果层路线的本质代价。当前果实每卡路里
  只给 `0.10 / fruit_energy` 的水（草是 0.10），把热量挪去果实等于砍水补贴。给果实
  它应有的预成水后，`frugivory_frac ≥ 0.20` 与渴死护栏可以同时成立。
- **[对应] 机制**: 新增 `fruit_water_frac`，`dynamics.eat_fruit` 的取水改用它
  （原为共用 `forage_water_frac`）。**默认值 0.10 与 `forage_water_frac` 默认值相同，
  故默认世界逐位不变、golden 不动。**
- **水中性剂量的算术**: 要让果实每卡路里的水等于草，需
  `fruit_water_frac = forage_water_frac × fruit_energy × eat_efficiency`
  = `0.10 × 4.0 × 1.0` = **0.40**。这不是调参调出来的数，是等式解出来的。
  **[现实]** 而真实果实含水 80–90%、高于草料的 60–80%，所以「水中性」其实仍偏保守，
  故另设一个 0.60 的富水臂。
- **成功判据**（先写后跑，与 R1b 同口径、同严格度，护栏一律**逐种子**）:
  存在一个臂在 6 配对种子上满足 —— 中位 `frugivory_frac ≥ 0.20`
  **且** `carnivore_frac ≥ 0.05` 6/6
  **且** `population` ∈ 同种子基线 [0.7,1.3]× **6/6**
  **且** `death_thirst_frac` ≤ 同种子基线 +5pp **6/6**。
- **失败判据**: 没有任何臂四条全过 6/6 → **「加厚果层造第二生态位」整条路线证伪**，
  S 链在入口断掉，backlog 跳到空间轴（R3 改为在薄果层下做），并把这条负结果写进
  `docs/experiments.md` 失败实验档案。
- **臂**（6 配对种子 0–5，20000 步）:
  - `A_base` 默认
  - `C_fwf10` = `frb=0.25 fe=4.0 fwf=0.10`（R1b 的失败臂，重跑作同 commit 对照）
  - `D_fwf40` = `frb=0.25 fe=4.0 fwf=0.40`（水中性）
  - `E_fwf60` = `frb=0.25 fe=4.0 fwf=0.60`（富水，贴近真实果实含水量）
- **预标风险**: 抬 `fruit_water_frac` 是往世界里加水，可能重演 `water_system.md` 记过的
  副作用——每个有效水杠杆都让种群涨 27–72%、捕食者占比升到 33–41%。**`population` 与
  `carnivore_frac` 两条护栏就是防这个的，不得放宽。**
- **git hash**: `54a3259`（provenance；分析时 HEAD 已到 `67564f1`，但
  `git diff 54a3259 HEAD -- underworld/ scripts/` 为空，无跨 commit 混淆）

### 8.1 判决：三臂全部不通过，§8 失败判据触发

24 个 run，产物 `outputs/20260803-fruit-water/`，
分析脚本 `explorations/20260803-fruit-water/analyze.py`（全量 stdout 482 行在其 `output/`）。

| 臂 | 中位 frugivory ≥0.20 | carn ≥0.05 | pop ∈[0.7,1.3]× | 渴死 ≤+5pp | 判决 |
| --- | --- | --- | --- | --- | --- |
| C_fwf10 | 达成 0.2116 | 达成 6/6 | 达成 6/6 | **未达成 0/6** | 不通过 |
| D_fwf40（水中性） | 达成 0.2484 | 达成 6/6 | 达成 6/6 | **未达成 3/6** | 不通过 |
| E_fwf60（富水） | 达成 0.2314 | 达成 6/6 | **未达成 4/6** | **未达成 4/6** | 不通过 |

**[本世界实测]** 剂量在**均值口径上**确实单调修复渴死（相对基线 +7.73 → +3.58 → −0.63 pp），
但**逐种子口径下没有任何剂量修好护栏**。s3 与 s5 在三档全破且随剂量**加重**。

**口径冲突必须两边都报**：唯一能归因到 `fruit_water_frac` 的单变量对照是 D−C 与 E−C，
它们的渴死 bootstrap CI 都不含 0（−0.0415 [−0.063,−0.016]、−0.0836 [−0.144,−0.022]），
**但配对符号秩 p = 0.0625 / 0.1562，未达 0.05**（只有 5/6 与 4/6 同向）。
分歧来自 s3/s5 的反向。**只引 CI 不引 p 就会把这条讲成成立，那是选择性报告。**

### 8.2 我的诊断是错的：渴死破裂是**承载力问题**，不是水口径问题

这是本回合真正的产出，而且它推翻了 §7.5 那个看起来很干净的机制解释。

**[本世界实测]** 种群比值与渴死增量高度共变：Spearman ρ = **+0.9030**（n=18 配对点，
非独立）；只看 D/E 内部 ρ=+0.8252、p=0.0010。
**而 `mean_water` 在三个臂对基线全部无显著变化**（D−A p=1.0000、E−A p=0.8438）。

**加进世界的水，被多出来的身体吃掉了。** 果层供的能量抬高了承载力，种群随之超调
（C/D/E 平均 +16~22%，E 的 s3 到 1.449×、s5 到 1.351×），更多个体被推到离水更远处，
渴死于是随种群涨——**与每卡路里给多少水几乎无关**。E 臂的方差爆开正是这个的指纹：
{s0,s1,s2,s4} 渴死改善且种群持平，{s3,s5} 渴死恶化且种群 1.35–1.45×，**按种群超调二分**。

**[本世界实测] 预标风险部分兑现**：`water_system.md` 记的 +27–72% / carn 33–41% 在**均值**上
没有重演（+16–22% / carn 0.127–0.184），但**尾部兑现了**——E 的 s3/s5 破种群护栏、
s4 的 `carnivore_frac`=0.3176 已触到 0.33 下沿。

### 8.3 §8 失败判据执行：果层增厚路线（as specified）证伪

按 §8 原文——「没有任何臂四条全过 6/6 → 加厚果层造第二生态位整条路线证伪」——**执行**。
负结果同时写进 `docs/experiments.md`。

**不采中位读法**：中位口径下 D（+3.86pp）与 E（−4.95pp）都会「通过」。
**正因为它能让结论翻正，才更不能在看到数据之后去选它——那等于把护栏删掉。**

---

## 10. 三个回合合起来的真结论：水限世界里，加食物只会变成更多身体

R1 / R1b / R1c 是三次独立的失败，但指向同一堵墙，值得单独立一节，因为它改写纲领。

**[本世界实测]** 果层供能份额可以从 0.1% 一路做到 25%（这一点毫无疑问、6/6、p=0.031 地板）。
但每一次，多出来的能量都**转化成更多的身体，而不是另一种活法**：种群 +16~22%，
`mean_water` 不动，渴死随种群走（ρ=+0.90）。

这与两份已归档的判决是同一堵墙：
- `herbivore_overpopulation.md` §6：四条降密度杠杆全部失败，根因水限 + 密度尺度不变。
- `water_system.md`：**每一个有效水杠杆都让种群涨 27–72%**、捕食者占比升到 33–41%。

**合起来的命题（本纲领的核心修订）：在一个承载力由水锁死的世界里，
「增加食物供给」这条路造不出第二个生态位——它只会等比例地造出更多同一种生物。**
第二个生态位必须来自**不增加承载力**的轴：

- **(i) 重新分配现有生产力**：把总植物通量保持不变、只改变它在草/果两层之间的**构成**。
  这与 R1b/R1c 测的「加厚」是**不同的假设**，不是它的救援臂——加厚测的是「更多食物」，
  再分配测的是「同样多的食物，两种吃法」。→ **R3（§11）**
- **(ii) 分割空间或时间**：同一份资源、不同的取用地点或时段，零新增能量。
  → 原 backlog 的空间轴，`day_night.md` 的相位分箱探针可复用。

**纲领 §1.1 的排序据此更新**：资源轴不再是「加厚果层」，而是「等量再分配」；
「加厚」已证伪，不要再试。

---

## 9. R2 厚果层下的资源分割（2026-08-03，run_id: 20260803-partition）

**前置**：R1c 若无通过臂，则本回合的「厚果层」用 §7 的 C 臂配置并**明标护栏已破**——
在破护栏的世界里测分化仍有信息量（它回答「即使给了第二个饭碗，分化会不会发生」），
但结论不得用于支持「默认开」。

### 9.1 为什么这一轮要交叉 `forage_tradeoff` 强度

§6 那条算术更正的直接后果：频率依赖下专精者亚种群比例 `q ≈ f`，**任何果层份额都撑得起
成比例的亚种群**，所以「份额不够」从来不是分化不发生的充分解释。真正卡住的是
**选择梯度 vs 基因流**——而选择梯度由 `forage_tradeoff` 的强度决定，不由 `f` 决定。
`§9.5` 那次只开了 `forage_tradeoff=0.5` 一档，**把强度当常数、只动份额**，因此它的
根因诊断（「无生态位可专精」）是不完整的。本轮把两者交叉。

### 9.2 设计：2 × 3 因子，6 配对种子

| 因子 | 水平 |
| --- | --- |
| 果层 | **薄**（默认）/ **厚**（R1c 通过臂，或 §7 的 C 臂） |
| `forage_tradeoff` | **0.0**（基因编译期断开＝纯漂变对照）/ **0.5** / **1.0** |

6 臂 × 6 配对种子 = 36 run，20000 步，6 并发（约 30 分钟）。
测量一律用 `scripts/probe_trait_dist.py --trait forage_pref --lineage herb`
（一次 run 同时给形状统计与护栏，无需再跑 `run_headless`）。

### 9.3 可证伪预测（先写后跑）

- **P1 复制**：**薄**果层下，`forage_tradeoff=0.5` 的 `sd` **低于** `0.0` 对照
  ——即复制 `feasibility.md §9.5` 的方差反向塌缩（那次测得 −32.6%）。
  **若不复制，则 §9.5 本身存疑**，要先查清楚再谈别的。
- **P2 主判据**：**厚**果层下，`forage_tradeoff=0.5` 的 `sd` **高于** `0.0` 对照，
  且 `blrt_lr_per_n` 同向更高；两项各 ≥5/6 同向、配对 Wilcoxon p≤0.05。
- **P3 剂量**：厚果层下 `tradeoff=1.0` 的 `sd` 与 `blrt_lr_per_n` **高于** `0.5`
  （选择强度是那个真正的旋钮——这是 §6 算术更正的直接检验）。
- **护栏**：`carnivore_frac ≥ 0.05` 且 `min_pop ≥ 1` 逐种子 6/6（本轮不设种群区间护栏，
  因为厚果层臂的种群偏移已在 R1b/R1c 量过；这里只防灭绝）。

### 9.4 失败判据与它意味着什么

**P2 不成立**（厚果层 + 强权衡仍不分化）→ **资源分割在本世界不由资源结构决定，
而是被基因流压平**。那么下一步不是继续加厚果层，而是**生殖隔离**——把
`assortative_mating` 从只按 `diet` 排序扩展到也按 `forage_pref` 排序。那是更大的改动，
且会碰到 `reproduction._assortative_mate`，**属于要用户拍板的岔路**（写进 HANDOFF PENDING，
不擅自开跑）。

**P1 不复制**（薄果层下方差不塌缩）→ 先停下来查 §9.5，不要在一个可能有问题的先例上继续搭。

- **git hash**: `9296fd7`（`outputs/20260803-partition/provenance.txt`）

### 9.5 先记一处我自己的预注册偏离

§9.2 写的因子是 `forage_tradeoff` **三档 0.0 / 0.5 / 1.0**，实跑**只开了 0.0 与 1.0**
（为省 GPU 砍掉中间档），**当时没记录，是分析 agent 核 `overrides` 时抓到的**。后果两条：
- P2 原文写的是 0.5，本批测的是 **1.0＝更强的选择**，所以「1.0 都不分化」是对 P2 的
  **更强证否**，方向上不利于我自己的假设，可以采信；
- **P3 剂量项（0.5 vs 1.0）本批无法检验**，缺臂。要补就是 18 个 run。

### 9.6 判决：**P2 不成立，而且失败方式不是我预设的那一种**

36 个 run（2 臂 × 6 种子 × **3 重复**），格均值上做 6 配对检验。
脚本 `explorations/20260803-partition/analyze.py`。两臂 `overrides` **只差 `forage_tradeoff`**，
归因干净。

| 指标 | P（tradeoff=0.0，中性漂变） | Q（tradeoff=1.0） | 差 [bootstrap CI] | 配对 Wilcoxon | 效应/噪声 |
| --- | --- | --- | --- | --- | --- |
| **`sd`** | 0.09508 | **0.05866** | **−0.03643** [−0.0629,−0.0145] | **p=0.031**（地板），**6/6 反向** | **−1.42** |
| `blrt_lr_per_n` | 0.5799 | 0.2432 | −0.3367 [−0.671,−0.015] | p=0.156，5/6 | −0.94（**落在噪声内，读作 NULL**） |
| `bimodality_coef` | 0.5864 | 0.4767 | −0.1097 | p=0.156 | — |
| `mean` | 0.51119 | 0.51159 | +0.0004 | p=0.688 | — |

**[本世界实测] `forage_pref` 的方差不但没变宽，反而比中性漂变对照窄 38.3%（6/6 同向）。**
护栏两臂 6/6 全过；`population` / `n` / `death_thirst_frac` 两臂无差
（p=0.56 / 0.56 / 0.44）——**这是纯性状分布的效应，不是靠改变种群规模达成的**。

**合并直方图（每臂 18 run 逐桶求和）：两臂都是单峰、峰位都在 0.5125，Q 明显更窄。**
P 跨 [0.21, 0.86] 且右肩厚重（那是**厚尾，不是第二个峰**）；Q 跨 [0.24, 0.74]，右尾被完全削掉。
Q 在 0.24 附近有一个隔着真空的小簇，看着像次群——**但 100% 来自单个 run
（`Q_tradeoff1_s1_r3.log`），其余 17 个 Q run 在该区间全为 0。不是可复现的次群。**

### 9.7 这推翻了 §9.4 预设的失败机制（岔路的前提没了）

§9.4 写的失败子句是：「P2 不成立 → 分化被**基因流**压平 → 下一步是生殖隔离
（`assortative_mating` 扩到按 `forage_pref` 排序）」。**这个推理链断在第一环。**

**[本世界实测]** 基因流只能把处理臂压回**等于**中性漂变（Q≈P），**它没有理由把方差压到
低于中性**。实测 Q **显著低于** P，说明存在**主动的稳定化选择**把分布收向中间型；
`mean` 两臂完全无差（0.5112 vs 0.5116）——**「中间最优 + 稳定化」的标准签名**。

**→ 生殖隔离对抗的是基因流，治不了稳定化选择。§9.4 那条岔路的前提已被数据推翻，
不该再作为下一步候选交给用户。**

### 9.8 同时证否了 §9.5（feasibility.md）当年的归因

`feasibility.md §9.5` 测到方差反向塌缩 −32.6%，归因写的是「果层只有草层的 0.07%、
**没有第二个饭碗可专精**」——本纲领 §1 的表格也照抄了这条。

**[本世界实测] 本轮给了 28% 的真饭碗 + 最大选择强度，塌缩反而更深（−38.3%）。
所以那个归因是错的：塌缩的原因不是「没有饭碗」，是「中间型最优」。**
（复现本身很干净：−32.6% → −38.3%，两个独立配置下同向同量级。）

### 9.9 为什么中间型会最优 [提案，非结论]

**未测量适应度曲面**，以下是解释不是结论：果层的空间位置由
`terrain.py:207` 的 `fruit_capacity = fruit_max × patch × forest² × (1−rock)` 决定——
**果实长在森林里，而森林是中海拔近水带，正是所有个体本来就待的地方**。
两种资源在空间上**交织**而非分隔，于是一个个体在一生中会反复遇到两者，
**「两样都能吃」严格优于「只精一样」**。

专精只有在个体**大部分时间只遇得到一种资源**时才划算。
**→ 资源分割需要的不是「第二个饭碗」，而是「两个饭碗离得够远」。**
这恰好就是用户那句「**局部的**多物种生态箱」的字面含义，也把 backlog 指回**空间轴**：
要么把果层挪出森林带（改 `fruit_capacity` 的地形门控），要么走时间轴（昼夜错峰取食）。
**两者都会改默认世界行为，属于要用户拍板的岔路。**

---

## 11. R3 等量再分配：同样多的食物，两种吃法（2026-08-03，run_id: 20260803-repartition）

**这不是 R1 的救援臂。** R1b/R1c 测的是「**更多**食物能不能造生态位」——已证伪。
R3 测的是不同的命题：「**同样多**的食物，分成两层供应，能不能造生态位」。
区别是可操作的：R3 **要求总植物通量守恒**，这正是 §10 认定的那条约束。

### 11.1 为什么 R1c 的机制修复现在才派上用场

`fruit_water_frac` 在 R1c 里没能救回护栏（因为破的是承载力不是水口径），
但它是 R3 的**前置条件**：草的水挂能量、果实的水在解耦前只有草的 1/`fruit_energy`，
所以「砍草补果」在旧实现下必然连带砍掉觅食水补贴（探针一 q5 臂渴死升到 52.5% 就是这个）。
水中性剂量 `fwf = 0.10 × fruit_energy = 0.40` 让两层每卡路里供水相等，
**砍草补果才第一次成为一个干净的替代实验**，而不是偷偷同时动了水。

### 11.2 假设与判据（先写后跑）

- **假设**: 存在一组参数使 **总植物通量 `graze_gain + fruit_gain` 守恒**（落在同种子基线的
  [0.90, 1.10] 倍内）而 `frugivory_frac ≥ 0.20`，且 R1b/R1c 那四条护栏**逐种子 6/6 全过**。
- **成功判据**（比 R1c 多一条守恒约束，其余不放宽）:
  中位 `frugivory_frac ≥ 0.20`
  **且** 总植物通量 ∈ 同种子基线 [0.90,1.10]× **6/6**
  **且** `carnivore_frac ≥ 0.05` **6/6**
  **且** `population` ∈ 同种子基线 [0.7,1.3]× **6/6**
  **且** `death_thirst_frac` ≤ 同种子基线 +5pp **6/6**。
- **失败判据**: 无臂全过 → **资源轴整条关闭**（加厚已证伪、再分配也不行），
  backlog 只剩空间/时间轴，写进 `experiments.md` §5 作续判。
- **主旋钮**: 果侧 `fruit_regrow_baseline=0.25 fruit_energy=4.0 fruit_water_frac=0.40`（D 臂配置）
  固定；草侧用 `regrow_baseline`（必要时加 `plant_max`）下调来抵消。
- **先跑定标探针**（单种子、非结论）找抵消剂量，再跑 6 配对种子判决。
- **预标风险**: 砍草会压捕食者（草→食草→食肉的底座变薄），`carnivore_frac` 护栏就是防这个；
  `config.py` 记过 `plant_max=1.2` 会更快饿死捕食者，故优先动 `regrow_baseline` 而非 `plant_max`。
- **git hash**: 见 `outputs/20260803-repartition/verdict/provenance.txt`

### 11.3 抵消剂量探针（单种子，**非结论**）

出处 `outputs/20260803-repartition/probe/*.log`。果侧固定在 D 臂配置，草侧下调。
seed 0 的基线总植物通量 = **263.4**，守恒窗 [237.0, 289.7]。

| 臂 | grassF | fruitF | totalF | tot/base | frugivory% | pop/b | carn% | Δthirst | 守恒 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| t0 基线 | 263.0 | 0.4 | 263.4 | 1.000 | 0.13 | 1.000 | 7.9 | +0.00 | — |
| t1 grb=0.012 | 220.6 | 100.5 | 321.1 | 1.219 | 31.30 | 1.136 | 10.9 | +7.03 | ✗ |
| t2 grb=0.010 | 170.0 | 44.5 | 214.5 | 0.814 | 20.74 | 0.793 | 24.4 | −7.86 | ✗ |
| t3 grb=0.008 | 166.0 | 39.3 | 205.3 | 0.780 | 19.13 | 0.774 | 21.5 | −0.84 | ✗ |
| t4 grb=0.006 | 143.6 | 106.5 | 250.1 | 0.950 | 42.57 | 1.165 | **4.5** | +8.29 | ✓但破两条护栏 |
| **t5 grb=0.010 + plant_max=2.0** | 183.3 | 77.0 | 260.4 | **0.989** | **29.59** | 0.930 | 17.0 | **+3.46** | ✓ **四条全干净** |
| t6 grb=0.008 + regrow_rate=0.05 | 142.2 | 59.0 | 201.2 | 0.764 | 29.33 | 0.897 | 12.8 | +2.35 | ✗ |

**[本世界实测] 抵消剂量对草侧旋钮是非单调的**：`regrow_baseline` 0.012→0.010→0.008→0.006
给出的 fruitF 是 100.5 → 44.5 → 39.3 → 106.5。这不是噪声（幅度太大），是**生态反馈**：
砍草 → 种群变化 → 对果层的消费压力变化 → 果层现存量变化 → logistic 项跟着变。
**两层通过共同的消费者耦合，所以「按公式算出该砍多少草」不成立，只能实测扫。**

正式判决取 **t5** 为主臂，另加一个邻近臂 `G`（`grb=0.012 + plant_max=2.0`）对冲单种子噪声。

### 11.4 判决：**无法判定**（judged undecidable），因为判据的容差窄于复现噪声

18 个 run，产物 `outputs/20260803-repartition/verdict/`，
脚本 `explorations/20260803-repartition/analyze.py` + `terrain_null.py`。

| 判据 | F_grb010_pm20 | G_grb012_pm20 |
| --- | --- | --- |
| C1 中位 frugivory ≥0.20 | 达成 0.2781（6/6） | 达成 0.2981（6/6） |
| C2 总通量 ∈[0.90,1.10]× | **2/6** | **2/6** |
| C3 carn ≥0.05 | 达成 6/6（min 0.161） | 达成 6/6（min 0.074） |
| C4 pop ∈[0.7,1.3]× | 达成 6/6（0.799–1.007） | 达成 6/6（0.949–1.281） |
| C5 渴死 ≤ base+5pp | **4/6**（s4 是 +5.04pp，超线 0.04pp） | **2/6**（最差 +14.47pp） |

**字面上两臂都不通过。但这个判决不能采信**，理由在下面，而且它不是为了救结论——
**护栏宽度一个字没动，两臂也都没有被改判为通过。**

### 11.5 为什么不能采信：判据容差 ≈ 1 个复现噪声 SD

分析中派生出一个本轮最重要的量：**同种子、同配置、同代码重跑的散度**。
`A_base seed 0` 重跑 6 次（`outputs/20260803-repartition/replicate/`，主控已独立复核原始 JSON）：

| 指标 | **同种子重跑 SD** | 跨种子 SD（n=6） | 倍数 |
| --- | --- | --- | --- |
| `population` | **146.6**（CV 0.082） | 39.2（CV 0.023） | **3.7×** |
| `carnivore_frac` | **0.0527**（同一 seed 跑出 0.068–0.237） | 0.0131（CV 0.079） | **4.0×** |
| `total_flux` | 19.1（CV 0.079） | 9.6（CV 0.039） | 2.0× |
| `death_thirst_frac` | 0.0428（4.28 pp） | 0.0350 | 1.2× |

代入两条失败的判据：
- **C2 的 ±10% 窗**：比值是处理/基线，分子分母各带一份噪声 → 比值 CV ≈ √2×0.079 = **0.112**，
  即 **±10% 窗 ≈ ±0.9 个复现 SD**。一个真效应为零的配置，逐种子 6/6 落窗内的概率约
  `0.63^6 ≈ 6%`。**C2 作为 6/6 逐种子护栏，与配置好坏无关地几乎必然失败。**
- **C5 的 +5pp 线**：复现 SD 就是 **4.28pp**，即容差 ≈ **1.2 个复现 SD**。零效应下每颗种子
  约 12% 概率自己撞线。

**所以 R3 记为「无法判定」，不是「不通过」。** 这与事后放宽判据的区别是可操作的：
判据数值一个都没改，改的是**测量**——要判它必须先把噪声压下去（每格重复 k 次取均值，
噪声降 √k；或用确定性 XLA 算子）。测量修好之前，R3 的通过/不通过都没有信息量。

**§11.2 的失败判据（「无臂全过 → 资源轴整条关闭」）因此不执行**：它的隐含前提是判据可测，
而该前提被违反。资源轴**挂起**，不宣布关闭，也不宣布成功。

### 11.6 仍然可信的三件事（效应量远超复现噪声）

用**复现 SD**（不是跨种子 SD）当尺子，只有两个指标的臂间差值真的超出噪声：

1. **[本世界实测] `frugivory_frac`**：F +0.269、G +0.286，6/6 同向，p=0.031（地板），
   效应是复现噪声的 **500 倍以上**。**「同样多的食物、两种吃法」在供给端确实做到了。**
2. **[本世界实测] `forest_frac`**：F +0.066、G +0.051，6/6，p=0.031，约 5–7 个复现 SD。
   但**必须对着零模型读**：基线 0.2101 恰好坐在「按草层承载力加权」的零模型 0.2190 上；
   F 0.2756 / G 0.2610 只走完「草加权 0.219 → 果加权 0.612」全程的 **14.4% / 10.7%**。
   **种群向林冠挪了一点，远没有搬到果层所在地。**（且伪重复：同一张地图。）
3. **[本世界实测] 超调减弱但未消失**：pop 比值与渴死 pp 的 Spearman ρ 从 R1c 的 **+0.9030**
   降到 **+0.6154**（n=12, p=0.033）。F 平均 0.894（**欠**调），G 平均 1.144（仍超调）——
   两臂分列超调轴两侧。**守恒约束把种群拉回了一半，但渴死代价没跟着消失**（+4.21pp，
   甚至略高于 R1c 的 +3.56pp）。

### 11.7 一处必须更正的过度解读（分析 agent 的第 5 条结论）

分析报告写「果实吃到 27–29% 份额却没分化出果实专食者（`forage_pref_std` 纹丝不动）」。
**这条不成立**：R3 三个臂的 `overrides` 里**没有 `forage_tradeoff`**，它停在默认 **0.0**，
即 `dynamics.graze` / `eat_fruit` 里那个分支被编译期切掉，**`forage_pref` 基因对世界毫无作用**。
基因不动是必然的，不是发现。**R3 在设计上就回答不了分化问题**——那是 §9 R2 的事。
（这也再次说明为什么中性对照必须显式跑：`forage_pref_std = 0.055` 在这里就是纯漂变的读数。）

### 11.8 预标风险：方向与预测相反，但落在噪声内

预标的是「砍草会压捕食者」。实测**反向**：砍得更狠的 F 捕食者反而更高
（0.2156 vs 基线 0.1648），砍得轻的 G 反而更低（0.1394）——跟着种群**反向**走
（F 欠调→carn 高，G 超调→carn 低）。但 `carnivore_frac` 的复现 CV 是 **0.376**，
两个差值只有 0.88 / 0.44 个复现 SD，**都在噪声里，两个方向都不能声称**。
C3 护栏本身 6/6 过，无臂逼近 0.05。

### 9.10 P3 剂量项补齐（2026-08-03）：**单调，没有分化窗口**

§9.5 记的偏离已补上——加跑 `forage_tradeoff=0.5`（M 臂，6 种子 × 3 重复 = 18 run），
与 R2 的 P(0.0) / Q(1.0) 同配置同目录，三臂 `overrides` 逐字段核过**只差这一项**。
脚本 `explorations/20260803-partition/analyze_p3.py`。

| `sd`（格均值） | P(0.0) | **M(0.5)** | Q(1.0) |
| --- | --- | --- | --- |
| 值 | 0.09508 | **0.07025** | 0.05866 |
| 相对 P | — | −26.1% | −38.3% |

**[本世界实测] 单调。`M > P`（歧化方向）的种子数 = 0/6**，`bimodality_coefficient` 同样 0/6，
`blrt_lr_per_n` 1/6。**不存在「弱权衡歧化、强权衡稳定化」的分化窗口——R2 没有跨过头。**
三臂 Friedman `sd` p=0.0094。M 臂直方图也没有可复现的双峰（低值簇/谷底比 0.18，
而 Q 那个比值 2.37 的簇**再次确认 100% 来自单个 run**，是伪影）。

**[本世界实测] 挤压在半剂量已完成 68.2%**，且 **M 与 Q 在 n=6 下不可分**（`sd` p=0.4375）。

**三条必须一起写下的限制**（分析 agent 主动标的，不是我事后补的）：
1. **`M−P` 的 `p=0.03125` 不稳**：它同时是 n=6 的地板值、在**中位数**聚合口径下失守
   （p=0.09375），而且配对差紧得反常（实测差 SD 0.00627 vs 复跑噪声预测 0.02767，
   卡方下尾 p=0.0016）。**「M<P 方向 6/6」可信，「p≤0.05」不可信。**
   对照之下 `Q−P` 对口径不敏感，稳。
2. **只测了 0/0.5/1 三点。** 若分化窗口很窄（比如落在 0.1–0.3），本设计会直接跨过去。
   **「单调」这个结论只在这三点上成立。**
3. **伪重复**：果层由 forest 派生、forest 由高程派生，所以「适应度曲面单峰」只对
   **这一套河系/森林布局**成立。

**[本世界实测] 又一次印证方差那把尺子**：M 臂**格内**（同种子复跑）SD 0.0303
**大于**格均值的跨种子 SD 0.0216（比 1.40）。主导方差仍是复现噪声而非创始者，
与 `run_to_run_variance.md` 的 ICC 结构一致。分析 agent 据此给了一条有用的预算建议：
**加种子和加复跑几乎等价，但加种子还顺带减少伪重复——预算优先投种子数。**

护栏三臂全部 6/6（`carnivore_frac` ≥ 0.05、`min_pop` ≥ 1、无灭绝）。

### 9.11 窄窗口收口（五点曲线，90 run）：**没有高于漂变的窗口**，但曲线**不单调**

补上 `forage_tradeoff = 0.125` 与 `0.25` 两点（各 6 种子 × 3 重复），与 P/M/Q 同配置同目录，
五臂 `overrides` 逐字段核过只差这一项，跨三个 commit 但
`git diff -- underworld/ scripts/` 为空。脚本 `explorations/20260803-partition/analyze_curve.py`。

| `sd`（格均值） | P **0.0** | L **0.125** | N **0.25** | M **0.5** | Q **1.0** |
| --- | --- | --- | --- | --- | --- |
| 值 | 0.09508 | **0.07953** | **0.09294** | 0.07025 | 0.05866 |
| vs P | — | −0.01555，**6/6**，p=0.031（地板） | −0.00214，**4/6 反向**，**p=1.00**，CI 含 0 | −0.02483，6/6，p=0.031 | −0.03643，6/6，p=0.031 |

**[本世界实测] 预注册判据未被触发**：`L>P` 的种子数 **0/6**，`N>P` 4/6 但整体打平。
**五个剂量点没有任何一点的 `sd` 高于中性漂变对照 → 分化窗口不存在。**

**两处措辞必须写准，否则会被自己的数据打脸**（分析 agent 主动纠的）：

1. **不能写「五点单调」。** `N−L = +0.01341`，**5/6 同向、CI [+0.005,+0.021] 不含 0**（p=0.0625）——
   0.25 相对 0.125 有一个真实的抬头。正确措辞是「**没有高于漂变的窗口**」，不是「单调挤压」。
2. **不能写「N 比漂变更窄」。** N−P 的负号**几乎全部来自单个离群 run**
   （`P_tradeoff0_s0_r3.log` 的 `sd=0.19838`，是全 90 run 最大值）；剔除 seed 0 后 N−P **翻正**
   为 +0.00440，换成格中位数口径也翻正。**N(0.25) 是 NULL，不是更窄。**

**[本世界实测] 这个「不存在」是有边界的**：本设计对 `sd` 的 80% 功效最小可检出效应是
**+24.2%（L 对比）/ +40.9%（N 对比）**。**所以排除掉的只是「幅度 ≥ 24–41% 的窗口」，
更窄的窗口本设计看不见。** 而 `blrt_lr_per_n` 的 MDE 已达 +104%——**这个指标本身分辨率不够**，
不值得再往它上面加种子。

**[本世界实测] 双峰在任何一臂都没出现**：`bimodality_coefficient > 0.555` 的 run 数
L=10/18、N=10/18，与中性对照 P 的 **10/18 完全相同**（M=5/18、Q=3/18）。
Q 臂那个唯一「真凹陷」的小簇**第三次确认 100% 来自 `Q_tradeoff1_s1_r3.log`**，是伪影。

**[本世界实测] 权衡强度不碰生态护栏**：五臂 Friedman 对 `carnivore_frac` p=0.83、
`population` p=0.78、`min_pop` p=0.059；30 个格全部 `carn_frac ≥ 0.05` 与 `min_pop ≥ 1`。
唯一 `carn_frac < 0.05` 的 run 落在**中性对照臂**。
真正被权衡改动的是行为结构——`frugivory_frac` 随强度下降（L−P、N−P 的 CI 均不含 0）。

**又一次印证噪声那把尺子**：**连最强的 L−P，效应/噪声比也只有 0.62（<1）。**
它拿到 6/6 是因为实测配对差 SD 只有噪声模型预测的 0.51 倍（池化格内 SD 被少数离群 run 抬高）。
**本项目在这个量级上做判决，靠的是符号一致性而不是效应量——这一点应当写在每个结论旁边。**

---

## 12. 【下一轮执行计划】把果层挪出共居带（用户已拍板，2026-08-03）

**先决条件：`docs/run_to_run_variance.md §7` 的新统计协议必须先落地**（12 种子 × 2 重复）。
否则本轮判决会重蹈 R3「判据容差 ≈ 1 个噪声 SD」的覆辙。

### 12.1 病根定位到了具体一行

§9.9 的诊断在代码里能指到确切位置。`terrain.py`：

```
water_prox = exp(-water_dist / forest_water_scale)      # 离水越近越大
forest     = elev_band * water_prox                      # 森林 = 中海拔 ∩ 近水
fruit_capacity = fruit_max * patch * forest**2 * (1-rock)
```

**果层的栖息地项是 `forest²`，而 `forest` 本身就是「离水近」做出来的。**
于是果实必然长在所有个体本来就挤着的那条河岸林带里——两种食物在空间上完全交织，
个体一生反复遇到两者，「两样都能吃」严格优于专精。**稳定化选择不是 bug，是这个几何的正确解。**

### 12.2 手术：给果层一个可调的「干旱」栖息地项

**[提案] 最小改动**，`terrain.py` 那一行改成两项的加权混合：

```
dry = smoothstep((water_dist - fruit_dry_d0) / (fruit_dry_d1 - fruit_dry_d0))
habitat = (1 - w) * forest**2 + w * dry          # w = cfg.fruit_dry_weight
fruit_capacity = fruit_max * patch * habitat * (1-rock)
```

- **`fruit_dry_weight` 默认 0.0 → `habitat` 恒等于 `forest²` → 世界逐位不变、golden 不动。**
- `w=1` 时果层完全由「离水多远」决定，与森林脱钩。
- `fruit_dry_d0/d1` 控制干旱带从哪里开始（单位是世界单位，参照
  `river_half_width=16`、`herb_water_dist` 基线约 10–25）。
- **必须补的单测**：`w=0` 时 `terrain.build` 的 `fruit_capacity` 与旧实现逐元素相等。

### 12.3 阶段与闸门（前一阶段不绿不许进下一阶段）

**A｜零成本先量重叠度（不写代码）。** 现在果层和种群到底重叠多少？
用已有产物算：取一次基线 run 的存活个体位置 → 落到 cell → 与 `terrain.fruit_capacity` 求交。
**产出一个基线「重叠度」数字**，后面所有分离效果都对着它读。脚本进 `explorations/`。
*没有这个数，「挪开了」就无从判定。*

**B｜落地旋钮（默认关、bit-exact）。** 按 §12.2 改 `terrain.py` + 加 `Config` 字段 + 单测 +
`check.py --full` 全绿 + **golden 必须没动**。提交。

**C｜单种子定标探针（约 6 臂，非结论）。** 扫 `fruit_dry_weight` × `fruit_dry_d0`。
**这一阶段的核心不是找「分离最大」，而是找「分离了但果实还吃得到」的那个点。**
三个读数一起看：
- 重叠度（A 的口径）——下去了没有；
- **`frugivory_frac`**——**如果果层挪到没人去的地方，这个数会塌回 0，那等于把果层删了，不是造生态位**；
- 护栏 `carnivore_frac` / `population` / `death_thirst_frac`——**预标风险：水**。

**这三者存在张力，很可能没有同时满足的点。若确实没有，本轮就此为负结果收口**
（写进 `experiments.md §5` 续判），**不要硬调**。

**D｜正式判决（新协议：12 种子 × 2 重复 × 2 臂 = 48 run，约 45 分钟）。**
两臂都用 C 选出的工作点，只差 `forage_tradeoff`：
- 对照 `forage_tradeoff=0.0`（基因编译期断开＝纯漂变）
- 处理 `forage_tradeoff=1.0`
测量用 `scripts/probe_trait_dist.py --trait forage_pref --lineage herb`（一次 run 同时出形状与护栏）。

**判据（先写后跑，与 §9.3 P2 同口径但换新协议）**：
处理臂 `sd` **高于**对照，≥9/12 种子同向，配对 Wilcoxon p≤0.05，
**且必报「效应量 ÷ √2·σ̂_W/√2」比值**（<1 标功效不足）。
参照系已经有了：同一指标在**共居**几何下是 **−38.3%**（Q vs P，§9.6）。
**所以本轮真正要看的是符号能不能翻正**，而不只是显著性。

**E｜跨地形复现（仅当 D 为正）。** 空间结论必须交叉
`ridge_wavenumber ∈ {1,2}` × `ridge_base_frac ∈ {0.4,0.5}`，否则只对这一条河系成立。

### 12.3.A A 阶段实施（2026-08-03，run_id: `20260803-overlapA`）

**先写口径再采数**——这一节在跑之前写好。A 阶段没有两臂、没有假设检验，但它决定了
C/D 拿什么数当「分离」的证据，**看到数字之后再挑主口径，等于给后面的判决留后门**。

- 脚本: `explorations/20260803-overlapA/measure_overlap.py`（+ `README.md`）
- 设计: 12 种子 × 2 重复（新协议），20000 步；个体密度取**后 25% run、每 200 步一帧**
  累加，不用末帧快照（单帧空间噪声远大于要测的效应）
- **两个臂，不是一个**（开跑后发现的必要修正，写在这里而不是事后解释）：
  - `base` = **默认配置**。第一个完成的 run 就暴露了问题：默认世界的
    `frugivory_frac ≈ 0.0013`——**果实几乎没有人吃**。
  - `niche` = **厚果层配置**，即 R2/P2/P3/P4 一直用的那套「第二生态位」参数
    （`fruit_regrow_baseline=0.25`、`fruit_energy=4.0`、`fruit_water_frac=0.40`、
    `regrow_baseline=0.010`、`plant_max=2.0`，出处 `outputs/20260803-partition/provenance.txt:8`），
    果层真供约 28% 能量。
  - **为什么必须补**：D 阶段的参照系是 §9.6 的 −38.3%，那个数就是在厚果层世界里测的；
    `forage_pref` 在默认世界里没有可作用的资源。基线量在默认世界、判决发生在厚果层世界，
    「重叠度下去了没有」就无从对比。
  - **顺带这本身就是 A 要回答的问题**：把果层从「没人吃」变成「供 28% 能量」，种群会不会
    往果层上挪？两臂的 `sel_ratio_water` 之差就是答案。**若两臂都 ≈1.0，说明共居是纯几何的，
    不是行为选择的结果**——那会直接改变 §12.5 的读法。
- **主口径（先定死）**: **`sel_ratio_water`** —— 按水距分层匹配的零模型下的选择比。
  它问的是「**扣掉『渴死把所有人钉在河边』这一条之后，种群还额外偏好果层吗**」。
  选它当主口径的理由：手术（§12.2）要动的正是「果层 ↔ 水」这个耦合，而 `sel_ratio_land`
  会把「两者都靠水」这件事整个算进重叠里，读不出手术做了什么。
- 次口径: `sel_ratio_land`、`frac_in_fruit`（各配 N-land / N-water 两个零模型）
- **形状哨兵**（§12.4 风险 3）: `fruit_cells_eff` / `fruit_cells_pos` / `fruit_cap_total`
  —— **「把果层挪开」和「把果层删掉」在选择比上长得一模一样**，只能靠这三个数加
  `frugivory_frac` 分辨。B 阶段之后立刻复查。
- **Schoener's D 报但不当主口径**：它同时惩罚「两个分布集中度不同」。种群沿河高度聚集、
  果层摊在 2000+ 格上，所以实测 D 会**低于**均匀零模型的 D，而同一批数据的选择比却 >1。
  D 在这里是「形状是否分开」的指标，不是重叠度。（探针实测：D 0.100 vs N-land 0.118，
  同时 `sel_ratio_land` = 1.36。**这是口径混杂，不是矛盾。**）

### 12.3.A' A 阶段判决（2026-08-03，48 run，判决由 `result-analyst` 出）

**一句话：共居是真的，而且在判决要发生的那个世界里是「行为性」的，不是纯几何的——
我预注册时写下的那个猜想被自己的数据推翻了。**

**[本世界实测] 两臂基线**（12 种子 × 2 重复，逐种子表见
`explorations/20260803-overlapA/analyze.py` 输出；每个数可追到
`outputs/20260803-overlapA/{arm}_s{seed}_r{rep}.log:8`）：

| 主口径 `sel_ratio_water` | `base`（默认） | `niche`（厚果层） |
| --- | --- | --- |
| 值（零模型 = 1.0） | **1.0089** | **3.7122** |
| 同向种子 | 8/12 | **12/12** |
| 配对 Wilcoxon 双侧 p | 0.15137 | **0.00049（n=12 地板）** |
| 95% bootstrap CI | [−0.0008, +0.0186] **含 0** | [+2.514, +2.948] |
| 效应/噪声 | +0.68 **<1，功效不足** | **+8.73** |
| `frugivory_frac` | 0.0011 | 0.2898 |

- **`base` 世界里，果层与种群的重叠是纯几何的。** `sel_ratio_land = 1.1269`
  （12/12、p=0.00049、比值 **+8.65**）说明种群确实坐在比随机陆地高 12.7% 的果层承载上，
  **但换成水距匹配零模型后差值落回 0**。两者之差就是「大家都被水钉在同一条带上」这一项。
  `frac_in_fruit` 甚至略低于水距零模型（−0.00045，10/12 为负，p=0.176）。
- **`niche` 世界里，重叠是行为性的，而且很强。** 扣掉水距之后仍是零模型的 **3.71 倍**。
- **把果层从「没人吃」变成「供约 29% 能量」，种群显著往果层上挪**：
  Δ`sel_ratio_water` = **+2.703（+268%）**，12/12，p=0.00049，比值 **+8.69**。

**[本世界实测] 预注册的一条猜想被证伪。** §12.3.A 写的是「若两臂都 ≈1.0，说明共居是纯几何的，
那会改变 §12.5 的读法」。**结果不是这样**：`base` ≈1.0 而 `niche` = 3.71。
**共居在默认世界是几何的，在厚果层世界是行为的。§12.5 的读法要按 `niche` 写。**

**[本世界实测] C/D 的基线固定为 `niche` 臂**，三条定量理由：
1. **`base` 没有可下降的量。** 它离零模型只有 +0.0089，而本设计 p≤0.05 口径的 MDE 是
   0.0173——「**可下降空间 ÷ MDE = 0.52**」，手术做对了也测不出下降。`niche` 的同一比值是 **6.62**。
2. `base` 的 `frugivory_frac = 0.0011`，§12.3 C 三读数之一「会不会塌回 0」在那里**已经是 0**，
   判据无法定义。
3. D 的参照系 §9.6 的 −38.3% 就是在厚果层世界测的。

**[本世界实测] 分辨率**：12×2 在 `niche` 主口径上，重叠度下降 20% 的功效是 **0.999**，
下降 10% 只有 **0.718**；p≤0.05 口径的 MDE = 0.410（占基线 11.0%）。
**`base` 的零结论只排除了 >0.0173 的偏好**，更小的果层偏好本设计看不见。

**[本世界实测] C/D 判据容差**（= 2 × 自估配对差噪声，`niche` 臂；
`explorations/20260803-overlapA/resolution_and_tolerances.py`）：

| 护栏 | `niche` 基线 | 容差（2×噪声） | 占基线 |
| --- | --- | --- | --- |
| `sel_ratio_water` | 3.7122 | ±0.879 | 23.7% |
| `population` | 1876.7 | ±592.5 | 31.6% |
| `carnivore_frac` | 0.1329 | ±0.0943 | **71.0%** |
| `frugivory_frac` | 0.2898 | ±0.0737 | 25.4% |
| `herb_water_dist` | 43.08 | ±5.68 | 13.2% |
| `forest_frac` | 0.2725 | ±0.0611 | 22.4% |

**两条必须跟着容差一起写的警告**：`carnivore_frac` 的 2×噪声占基线 **71%**、`population`
在 `niche` 世界 `σ̂_W=296` 而 `σ_B=0`——**C 阶段的单种子探针只能读「有没有塌到 0」，
不能读「有没有变差」**。`niche` 世界里 `population`/`frugivory_frac`/`herb_water_dist`/
`forest_frac` 的 **ICC 实测为 0**，对这四个护栏**配对种子买不到任何东西**。

**[本世界实测] 三条对我自己预注册口径的更正**（analyst 主动提的，都成立）：

1. **`sel_ratio_water` 单独回答不了「种群被挪走了没有」。** 它的零模型用的是**实测个体
   自己的水距边缘分布**——一个处理后变量。手术若把果层推向干旱带、种群跟过去一部分，
   零模型会跟着一起移。**「有没有离开河岸」必须另读 `herb_water_dist`**（`niche` 基线
   43.08）。C 的读数从三个扩到五个。
2. **`frac_in_fruit` 的「有效格」阈值是场最大值的 5%，而场最大值会随手术改变**，
   手术前后比的是不同的格集合。**必须与 `fruit_cells_eff` 一起读。**
3. **`schoener_d` 的预注册警告要更新**：它在 `base` 里被证实（D 0.1138 < 零模型 0.1210，
   12/12），但在 `niche` 里**符号翻正**（0.2482 > 0.1261，12/12）。所以「D 低于零模型」
   不是普遍现象，而是「果层没人吃时」的现象——在 C/D 实际发生的 `niche` 世界里，D 可用。

**[本世界实测] §12.4 风险 1 的读法要改。** 预标写的是「最可能的结局是没人去吃」。但
`niche` 世界的种群扣掉水距后仍以 3.71× 追踪果层，**更可能的失败模式是它们跟着果层走进
干旱带然后渴死**——即 `herb_water_dist` 与 `death_thirst_frac` 一起上升，而不是
`frugivory_frac` 塌。`measure_overlap.py` 已补上全程累加的死因份额。

**两条必须记住的局限**：
- ⚠️ **跨臂差不能归因给单一变量。** `niche` 是 5 项一起改的捆绑
  （`fruit_energy`/`fruit_regrow_baseline`/`fruit_water_frac`/`plant_max`/`regrow_baseline`），
  这是 R2 起就在用的那套。「变值钱就往上挪」只能写成「这套捆绑配置下」。
- ⚠️ **48 个 run 全在同一张地图上**（`fruit_cap_total` 只有一个取值）。本轮**全部指标都是
  空间性的**，所以结论只对**这一条河系**成立——**§12.3 E 的跨地形交叉是必须的，不是可选的。**

### 12.3.B B 阶段落地（2026-08-03）：旋钮已装，默认关且 bit-exact

`underworld/terrain.py` 的 `fruit_capacity` 那一行改成走一个 `habitat` 项，
`Config` 加三个字段：`fruit_dry_weight`（默认 **0.0**）、`fruit_dry_d0` / `fruit_dry_d1`。
**默认 0 时整个分支被跳过**，字段逐位不变、`golden` 未动（`check.py` 41 项绿）。

**实现上对 §12.2 的两处偏离，都写下来**：

1. **编译期门控，不是纯算术混合。** §12.2 提的是 `(1-w)*forest² + w*dry`；改用
   `if cfg.fruit_dry_weight > 0.0:` 包起来，与 `dynamics.graze` 里 `forage_tradeoff` 的
   写法一致。理由：`w=0` 时那行代码根本不执行，bit-exact 是**构造保证**的，而不是靠
   「`0.0 * dry` 恰好等于 0」——后者在 `dry` 出现非有限值时不成立。
2. **加了容量归一化（这条是必须的，否则 C/D 无法归因）。** 装上旋钮先量了一遍形状，
   发现 blend 到 `w=1` 把**果层总承载从 536.8 抬到 1026.3，+91%**。原因：`forest²` 在
   几乎所有格上都很小（窄带再平方），而 `dry` 在四分之一张图上饱和到 1，所以任何朝 `dry`
   的混合都顺带加厚果层。**那样的处理臂同时是「挪开了」和「厚了一倍」，任何效应都归因不了**
   ——而「加厚果层」已是被证伪的关闭路线（§8.3、`experiments.md` §5），也是 §12 明写的
   非目标。归一化后总承载逐点相等（+0.000%），空间挪动不受影响。**这与 §11 等量再分配是
   同一条纪律：永远把「在哪」和「多少」分开。**

**[本世界实测] 旋钮的剂量-形状表**（`terrain.build`，无 RNG，一次算清）：

| `fruit_dry_weight` | 0.0 | 0.25 | 0.5 | 0.75 | 1.0 |
| --- | --- | --- | --- | --- | --- |
| 承载加权水距 | **25.80** | 53.76 | 72.97 | 86.97 | **97.63** |
| 与 `forest` 相关 | **0.285** | 0.190 | 0.057 | −0.027 | **−0.073** |
| 有效格（≥5% 场最大） | 792 | 1346 | 1380 | 1024 | **726** |
| 正值格 | 2144 | 2144 | 2144 | 2144 | 1120 |
| 总承载 | 536.84 | 536.84 | 536.84 | 536.84 | 536.84 |

**§12.4 预标风险 3（`patch × dry` 把果层碎成零星几格）没有发生**：有效格数 792 → 726，
与基线同量级；`w=1` 时正值格从 2144 掉到 1120，但那是干旱带外的格被清零，不是碎裂。

**[本世界实测] 补一个预注册漏掉的读数：草层与果层的分离度本身。** §12 的目标是「把两个
饭碗在空间上拉开」，而 §12.3.C 预注册的五个读数**没有一个直接量这个**——它们量的都是
「种群相对果层」，不是「果层相对草层」。这个量只依赖地形、不依赖任何 run，所以补算是零成本的。
**（诚实标注：它是在看到 C 的数据之后才补的，不是预注册项。）**

| `fruit_dry_weight` | 0.0 | 0.25 | 0.5 | 0.75 | 1.0 |
| --- | --- | --- | --- | --- | --- |
| 果层重心水距 | 25.80 | 53.76 | 72.97 | 86.97 | **97.63** |
| 草层重心水距 | 49.75 | 49.75 | 49.75 | 49.75 | 49.75 |
| **重心间距** | −23.94 | **+4.01** | +23.22 | +37.22 | **+47.89** |
| Schoener D(草,果) | 0.1280 | **0.1468** | 0.1416 | 0.1305 | **0.0718** |

**这条曲线是非单调的，而且非单调的方式会误导工作点的选择**：草层重心并不在河边
（`capacity` 主要是 `grass_base` 的均匀底噪，重心 49.75 ≈ 陆地水距中位数 45.2），
而 `w=0` 的果层重心在 **25.80**——**两个饭碗本来就有 24 个世界单位的间距，只不过果层偏
河一侧**。于是把果层往外推时，它先**穿过**草层重心（`w=0.25`，间距只剩 +4.0、D 反而升到
最高的 0.1468），再才拉开。**`w=0.25` 是曲线上最差的点，不是「弱一点的处理」。**

（D 在这里仍带 §12.3.A 记过的集中度混杂——果层集中、草层摊开——所以**重心间距是更可靠的
那一列**，D 只作旁证。）

**[本世界实测] 顺带更正 §12.2 里两个参照数字**（12 种子 × 2 重复实测 + 一次 `terrain.build`）：
`river_half_width` 是 **8.0** 不是 16；`herb_water_dist` 基线是 **45.4** 不是 10–25。
干旱带的默认边缘据实测重定：陆地 `water_dist` 跨 0–148、中位数 45.2、p90 103.7，
所以 `d0=45`（陆地中位数起坡）、`d1=105`（接近 p90 饱和）。

单测 `tests/test_terrain_fruit_dry.py`（11 项全绿）：默认必须为 0、`w=0` 与旧公式**逐位**
相等（`patch` 从格心与 cfg 常量独立重建，不引用新代码的任何中间量，否则测的只是「新路径
等于它自己」）、`w=1` 与 `forest` 解耦且果层往远水处挪、**四个剂量点的容量中性**、
边缘有序、中间剂量的水距单调。

### 12.3.C C 阶段设计（先写后跑，2026-08-03，run_id: `20260803-dryC`）

- 脚本: `explorations/20260803-dryC/sweep_c.sh`，复用 `measure_overlap.py`
- 世界: **`niche`（厚果层）**，理由见 §12.3.A'
- 扫: `fruit_dry_weight ∈ {0.25, 0.5, 0.75, 1.0}`，`d0/d1` 先用默认 45/105。
  `w=0` 不重跑——那就是 A 的 `niche` 臂，24 个 run 已在 `outputs/20260803-overlapA/`。
- **每臂 3 个种子，不是 §12.3 C 原写的单种子**。理由是 A 就地测出了噪声：`niche` 世界里
  `carnivore_frac` 的 2×噪声占基线 **71%**、`population` 的 `σ_B=0`——**单 run 根本读不出
  护栏有没有变差，只能读有没有塌到 0**。3 个种子仍远低于判决协议（12×2），**本阶段
  照旧不产出结论**。

**五个读数（A 判决把三个扩成五个），先写清各自要看什么**：

| 读数 | 看什么 | `niche` 基线 | 容差（2×噪声） |
| --- | --- | --- | --- |
| `sel_ratio_water` | 重叠度下去了没有 | 3.712 | ±0.879 |
| `frugivory_frac` | **果实还吃不吃得到**——塌回 0 就等于把果层删了 | 0.290 | ±0.074 |
| `fruit_cells_eff` | 果层碎没碎（§12.4 风险 3） | 792 | — |
| `herb_water_dist` | **种群有没有离开河岸**（主口径回答不了，见 §12.3.A' 更正 1） | 43.08 | ±5.68 |
| `death_thirst_frac` | **跟着果层走进干旱带渴死**（§12.4 风险 1 的新读法） | 待测 | 就地自估 |

**这五者存在张力，很可能没有同时满足的点。若确实没有，本轮就此为负结果收口**
（写进 `experiments.md §5` 续判），**不要硬调**——尤其**不许为了让 C 好看而破坏
`fruit_dry_weight` 的容量中性**，破坏了 D 就归因不了。

#### C 阶段读数（15 run = 5 臂 × 3 种子；**探针，非结论**）

`w=0` 参照臂与 A 的 `niche` 臂交叉核对通过（`sel_ratio_water` 3.496 vs 3.562，差 +0.066，
远小于 A 自估的 `σ̂_W`=0.439）。`fruit_cap_total` 在真实仿真里也确认容量中性（五臂全部
536.842，偏差在第 4 位小数）。容差沿用 A 的自估值，**不是本轮自估**（`r=1` 估不出）。

| 读数 | w=0 | 0.25 | 0.5 | 0.75 | 1.0 | 容差(2×噪声) |
| --- | --- | --- | --- | --- | --- | --- |
| `frugivory_frac` | 0.339 | 0.316 | 0.360 | 0.311 | **0.363** | ±0.074 |
| `herb_water_dist` | 43.59 | **51.65** | **56.31** | **53.92** | **62.61** | ±5.68 |
| `death_thirst_frac` | 0.527 | 0.533 | 0.615 | 0.629 | **0.675** | 未测 |
| `carnivore_frac` | 0.203 | 0.192 | 0.110 | 0.118 | **0.063** | ±0.094 |
| `forest_frac` | 0.293 | **0.229** | **0.184** | **0.179** | **0.128** | ±0.061 |
| `population` | 1815 | 1614 | 2004 | 2130 | **2716** | ±593 |
| `sel_ratio_water` | 3.562 | **2.609** | 3.220 | 3.776 | **4.983** | ±0.879 |
| `fruit_cells_eff` | 792 | 1346 | 1380 | 1024 | 726 | — |

（**粗体 = 越过容差**。`fruit_cells_eff`/`fruit_cap_total` 是地形量，臂内跨种子 SD 为 0。）

**[本世界实测] 两条与 §12.4 预标相反的结果**：

1. **`frugivory_frac` 一个 `w` 都没塌**（0.31–0.36 全程）。§12.4 风险 1 写的「最可能的结局
   是没人去吃」**没有发生**。§12.3.C 那条「塌回 0 就等于把果层删了」的拦阻判据**没有触发**。
2. **`herb_water_dist` 每一个 `w` 都越过容差上升**（43.6 → 62.6）。
   **种群真的离开了河岸。** `water_system.md`、`herbivore_overpopulation.md`、`day_night.md`
   三份文档记的都是「水把一切钉在河边」这堵墙，而挪动果层把种群推离了它——
   `landscape_of_fear.md §6` 试过用恐惧场做同一件事，只弱成立（`carn_water_dist` +1.4）。

**代价也是实测到的**：`death_thirst_frac` 0.527 → 0.675、`carnivore_frac` 0.203 → 0.063、
`forest_frac` 每个 `w` 都越容差下降、`population` 在 `w=1` 升到 2716。

**[本世界实测] `sel_ratio_water` 在这里非单调（2.609 → 4.983），再次印证它回答不了「分离」**
——它的零模型是处理后变量，种群跟着果层走时零模型一起移。**分离要看 §12.3.B 那张地形级
重心间距表**（−23.9 → +47.9），护栏要看上表。`analyze_c.py` §3 的「判读」列用了
`sel_ratio_water` 当分离判据，**那一列不可采信**。

#### C 阶段判决（2026-08-03，由 `result-analyst` 出）：**没有可用工作点，按 §12.3.C 收口为负结果**

**决定性的一条不在种群上，在地形上：两个饭碗从来没有在格子级分开过，而且分不开。**

`ecology` 的草层承载是 `fertility = grass_base + forest_bonus · forest`，
而 `grass_base=0.65`、`forest_bonus=0.35` ⇒ **`fertility ∈ [0.65, 1.0]`，动态范围结构性
封顶 1.538×**。草沿水距近乎平坦（1.608 → 1.350，**全程只降 16%**），干旱带占陆地格
**50.2%**、占草层总量 **48.75%**。

**[本世界实测] 于是任何 `w` 下都不存在「果富草贫」的格子**：

| `fruit_dry_weight` | 0 | 0.25 | 0.5 | 0.75 | 1.0 |
| --- | --- | --- | --- | --- | --- |
| 果处草量 ÷ 陆地均值 | 1.181 | 1.098 | 1.040 | 0.998 | **0.966** |
| 果层坐在草分布第几百分位 | 86.9 | 72.4 | 56.0 | 39.2 | **28.9** |
| \|重心间距\| ÷ w=0 基线 | 1.00 | **0.17** | 0.97 | 1.55 | **2.00** |

把果层推到最远（`w=1`），它所在的格子草量也只有陆地均值的 **0.966 倍**——
**没有一处是「有果实、没有草」的**。**手术能造出的唯一差别成本是水，不是食物。**

**[本世界实测] `w=0.25` 是自带的反证**：那里地形级重心间距只剩 **+4.01**（≈零分离），
`herb_water_dist` 却照样上升 **+8.58（+4.93 SE）**。**没有分离，照样能把种群赶出去**——
所以 C 观测到的「种群离开河岸」**不是分割的证据，只是「果实在哪，人就跟到哪」**。

**[本世界实测] 护栏只破了一项：渴死。** 而且我先前读出的「食肉者崩了」是**锚点假象**——
`dryC` 的 `w=0` 参照臂 `carnivore_frac=0.2028` 被单个 run 抬高（seed 2 = **0.4015**，
高于 A 的 24 个 `niche` run 的最大值 0.2536）。改用 A 的 12×2 基线 0.1329 重锚后：

| 护栏 | A 的 12×2 基线 | `w=1.0` | Δ / SE | 判 |
| --- | --- | --- | --- | --- |
| `carnivore_frac` | 0.1329 | 0.0628 | −2.35 | **未破**（在 A 容差 ±0.0943 内） |
| `death_thirst_frac` | 0.5225 | 0.6746 | **+3.98** | **破**（`w=0.5/0.75` 亦越 2 SE） |
| `frugivory_frac` | 0.2898 | 0.3627 | +3.23 | **没塌**（风险 1 确未发生） |
| `population` | 1876.7 | 2716 | +4.63 | 升，非崩 |

渴死绝对量 150121 → 206599（**+37.6%**），死因预算从捕食 0.358 转向渴死 0.227→…。
**这正是 §12.3.A' 改写过的失败模式：不是「没人去吃」，是「跟着果层走进干旱带然后渴死」。**

**[本世界实测] 主口径 `sel_ratio_water` 还有第二个独立的失效理由**（我先前只找到一个）：
它的**可达上限本身随臂缩小 2.0×**。即使把水距边缘分布**固定**在 `w=0` 那一档，
理论上限仍从 24.99 掉到 12.43——**量纲缩小是场变了造成的，与种群移不移动无关**。
去掉量纲后（实测 ÷ 该臂上限）它**单调上升**：0.143 → 0.133 → 0.208 → 0.276 → **0.414**。
**所以它在任何读法下都不能跨臂比较**，`analyze_c.py` §3 的「分离✓/✗」列作废
（它还恰好把地形级最差点 `w=0.25` 判成唯一的 ✓）。

**统计地位说清楚**：本阶段 n=3 × r=1，配对符号秩双侧 p 地板是 **0.25**，
**没有任何 p 可能 ≤0.05**。但**判负的三条理由里有两条是纯地形确定值**
（草层动态范围封顶、果处草量 ≤ 陆地均值），不受 n=3 限制。`death_thirst_frac` 的噪声是
从 `outputs/20260803-partition/P_tradeoff0`（6×3、同世界参数、同护栏口径）**借来的**，
不是本轮自估——本轮 3 个种子的散度是 χ²(2) 估计，本身不可信。

**因此 D 阶段不跑。** 按 §12.3.C 写死的规矩收口为负结果，负结果进
`docs/experiments.md §5.6`。**不是「D 判负」——D 从未运行**；被证伪的是它的前提：
**这个世界做不出「两个饭碗在空间上拉开」这件事**，因为其中一个饭碗铺满了整张地图。

### 12.4 预标风险（先写下来，别事后解释）

1. **水会撞上来。** 这个世界的一切都被渴死钉在河边（`water_system.md`、
   `herbivore_overpopulation.md`、`day_night.md` 三份文档记的都是这堵墙）。
   把果实挪到干旱带，最可能的结局是**没人去吃**——那 `frugivory_frac` 会塌，
   C 阶段就该拦住，不要带着塌掉的果层进 D。
2. **「分离」和「可达」是对立的。** 分得越开越容易出分化，也越容易变成把果层删掉。
   C 阶段扫的就是这条张力曲线，**如果曲线上没有可用点，这本身就是结论**——
   它会把「水是这个世界一切的天花板」从三条独立证据变成四条。
3. **`patch` 与 `dry` 可能不兼容**：`patch` 是正弦格，`dry` 是环河带。两者相乘后
   果层可能碎得只剩零星几格。**A 阶段的重叠度脚本要顺手报出「果层有效格数」**，
   在 B 之后立刻复查一次。

### 12.5 若 D 为负

**不要再调果层的任何参数。** 三条资源轴（加厚 / 等量再分配 / 空间分隔）就全部走完，
结论是「在水锁死承载力的前提下，这个世界坐不下第二个食草生态型」。
那时唯一还没试过的是**放松水本身**——但那是重设世界，属于比本轮大一个量级的决定，
写进 `HANDOFF.md` 交用户，不擅自开跑。

---

## 13. 【R9】把两个饭碗分开——从**草层**下手，不是果层（2026-08-03，run_id: `20260803-shade`）

### 13.0 授权与它踩到的那道闸

§12.5 写死了一条闸：「放松水本身……那是重设世界，属于比本轮大一个量级的决定，
写进 `HANDOFF.md` 交用户，不擅自开跑。」`.context/current-focus.md` 把同一件事列为
两个待拍板方向之一。**本轮的 `water_sea_dist` 就是那件事，不要粉饰成「顺手修个 bug」。**

授权来自用户本次会话的开场：目标仍是「小型生态群落 + 性状演化」，并要求
「按照这个目标一直往下进行推进」「可以提出并实践一些新奇的 idea」。§12.5 等的就是这个签字。
**但闸的另一半仍然生效**：两个旋钮默认关、golden 不动，**翻不翻默认值由 R9 的数据定**，
不夹在实验里一起做。翻默认值的确切代价见 §13.2 末尾的表。

### 13.1 病根：实现与自己声明的设计意图反号 [对应]

八个回合动的一直是**果层**（加厚 §5–§8、等量再分配 §11、空间分隔 §12）。
§12.3.C 判负时自己指出了唯一没走过的方向：**能挪的只有果层，而另一个饭碗无处不在。**

`config.py` 在 `fruit_max` 上方写着这个世界为什么要有果层，原文：

> "Forests hold far more total biomass than grassland but far *less* that a grazer can
> eat -- woody, tough, defended, **with the canopy shading out the herb layer**. That is
> why savanna carries the large-herbivore biomass and rainforest does not. … **canopy is
> not simply "more food" here; it is a low grazing floor with a high-value exception
> scattered through it**."

而 `terrain.py` 写的是 `fertility = grass_base + forest_bonus * forest`，`forest_bonus = +0.35`：
**满冠层的草承载是空地的 1.538 倍——林冠是全世界草最肥的地方。**
注释说林下应当是「低放牧底」，代码给的是「最高放牧顶」。

**这不是调参分歧，是实现与注释反号。** 而这一个正负号，正是三轮资源分割实验死在的那个几何：
果层由 `forest²` 门控长在林冠里，林冠又是草最肥的地方 ⇒ 两种资源完全共址 ⇒
「两样都能吃」严格优于专精（§9.6 / §9.10 / §9.11 实测的中间型最优）。

**注意与 `experiments.md §5.6` 提案的符号相反**：那里写的是「压 `grass_base`、**抬**
`forest_bonus`」。抬 `forest_bonus` 会让草**更**集中在果实所在处，加重重叠。
本轮走的是相反的符号。

### 13.2 第二处：`water_dist` 是「到最近**河**的距离」，不是它声称的「到最近河/海的距离」[对应]

**[核查记录] 本节措辞被 `claim-verifier` 收紧过一次。** 我最初写的是「实现与两处注释都不符」，
那是过度声称：**喝水那半是对的**。收紧后的正确措辞如下。

- `terrain.py` 是 `jnp.where(is_sea, 0.0, d_river)`：海格归零，陆地格一律读 `_dist_to_rivers`
  的结果，**到海的距离从未被计算**。`Terrain.water_dist` 的字段注释
  `distance to nearest river/sea` 因此对全部 13568 个陆地格都是错的。
- `build()` 里那段注释**半真**：
  - **喝水这半成立** —— `dynamics.py` 查 `wd < river_half_width`，站上海格 `wd=0` 就能喝，
    `underworld/` 里也没有任何地方拦住个体走进海。
  - **水感这半只在岸线一格内成立** —— `sensors.py` 每扇区只在 `food_sample_dist=9` 处采一格，
    采样点落进海格才读到满值。**所以海在陆地上没有距离梯度，只有岸线处的一个阶跃**，
    有效探测距离约 11 世界单位，而河是 `vision_radius + food_sample_dist = 30`。
- **这是无记录的疏漏，不是刻意设计**：该行自 `32c983f`（原始地形提交）起未改动，
  该提交信息未提及此事；`docs/` 全量检索无任何「海不提供水距梯度」的决定。反向证据两条——
  `water_system.md:416`「水只在河道/**海岸**」与 `biology.md:136`「**河海**是食草动物唯一水源」
  都按「海是水」在用这个字段。**没有任何文档站在实现这一边。**
- **为什么一直没被看见**：`server/protocol.py` 把水编码成 `w = 1 - clip(wd/river_half_width)`，
  海格 `wd=0` 在仪表盘上**渲染成满水**——看上去一切正常。

[本世界实测]（`explorations/20260803-shade/terrain_shade.py`，只读 `terrain.build`，无 RNG）：

| 读数 | 值 |
| --- | --- |
| 水距被高估 >1 单位的陆地格 | **3688 / 13568 = 27.2%** |
| 受影响格的中位高估幅度 | **43.3 世界单位**（最大 101.8） |
| 陆地水距中位数 现状 → 认海 | 43.8 → **34.5** |
| 可直接饮水的陆地格占比 | 7.97% → **10.52%** |
| 海岸带（离海 <8 单位的 376 个陆地格）现状水距中位数 | **54.7** |

海岸带同时是：`forest` 均值 0.123（陆地 0.274）、果承载 0.0163（陆地 0.0396）
——**它本来就是开阔草地，只是被判成了旱地**。而今天的海是一条「能喝水但没有食物」的死区
（`capacity` 在 `is_sea` 上被清零）。认海把海岸线变成「有草且能喝水」的新生境，
而 `forest` 只有 0.123 意味着**遮荫打不着它**——这正是两个旋钮互补的机制。

**翻默认值的确切代价（`claim-verifier` 逐条核出，先写在这里，不要事后才发现）**：

| 影响 | 具体 |
| --- | --- |
| ❌ **结论方向翻转** | `biology.md:156`「有效感知上限 30 < 全格离水中位 35.5 ⇒ 半张图上 8 个水通道恒为 0」——改后中位 **27.45 < 30，不等号反向**。这是「渴死是导航问题不是营养问题」的支柱，且被 `water_system.md:418`、`trait_roadmap.md` 的 `vision_radius` 主线引用。 |
| ❌ **旋钮标定作废** | `fruit_dry_d0=45.0 / d1=105.0` 明写「ramp 起于陆地中位、饱和于 p90」，锚的是 45.2/103.7 → 改后是 34.8/74.4。已在 `config.py` 注释里记下「不要两个旋钮一起开」。 |
| ⚠️ **零模型要重算** | `conventions.md §7` 的 `inland_frac` 零模型 0.556/0.675/0.650 → **0.469/0.565/0.545**；`scale_and_density.md:108` 的 35.5/41.6 → 27.45/31.30。 |
| ⚠️ **指标与零模型一起动** | `biology.md:219`「`herb_water_dist` 35.19 ≈ 中位 35.46 ⇒ 空间分布基本无偏」——两边都出自同一个 `water_dist`，必须整轮重跑才能重述。 |
| ✅ **不受影响** | §12 判负的决定性论证（`1.538×` 封顶是纯代数）；`water_system.md` 的水经济学五臂消融（与空间场正交）；`biology.md` 的 piosphere 分带（各带里离海 <8 的格只占 2.0–3.4%）。 |

### 13.3 手术：两个默认关、bit-exact 的旋钮（`2bb48e0` 已落地）

1. **`grass_shade`（默认 0.0）**——`fertility = clip(grass_base + forest_bonus·forest
   − grass_shade·forest, 0, None)`，随后**等量归一化**。照抄 `fruit_dry_weight` 已确立的
   纪律：**只挪不加，把「在哪」和「有多少」分开**。
2. **`water_sea_dist`（默认 False）**——`water_dist = min(d_river, d_sea)`。

**`grass_shade ≤ 1.0` 有一个零代码等价实现，已收进单测当独立对照**：`forest_bonus` 没有
clamp、全仓唯一消费者就是那一行，所以 `grass_base + (forest_bonus − s)·forest ≡
grass_base + forest_bonus′·forest`，两条路径只差一个被归一化除掉的全局标量。
**旋钮真正挣到自己位置的是 >1.0 那段**——那里 `clip(...,0,None)` 才生效，
而那个 clip 是「有果实、没有草」**唯一**的来源（零草陆地格 5.0% → 11.4%）。

### 13.4 地形级判据：先算地形，再跑种群（已做完）

§12 花 63 个 run 才判负，而最硬的两条证据只要 `terrain.build` 跑一次
（`MEMORY.md [LEARN:method]`）。本轮反过来做。脚本
`explorations/20260803-shade/{terrain_shade,terrain_cross,terrain_criterion}.py`，
输出 `outputs/20260803-shade/{cross,criterion}.txt`。**numpy 重算已对着 `terrain.build`
逐格核过**（`max|Δ| ≤ 9.5e-7`，f32 舍入量级）——重算写错了，下面每一个数都是错的。

**判据（§12.3.C 判负时点名缺的那个前提）：存不存在「有果实、没有草」的格子。**

| 臂 | 果富十分位处草量÷陆地均草 | 「果前10% ∩ 草<30%均值」格数 | 草重心水距 | 可饮格% | 专精可得回报\* |
| --- | --- | --- | --- | --- | --- |
| 现状 (off, 0) | **1.307** | **0** | 49.75 | 7.97 | 0.446 |
| shade 单独 (off, 1.0) | 0.491 | 24 (11.2%) | **59.27** ⚠ | 7.97 | 0.873 |
| 认海单独 (ON, 0) | 1.285 | 0 | **37.22** | 10.52 | 0.446 |
| 两者 (ON, 1.0) | 0.519 | 20 (9.3%) | 41.76 | 10.52 | 0.859 |
| **两者 (ON, 1.3)** | **0.125** | **173 (80.5%)** | **44.15** | 10.52 | **0.959** |
| 两者 (ON, 1.6) | 0.011 | 209 (97.2%) | 45.81 | 10.52 | 0.993 |

\* 果富格与草富格之间「果实能量份额」之差，`fruit_energy=4.0`（niche 臂）。
这是**专精的可得回报**：一个个体从草区搬到果区，食谱里果实份额能变多少。0 ⇒ 搬家不改食性。

**三条读法**：

1. **基线那个 0 是字面的**——现状世界里「果承载前 10% 且草承载低于陆地均值 30%」的格子
   **一个也没有**。§12.3.C 说的「没有一处」不是修辞，已收进单测。
2. **单独遮荫会撞水墙**：草重心水距 +19%，而 §12.3.C 实测 `herb_water_dist` 只要
   +4.93 SE 就破渴死护栏。
3. **认海买来的水距预算够付掉整个遮荫的代价还有剩**：ON+1.3 的草重心水距 **44.15 < 基线 49.75**。
   **这是八个回合里第一次「分离」与「可达」不再对立。**

草总量 20366.5 → 20711.5（+1.7%）、果总量 536.8 → 590.7（+10.0%），两者都由认海引起。
**果层那 +10% 是一个真实的混杂**（加厚果层已是关闭路线，`experiments.md §5`）——
本轮的处理是**把 `water_sea_dist` 钉在 ON 跑遍全部四臂**，让它成为世界的一部分而不是因子，
于是它对任何跨臂对比都是常量。代价是本轮不回答「认海本身有没有用」，那留给 R9-E。

**看得见的版本**：`outputs/20260803-shade/bowls_{base,r9}.png`（绿=草、红=果、黄=重叠）。
基线图上每一个果斑都是黄的；R9 图上林带里的果斑是纯红的，外面的平原是纯绿的。

### 13.5 种群实验设计（先写后跑）

**世界（全部四臂共用）**：`niche` 厚果层配置，出处 `outputs/20260803-partition/provenance.txt`
——`fruit_regrow_baseline=0.25 fruit_energy=4.0 fruit_water_frac=0.40 regrow_baseline=0.010
plant_max=2.0`，**外加 `water_sea_dist=1`**。
**为什么不是默认世界**：§12.3.A' 已判定——默认世界里果层与种群的重叠是纯几何的，
可下降空间 ÷ MDE 只有 0.52，手术做对了也测不出来。

**设计**：2×2 因子（`grass_shade` × `forage_tradeoff`），**12 种子 × 2 重复 = 96 run**，
20000 步，并发 6。

| 臂 | `grass_shade` | `forage_tradeoff` | 是什么 |
| --- | --- | --- | --- |
| `N0` | 0.0 | 0.0 | 平世界 + 中性漂变（基因被编译期断开） |
| `T0` | 0.0 | 1.0 | 平世界 + 最大选择强度（≈ §9.6 的条件） |
| `N1` | 1.3 | 0.0 | 结构世界 + 中性漂变 |
| `T1` | 1.3 | 1.0 | 结构世界 + 最大选择强度 ← **处理臂** |

**为什么 `forage_tradeoff` 必须是因子而不是常量**（`plan-critic` 拦下的第一个 BLOCKER）：
§9.6 的基线不是「方差多大」而是「**处理臂比中性漂变窄 38.3%**」。没有同世界的中性对照，
「方差变大了」这句话无法成立——不同世界的漂变方差本来就不同。
`forage_tradeoff` 默认 **0.0**，此时基因对采食零效应（`config.py` 明写 "ZERO effect"），
照 §12 那样只跑 sea × shade 会让 96 个 run 全是纯漂变，什么都判不出来。

**主判据（先写后跑）**——**交互项**，每个种子一个数：

    d_s = (T1_s − N1_s) − (T0_s − N0_s)

- **H1 形状** —— `forage_pref_std` 的 `d_s`：12 格均值配对 Wilcoxon 双侧 p ≤ 0.05，
  方向 ≥10/12，**且报出效应 ÷ 本实验自估的 `√2·σ̂_W/√r`**；比值 <1 即标注功效不足，
  **即使 p 过线**（`conventions.md §5`）。
- **H2 形状** —— `blrt_lr_per_n` 同上。
- **H3 空间（本轮新增，这才是「局部多物种」的字面含义）** ——
  个体的 `forage_pref` 与它所在格「果实能量份额」的加权相关 `ρ_space`。
  零模型 = 把个体位置随机置换 200 次（保持基因值与占据格的边缘分布）。
  预测：T1 的 `ρ_space` 显著 > 0，且 `d_s(ρ_space) > 0`。
  **H1/H2 说「基因分开了」，H3 说「它们住在不同地方」。只有 H3 成立才算生态位分化。**

**护栏（容差一律对着本轮 `N1` 臂自估的 `√2·σ̂_W/√r` 定，不凭直觉——§11.5 就是因为拿
±10% 当容差而判成「无法判定」）**：

- `death_thirst_frac` —— 历次破得最多的一条（§7 破 2–3/6、§8 三臂全破、§12 +37.6%）。
  容差 2 SE。
- `frugivory_frac` —— §12.4 风险 1 的拦截：林下无草之后没人进林子，就是把果层删了。
- `carn_speed` / `herb_speed` —— 风险 1（峰值 +39% ⇒ 伏击优于追击）的**指定读数**，
  `config.py` 的 `plant_max` 调优记录用的就是「食肉者平均速度是否向食草者靠拢而不是衰减到 0」。
- `carnivore_frac` / `population` / `min_pop` —— 灭绝与超调。

**尺子警告（`plan-critic` S-1）**：`herb_water_dist` / `carn_water_dist` / `water_bound_frac`
/ `inland_frac` 全部由 `terrain.water_dist` 算出，而认海换了这把尺子。四臂之间可比
（尺子相同），**但一律不得与 §12.3.A 归档的 `herb_water_dist=45.4`、`conventions.md §7` 的
`inland_frac` 零模型并列**。跨臂报这四个量时另用默认（river-only）水距场固定尺子再算一遍。

**失败判据与放弃条件**：

- H1 与 H2 都不成立 ⇒ **「空间分隔造生态位」整条路线关闭**，连同 §12 的负结果一起升级写进
  `experiments.md`，结论改为「在这个世界里资源分割不可达，下一步只能换轴（营养级 / 时间）」。
- H1/H2 成立但 H3 不成立 ⇒ **性状分化而非生态位分化**，单独记，不得当成多物种。
- **渴死若再次逐种子破半数，按 §12.5 收口，不降 `grass_shade` 剂量重试。** 剂量重试是
  §12 C 阶段已经走过且判负的路，重走一遍只会再花 15 个 run 得到同一个「没有可用工作点」。

### 13.6 预标风险（先写下来，别事后解释）

1. **等量归一化把草层峰值从 2.195 抬到 3.051（+39%）。** `config.py` 的 `plant_max` 有一整段
   调优记录：`plant_max=5.0` 会让伏击优于追击、`plant_max=1.2` 会饿死捕食者。
   这不是归一化选择的产物——**任何造出空间结构的做法都要么抬峰值、要么减总量**。
   已核：`sensors.py` 的 `food = edible / plant_max` **没有 clip**，所以感知不会饱和，
   只是整体放大；`ecology.regrow` 与 `check.py` 的断言都对着 `terrain.capacity` 而非
   `plant_max`，不会被打破。**但 `server/protocol.py` 的植物平面是
   `clip(plant/plant_max, 0, 1)`，遮荫臂在仪表盘上最肥的草会糊成一片饱和色**——
   要看世界就先调显示分母，并**截图确认**（`CLAUDE.md`：GPU/canvas 输出要看，不能只推理）。
2. **认海可能才是危险的那个。** 可饮格 +32%（相对），而 `herbivore_overpopulation.md` 记过
   超调。种群可能直接炸上去，把分化淹没在密度效应里。由 `population` / `min_pop` 拦。
3. **果层可能没人去。** §12.4 风险 1 的重演。由 `frugivory_frac` 拦。
4. **伪重复**：全部 96 个 run 仍在同一张地图上（`terrain.build` 不吃 RNG）。结论是
   「这条河系 + 这条海岸线」的，不是「河与海」的。跨地形交叉留作 R9-E。

### 13.7 判决（2026-08-03，96 run 全部完成、0 错误）

**判决全文在 `docs/multispecies_feasibility.md` §10**（结论归专题文档，纲领只留判决行）。
一句话：**分离做到了，物种没做出来**——遮荫世界里选择造出了稳健的生态型—生境关联
（原始 r：`T1−N1` = −0.2697，**12/12**，p=0.00049 地板，效应/噪声 −1.672，本纲领第一个
空间生态型信号），但基因**均值**动了而**方差与双峰性没动**：
**方向性选择 + 空间分选 = 一条 cline，不是分歧选择，不是两个物种。**

- **H1 / H2 判 undecidable（功效不足，功效 0.159 / 0.216）**，故 §13.5 那条
  「H1 与 H2 都不成立 ⇒ 整条路线关闭」的失败判据**不触发**——理由是功效，不是结果好看。
- **§13.5 的 H3 符号写反了**：`forage_pref` 是**草**偏好基因
  （`dynamics.py`：`grass_mult = 1 + tradeoff·(2·pref−1)`），所以实测的负号才是生境匹配方向。
  代码与数据两条独立证据见 §10.3。
- **主控一句结论被复核推翻**：不能说「稳定化塌缩被地形解除了」——那句和交互项
  （p=0.339）是同一件事，且 `T1−N1` 的 CI 含平世界的塌缩量。诚实版本见 §10.5。
- **根因量化**：果层只占陆地能量的 **11.15%**（草承载 18828.7 vs 果承载×4 = 2362.8，
  实跑配置 `plant_max=2.0`）。一个数一次解释完三个结果。
  **下一轮的自变量是果层能量占比，不是分离度**——分离度已到顶（果富格草量中位 0.000×）。

### 13.8 勘误：§13.4 的两个绝对草数算在了另一个世界上

`terrain_cross.py` / `terrain_criterion.py` 第一版写死 `Config()`（`plant_max=2.2`），
而 96 个 run 跑的是 `--set plant_max=2.0`。

- **§13.4 表里的全部比值不受影响**（`plant_max` 是全局标量，等量归一化又保总量）——
  已在真实配置下逐条复核，`果富处草量÷均草`、`零草格%`、`果能份额之差`、
  「果前10% ∩ 草<30%均值」计数**全部逐条对上**（`outputs/20260803-shade/criterion_runconfig.txt`）。
- **两个绝对数错 10%**：草层总承载 **20711.5 → 18828.7**；
  **§13.6 风险 1 的草峰值 2.195 → 3.051 应为 1.9951 → 2.7737**（相对 +39.0% 仍然对）。
  真实峰值离 `plant_max=5.0` 那条警戒线比预注册估计的**更远**，风险比预登记时略小。
- **§13.4 那句「numpy 重算已对着 `terrain.build` 逐格核过」要收紧**：那次核的是默认配置，
  没覆盖实验配置。两个脚本已改成接 `--set`。
